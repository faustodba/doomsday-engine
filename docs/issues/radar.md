# Issues — Radar & anagrafe membri

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 7 voci totali · 2 aperte · 5 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| — | Bot in modalità raccolta-only (28/04 19:45) | — | ⏸️ Tutti i task disabilitati da dashboard tranne `raccolta` (always) + `radar_census`. Modalità sicura mentre si indaga issue arena #83/#84. Ri-abilitare task uno alla volta dopo fix |
| WU158 | Anagrafe avatar membri alleanza — POC validato 99% accuracy | — | 🟡 POC IMPLEMENTATO 12/05 (script standalone `c:/tmp/test_anagrafe_*.py`), **integrazione bot pending**. **Obiettivo**: banca dati visiva di tutti gli avatar (~103 per HE-DAWN) navigando Alliance→Members→R4/R3/R2/R1 con scroll incrementale + dedup pHash. Usabile dal task radar per match avatar sulla mappa (identificare membri propria alleanza vs esterni). **Calibrazione per sezione (960×540)**: R4 (3 col box60 stride77 1frame), R3 (2 col box60 stride68 scroll 204px = 3 stride), R2/R1 (2 col box52 stride65 scroll 130px = 2 stride). Y_TL anchored al badge della sezione corrente + offset calibrato (R4+38, R3+32, R2+37, R1+37). **Slow_drag** duration 1500 ms = scroll lineare 1:1 (no momentum). **OCR contatori** (ROI x=780-860 escluso icona people, Otsu PSM7) → atteso pre-scansione (R4=8/8, R3=17, R2=30, R1=48). **Stop dinamico**: detect badge sezione successiva (sc≥0.85) → leggi TUTTE righe valide entro screen + STOP (no filtro margine, il pHash dedup gestisce overlap). R1 (ultima): backup `no_new ≥ 3`. **Filtro avatar**: edge density Canny > 0.08 AND grayscale std > 25 (discrimina avatar da testo/bg vuoto). **Dedup**: pHash 64-bit DCT-based + Hamming distance ≤ 6 (avatar default visivamente identici uniti in 1 entry). **Validazione**: FAU_05 = 104/103 (99% — 1 falso positivo R2), FAU_04 = 102/103 (con drift). **Trigger design**: flag `globali.radar_anagrafe_pending` autocancellante (pattern come "riavvia bot ciclo"), bottone UI manuale. Prima istanza ordinaria che vede flag ON lo esegue (FauMorfeus master escluso). **Memoria dettagliata**: `project_anagrafe_membri.md`. **Pending**: integrazione `shared/avatar_registry.py` + `tasks/radar_anagrafe.py` + endpoint + UI + match runtime in `radar_actions.py`. |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 18-bis | `radar_tool/templates/` mancante (dev+prod) | BASSA | ✅ RISOLTA 28/04 (47 template copiati da V5 `C:/Bot-farm/radar_tool/templates/` a V6 dev+prod, smoke test `radar_tool/_smoke_test.py` OK: 12 icone rilevate su sample. RF classifier conf bassa <0.60 → fallback heuristic via nome template funzionante. Re-training RF rimandato — non bloccante per produzione) |
| WU115 | Sistema debug screenshot unificato — hot-reload + dashboard toggle | MEDIA | ✅ IMPLEMENTATA 03/05 notte (Step A-E) + ESPANSA 04/05 (Step F, 14 task). Architettura indipendente dal funzionamento del bot, attivabile/disattivabile per ogni task **senza riavviare** via dashboard. **Step A-E**: `shared/debug_buffer.py` (NEW ~220) con `DebugBuffer.for_task()` factory + cache TTL 30s + `cleanup_old(days=7)`; schema config `runtime_overrides.json::globali.debug_tasks.{task}`; endpoint `/api/debug-tasks` JSON + `/ui/partial/debug-tasks` HTMX pill toggle in index.html; migrati 3 task originali (arena, arena_mercato, store) rimuovendo 3 toggle modulo-level legacy. **Step F**: pattern esteso a 14 task (vip → messaggi → boost → alleanza → donazione → radar → radar_census → truppe → zaino → main_mission → raccolta(+chiusura sub-classe) → rifornimento → district_showdown), ognuno con anomalia logic semantica via parametro `force=`. Lista finale `_KNOWN_TASKS`: 17 task (3 originali + 14 nuovi). **Effetto operativo**: utente abilita/disabilita debug per qualsiasi task da dashboard → al prossimo run del task (qualsiasi istanza) i screenshot vanno su `data/{task}_debug/`; senza anomalia → nessun file. Effetto al prossimo restart bot (instance metadata propagation). |
| WU147 | Rifornimento — nuovo pin centratura mappa + ROI dinamica + tap su centro pin | MEDIA | ✅ IMPLEMENTATA 11/05 mattina. Sostituito template centratura mappa con `pin/pin_rifugio.png` (27×28, copia da `c:/radar_tool/templates/`). Schema config esteso: `AVATAR_MAPPA_TEMPLATE` separato da `AVATAR_TEMPLATE` (ramo membri lascia `pin/avatar.png`). ROI strategia 2-step: **primaria 200×200** `(380,170,580,370)` centrata su centro mappa (480,270); se `score<0.75` retry **300×300** `(330,120,630,420)`. Tap castello: `(r.cx, r.cy)` = centro esatto del template matchato (no più `OFFSET_Y` hardcoded). **Validazione live FAU_00 11/05 08:46**: pipeline completa OK in 35.9s — match pin score=1.000 ROI primaria · tap (487,199) · RESOURCE SUPPLY score=0.999 · provviste 65M · DRL 200M · spedizione pomodoro lordo 3.977M / netto 3.499M (tassa ~12%). Bug correlato scoperto: `_compila_e_invia(nome_rifugio="placeholder")` triggera DEST MISMATCH OCR → BACK silenzioso; nello script test passare `nome_rifugio=""` per skip. **Diagnosi che ha portato al fix**: best-match globale del template ha dato score=1.000 a `(487,199)` mentre ROI 100×100 attorno a `(480,270)` non lo conteneva — il pin è ~71 px SOPRA il centro mappa. Test live in `c:/tmp/test_rifornimento_pin_rifugio.py`. |
| WU150 | Radar Actions — dispatcher post-pallini + handler card (GO+RESCUE) | MEDIA | ✅ IMPLEMENTATA 11/05 pomeriggio (commit `bf1dbcd`). Nuovo modulo `tasks/radar_actions.py` con orchestratore `process_radar_actions()` che integra il flusso completo del radar: (1) `_loop_pallini` esistente (tap pallini rossi), (2) wait 10s animazioni, (3) `RadarCensusTask` filtrato per categorie con handler, (4) dispatch `{categoria → handler}` per ogni record actionable, (5) loop fino a 0 pallini AND 0 actionable (max 10 iter safety). **Handler `card` (Protect Survivors)**: tap card → wait 2.5s → tap GO (90,465) → wait → tap RESCUE (233,386) → wait → tap RADAR_ICON (78,315) → torna mappa radar. Coord fisse calibrate live FAU_01: validate end-to-end con 3 cicli (2 card processate + ricomparsa pallini su (747,263)+(406,287) + tap pallini → 0 pallini). 10 categorie aggiuntive (skull/pedone/soldati/avatar/paracadute/camion/fiamma/bottiglia/numero/auto) con placeholder pronto per estensione handler. **`tasks/radar.py::run()`** modificato: sostituito blocco standalone `_loop_pallini` + census opzionale con singola chiamata a `process_radar_actions()`. Mantenuto census legacy opzionale (`RADAR_CENSUS_ABILITATO`) per compat. `_data()` esteso con `card_processate` per telemetria. **Test live `run_task.py --istanza FAU_01 --task radar --force`**: ESITO OK 50.8s, iter=1 (radar già pulito dai test debug precedenti), 0 pallini + 0 actionable → exit pulito. Cosmetic charmap errors residui in log Windows console (`→`/`✓` in radar.py legacy + run_task.py) non bloccanti (fail-safe True). Sync prod via cp tasks/radar*.py. Effetto al prossimo restart bot. |
| WU151 | Radar: popup pre-detect + safety break loop sterile + UTF-8 run_task | ALTA | ✅ IMPLEMENTATA 11/05 sera (commit `a636706`). **Sintomo prod su FAU_02**: 52 pallini "tappati" in 8 iter del nuovo `process_radar_actions`, 0 card processate. Diagnosi via `map_full.png` ciclo 18:01: popup card "Assist Ally" era già aperto a inizio task (residuo da ciclo precedente) → `_loop_pallini` rilevava falsi positivi nel popup (timer rosso, UI elements) → tap intercettati dal popup, no effetto → census riconosceva icone SOTTO il popup (pedone/avatar/paracadute/skull) ma non riconosceva il popup come card → 0 actionable → loop sterile fino a max_loops. **Fix A** (popup pre-detect): `_is_card_popup_open(ctx)` pixel test su ROI 11×11 attorno a (90,465) bottone GO — se `R>140 AND (R-B)>50` (arancione/giallo brillante vs terreno marrone) → popup OPEN. All'inizio di `process_radar_actions`, se popup aperto → `_resolve_card_popup()` (GO + RESCUE + RADAR_ICON) PRIMA del loop. Refactor `handle_card` split: `tap_card + _resolve_card_popup` riusabile. **Fix B** (safety break): counter `stagnant_iter` su iter consecutivi con `pallini>0 AND processed==0 AND pallini==last_pallini`; soglia `MAX_STAGNANT=2`. Su abort: tentativo recovery via `_resolve_card_popup` se popup detected. **Bonus fix `run_task.py`**: `sys.stdout.reconfigure('utf-8')` all'inizio per evitare charmap errors su Unicode (→/—/✓) console Windows cp1252 — era causa di `_loop_pallini` exception ("scan vuoti consecutivi — completato" pos 43). Telemetria estesa: dict result include `popup_pre_resolved: bool` + `stagnant_abort: bool`. **Effetto attivazione**: al prossimo restart bot prod (Python carica moduli al boot, no hot-reload). Le istanze del ciclo corrente con popup residuo continueranno a fare loop sterile fino a restart. **⚠ FIX A REVERTITO 14/05** (commit `1aa637a`): il pixel test su (90,465) causava falsi positivi su elementi mappa luminosi → `_resolve_card_popup` cieco apriva/chiudeva pannelli su UI sbagliata e bloccava i tap pallini successivi. FIX A rimosso. Il caso popup residuo a inizio task è ora gestito SOLO da FIX B con overhead ~30s (3 iter × 10s wait). `popup_pre_resolved` nel dict telemetria resta per compat ma è sempre False. Verifica prevista 15/05: cercare `[SAFETY] iter stagnante` nei log. |
