# Issues — Dashboard & configurazione

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 20 voci totali · 0 aperte · 20 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU193 | Card adaptive scheduler — input soglie perso durante editing (auto-refresh 30s) | MEDIA | ✅ RISOLTA 07/07/2026. Utente segnala che il campo "spedizioni oggi >" non accetta un valore inferiore a 100. Verificato che backend e reader (`core/adaptive_scheduler.py::_get_soglie`) accettano/restituiscono correttamente qualunque valore ≥0 — testato in produzione scrivendo 50 su `global_config.json`+`runtime_overrides.json` e rileggendo, esito corretto, poi ripristinato. **Causa reale**: `#adapt-card` ha `hx-trigger="load, refresh, every 30s"` che rimpiazza l'intero innerHTML (input inclusi) ogni 30s — se l'utente digita un valore ma il polling scatta prima di premere "salva", l'input viene sovrascritto dal valore server-side non ancora salvato, dando l'impressione che il valore più basso non venga mai accettato. Fix: script in `predictor_istanze.html` che ascolta `htmx:beforeRequest` su `#adapt-card` e annulla il refresh se l'utente ha il focus su un `<input>` della card. Verificato via render Jinja2 standalone (partial + pagina completa). **Trovato stesso pattern** (non ancora corretto) su `#notif-card` (email notifier) e `#tg-card` (telegram) in `config_global.html`. Sync dev+prod, commit `2484dcf`. Effetto al prossimo riavvio dashboard (non serve riavvio bot). |
| 18 | Dashboard `/ui` mostra raw `global_config`, bot usa merged con overrides | MEDIA | ✅ RISOLTA 22/04 (get_merged_config — dashboard ora mostra valori reali bot) |
| 20 | Dashboard V6 rewrite (FastAPI+Jinja2+HTMX) | — | ✅ CHIUSA 20/04 — commit `9773de3` + `runtime.json` orfano eliminato |
| 22 | Dashboard `layout` istanze deprecato — rimosso dalla UI (bot usa TM) | BASSA | ✅ RISOLTA 22/04 |
| 23 | smoke_test GlobalConfig dict vs dataclass (pre-esistente) | BASSA | ✅ RISOLTA 03/05 — `smoke_test.py::check_ctx` passava `{}` (dict) a `_build_ctx(ist, gcfg, ...)` → `gcfg.livello_nodo` falliva con `'dict' object has no attribute`. Fix: `gcfg = load_global()` + carica `runtime_overrides.json` per `ist_overrides` per-istanza + assert task_abilitato `isinstance bool` invece di `is True` (post-WU94 baseline reset task default OFF). Validato: 12/12 istanze TaskContext OK |
| 33 | Allocazione dashboard in percentuali vs frazioni (UI mostrava 4000%) | MEDIA | ✅ RISOLTA 23/04 (get_merged_config normalizza) |
| 36 | Override null (livello/max_squadre/layout) causava int(None) TypeError | ALTA | ✅ RISOLTA 23/04 (commit `4afb14e` — _ovr None-safe + exclude_none save) |
| 34 | engine_status.json WinError 5 (collision os.replace con dashboard reader) | BASSA | ✅ RISOLTA 23/04 (retry backoff 0.1-0.5s × 5) |
| 38 | Dashboard leggeva config/state da dev invece di prod (config_manager non onorava DOOMSDAY_ROOT) | MEDIA | ✅ RISOLTA 23/04 (_PROD_ROOT in config_manager coerente con stats_reader) |
| 41 | Integrazione DonazioneTask nella dashboard (pill + toggle) | — | ✅ IMPLEMENTATA 23/04 (TaskFlags + valid_tasks + ORDER) |
| — | run_dashboard_prod.bat rotto (chiamava `dashboard_server.py` inesistente) | MEDIA | ✅ RISOLTA 27/04 (`eddefc6` corretto `uvicorn dashboard.app:app` + 7 modalità doc) |
| — | run_prod.bat senza commenti / modalità multiple | BASSA | ✅ RISOLTA 27/04 (`1a960c1` 9 modalità preconfigurate documentate, modalità #6 RIDOTTA RAM 4 istanze per memory pressure dev tools) |
| 75 | Modalità manutenzione bot — file flag + dashboard toggle | — | ✅ IMPLEMENTATA 28/04 (WU51 `2f1b9ea` `core/maintenance.py` + `data/maintenance.flag` + endpoint `/api/maintenance/{start,stop,status}` + auto-resume) |
| 77 | Detect popup MAINTENANCE gioco (auto-pause + OCR ETA) | ALTA | ✅ IMPLEMENTATA 28/04 (WU53 `c9f543f` skip istanza, WU54 `fcdad78`+`55d62c7` template `pin_game_maintenance_refresh/discord`, OCR countdown `(598,348,699,373)`, hook 3 punti `attendi_home`, `enable_maintenance_with_auto_resume(eta+30s)`). Verifica end-to-end pendente — popup sparito durante test |
| WU90 | DonazioneTask — periodic 8h invece di always | MEDIA | ✅ IMPLEMENTATA 02/05 mattina. Pre-fix: `schedule="always"` `interval=0` → 7 run/notte/istanza × 23s = ~58 min/notte tempo bot. Post-fix: `schedule="periodic"` `interval_hours=8.0`. **Razionale**: throughput bound dal rate gioco (cap pool 30, 1 donate/20min = 3 donate/h). Pickup atteso 24-30 donate/run (range reale 8-10h × 3 donate/h). Saving ~47 min/giorno. File: `config/task_setup.json:6`. Validazione post-restart: osservare `donate_count` log (24-30 OK, ≥30+`cap raggiunto` → scendi 7h, <22 → sali 9h) |
| WU96 | Dashboard config + global config — layout uniforme tel-card/tel-grid/sec-label come index.html | BASSA | ✅ IMPLEMENTATA 02/05 pomeriggio. Riscritti entrambi i template con stesso pattern di `index.html`: `<div class="section">` + `<div class="sec-label">` + `<div class="tel-grid">` + `<div class="tel-card">{<div class="tel-head">titolo</div> + form + <div class="tel-foot">descrizione</div>}`. Banner top esplicativo della differenza: **config** (`runtime_overrides.json`, hot-reload, attivo prossimo tick) vs **global config** (`global_config.json`, baseline reset, richiede riavvio bot). `config_global.html` modernizzato: era id-based + JS save manuale legacy → ora HTMX nested + listener serializza JSON. Hidden input per mumu paths/qta_* che non vanno persi nel save (dataclass `_from_raw` userebbe default). Verificato GET /ui/config 38KB e /ui/config/global 27KB con identico count tel-card/tel-grid/sec-label/tel-head |
| WU93 | BannerLearner — auto-apprendimento banner non catalogati (no-AI) | ALTA | ✅ IMPLEMENTATA 02/05 pomeriggio. Sistema di riconoscimento automatico X chiusura top-right per popup ignoti. **Pipeline**: streak UNKNOWN >=4 → `detect_x_candidates(img)` heuristic OpenCV (maschere colore rosso/oro/magenta + edge density + filtro shape 40-65px aspect 0.7-1.4) → tap top-3 candidate con validazione (a) visual_diff_score >= 0.10 AND (b) score_home/map >= 0.70 → register in `data/learned_banners.json` con dedup similarity 0.85. **3 nuovi moduli**: `shared/banner_learner.py` (detection heuristic), `shared/learned_banners.py` (storage JSON, LRU 25 entry, auto-disable dopo 3 fail streak), 2 hook (`dismiss_banners_loop` + `attendi_home`). **Toggle globale**: `globali.auto_learn_banner` (default True) in `runtime_overrides.json` + `GlobaliOverride` + `GlobalConfig` per disattivare il processo da dashboard. **Dashboard**: pannello "🧠 banner appresi (auto)" con toggle ON/OFF in cima + tabella + preview X + actions enable/disable/delete (HTMX refresh 30s). Endpoint API `/learned-template/{name}/{x|title}` + `/api/learned-banners/{name}/{action}` + `/api/banner-learner/{enable|disable}`. **Validazione**: detection X tag Equipment Report ground truth (825,54) → detect (824,53) = errore 1px, score 0.649. Falsi positivi su HOME pulita: 0. Catalog runtime esteso: statici (priority 0-3) + learned (priority 4) + fallback X cerchio dorato/BACK arrow (esistenti). |
| WU104 | run_prod.bat — pre-kill bot orfani via PowerShell | MEDIA | ✅ IMPLEMENTATA 03/05 notte. Pre-kill nel batch PRIMA del lancio Python (era solo lato bot via `_cleanup_orfani_processi_startup` post-import, finestra ~10-15s di sovrapposizione su engine_status/state/logs). Comando: `powershell -NoProfile -Command "Get-CimInstance Win32_Process -Filter Name='python.exe' \| Where-Object {$_.CommandLine -like '*main.py*' -and $_.CommandLine -notlike '*-m uvicorn*'} \| ForEach-Object {Stop-Process -Id $_.ProcessId -Force}"` + `timeout /t 2`. Esclude `-m uvicorn` per non killare la dashboard. Doppia rete con `_cleanup_orfani_processi_startup` al boot del bot. |
| WU110 | BannerLearner cleanup — disabilitato per default, modulo deprecato | BASSA | ✅ RISOLTA 03/05 notte. Diagnosi: `auto_learn_banner=true` ma `data/learned_banners.json` mai creato (0 eventi `[LEARNER]` in tutti i log). Root cause: in `dismiss_banners_loop` ([shared/ui_helpers.py:446](shared/ui_helpers.py)) il learner si attiva solo se `not counts` (dict dismiss vuoto), ma il fallback X cerchio dorato (`pin_btn_x_close` ROI top-right) dismisses i banner unmatched PRIMA del learner aggiungendo `_unmatched_tap_x` a counts → learner SKIPPATO. In 4h: 6 fallback X dorato, 0 [LEARNER] events. **Fix opzione B** (cleanup): (a) default `auto_learn_banner=False` in `GlobaliOverride`, `GlobalConfig`, `global_config.json`, `runtime_overrides.json`; (b) docstring DEPRECATO in `shared/banner_learner.py` + `shared/learned_banners.py` (moduli lasciati in repo per git history); (c) pannello dashboard "🧠 banner appresi" rimosso da `index.html` (commento HTML con motivazione); endpoint `/ui/partial/learned-banners` lasciato attivo per compat (ritorna lista vuota). Smoke test: merge_config → `auto_learn_banner=False`, GlobalConfig._from_raw → False. Riattivazione futura possibile con refactor `learn-after-fallback` ~50 righe. |
| WU155 | Timeout boot HOME 180→300s + esposto in dashboard sezione sistema | MEDIA | ✅ IMPLEMENTATA 11/05 notte (commit `22560d6`). Richiesta utente: aumentare timeout polling HOME post-avvio gioco da 180 a 300s, renderlo configurabile dashboard sia HOME (DYNAMIC) che CONFIG (STATIC), posizionato accanto a `max_parallel`+`tick_sleep_min` nella sezione "sistema". **Modifiche**: (1) Default `MumuConfig.timeout_carica_s` 180→300; (2) nuovo path canonico dashboard `sistema.timeout_carica_s` (priorità lettura: `sistema.*` > `mumu.*` legacy > default 300); (3) `_merge_globali` propaga `globali.sistema.timeout_carica_s` da DYNAMIC su `merged.mumu.timeout_carica_s` finale (bot legge path interno invariato); (4) `SistemaOverride` Pydantic: aggiunto campo `timeout_carica_s: int = Field(default=300, ge=30, le=900)` — endpoint PUT/PATCH accetta automatic via setattr pattern WU139; (5) UI HOME (`index.html`) + CONFIG (`config_global.html`) sezione sistema: nuova riga "timeout boot HOME (s)" id `g-timeout-boot`/`gc-timeout-boot` range 30-900 step 30; (6) Payload JS `salvaGlobals` + `gcSalvaSistema` include `sistema.timeout_carica_s`; (7) `to_dict`: esposto `sistema.timeout_carica_s` per coerenza lettura dashboard; (8) `global_config.json` prod+dev aggiornato `mumu.timeout_carica_s=300` + nuovo `sistema.timeout_carica_s=300`, backup `.bak.20260511_wu155_timeout`. **Effetto attivazione**: restart bot prod (config_loader) + restart dashboard uvicorn (Pydantic schema) + refresh browser (template HTML). Backward compat: chi ha solo `mumu.timeout_carica_s` continua a funzionare via fallback. |
