# Issues — Arena · Mercato · District Showdown · Boost

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 52 voci totali · 4 aperte · 48 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 51 | DistrictShowdown — gate readiness popup fase 3/4/5 (tap a vuoto su MuMu lento → blocco WARFARE) | BASSA | 🆕 APERTA 24/04 (downgrade ALTA→BASSA 04/05) — proposta: `_wait_template_ready` analogo a pin_dado su sentinel di ogni popup (pin_alliance_influence / pin_achievement_rewards / pin_alliance_list / pin_vs_fund_raid). Non bloccante: DS è disabilitato dal flag dashboard (WU108) finché evento non utile o stabilizzazione MuMu |
| 52 | Notte 26/04 — produzione_corrente null + stab HOME 88% timeout + ARENA→ADB cascade + FAU_07 deficit | MIX | 🟡 parziale — 52c risolto da #56 WU24; 52a/b/d aperti |
| — | Pulizia + reinstallazione istanze MuMu (29/04 pomeriggio) | — | 🛠️ IN CORSO 29/04 — utente sta reinstallando le istanze MuMu (cascata ADB persistente FAU_02/03/04 durante test arena, FAU_00 stabile). Bot in pausa. Post-pulizia: validare ADB stabile + test settings lightweight + integrazione launcher + ri-test arena |
| 89 | Template arena Failure/Continue/Victory stale — UI client ridisegnata | ALTA | 🟡 PARZIALE 30/04 10:10 (WU77) — con cascade ADB risolto da Issue #88, finalmente possibile vedere il post-battle: client gioco ha ridisegnato UI. **Failure**: era "Failure" arancione/viola in (414,94,544,146); ora "Failure" bianco grande su sfondo magenta in (380,42,535,88). **Continue**: era "Tap to Continue" pulsante in (410,443,547,487); ora testo corsivo in (380,503,535,530). **Victory**: ancora da catturare. WU77 sostituiti template + ROI per Failure (155×46 score 0.998) e Continue (155×27 score 0.996), validato runtime su 3 sfide FAU_10. Tap coord _TAP_CONTINUE_VICTORY/FAILURE entrambi (457,515) per nuovo design unificato. Victory rimane vecchio template per ora — sarà aggiornato quando capita Victory naturale |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU185 | Arena — video introduttivo post-aggiornamento client (pulsante Skip) | MEDIA | ✅ RISOLTA 02/07/2026 — dopo reinstall istanze, il client mostra un video intro al primo ingresso Arena of Doom con pulsante "Skip" in alto a destra; se persa la finestra, schermata "Open" (busta) non più skippabile (indicazione utente). Fix `_gestisci_video_intro()` in `tasks/arena.py`: poll per Skip → tap; se Open, rientra e ritenta fino a 5 volte; dopo 5 falliti, fallback passivo. Nuovi template `pin_arena_08_skip_intro.png`/`09_open_intro.png`. **Validazione live ciclo 402**: Skip catturato 8/8 (100%). 3/8 istanze (FAU_02/06/09) fallite comunque dopo lo Skip — causa identificata: **non un difetto del fix**, ma interferenza ADB del watcher esterno di diagnosi usato durante il monitoraggio (screenshot paralleli sulla stessa porta, collisione col check ufficiale del bot — verificato su FAU_09: stesso frame, score reale 0.999 vs 0.110 letto dal bot). Confermato pulito su FAU_02 al retry ciclo 403: nessun video ricomparso, lista trovata immediatamente, sfide completate. **Lezione**: mai usare script ADB esterni al processo bot su istanze live — il lock anti-concorrenza `_screencap_global_lock` non copre processi esterni. **Aggiornamento 02/07 pomeriggio**: verifica completa giornata 02/07 — **11/11 istanze (100%) arena completata con successo**, incluse FAU_02/06/09 (le fallite del 01/07). Il video NON è quindi rigorosamente one-time-per-sempre: è ricomparso su FAU_10 lo stesso giorno di un successo precedente — natura esatta della ricorrenza non nota, da osservare se ricorre. **Path di fallback validato dal vivo per la prima volta** (FAU_10, 11:41-11:44 UTC): Skip mai catturato in 5 tentativi (~90s, 5 rientri completi), fallback passivo attivato correttamente e lista raggiunta al primo check (video già terminato nel frattempo) — nessun blocco, overhead totale ~2.5min, arena completata regolarmente (5/5 sfide). Fix considerato pienamente validato end-to-end, inclusi tutti i path di errore previsti |
| WU169 | DistrictShowdown — "icona evento non trovata" intermittente (banner chiuso/ambiguo) | MEDIA | ✅ RISOLTA 20/06/2026 — richiesta utente di verificare se le occorrenze coincidessero con il banner eventi chiuso (icona DS visibile solo a pannello aperto). Confermato: 24 fallimenti su 10gg, due pattern distinti — 8 l'08/05 tutti consecutivi su tutte le istanze senza alcun successo quel giorno (evento mensile non attivo, legittimo) vs 16 il 19-20/06 **intervallati con successi sulla stessa istanza** a poche ore di distanza (es. FAU_00 fail 19:52 → ok 22:45) → bug transiente, non "evento spento". Causa, 2 varianti osservate sulla stessa FAU_03: (A) banner rilevato chiuso correttamente (score 0.894) ma `time.sleep(1.0)` dopo il tap di apertura — sotto il minimo 2.0s della REGOLA DELAY UI del progetto — insufficiente per l'animazione, icona cercata su schermata non ancora renderizzata; (B) nessun log "banner chiuso" prima del fallimento — il check originale loggava/agiva solo se `score_chiuso >= 0.85`, sotto soglia (banner in transizione) il tap di apertura veniva saltato **silenziosamente**, icona rimasta nascosta. Fix: nuovo metodo `_assicura_banner_aperto()` che loggA sempre entrambi i punteggi (aperto/chiuso) e tappa apri a meno che "aperto" non sia confermato con score alto (evita di richiudere un banner già aperto, essendo un toggle); `run()` ora fa retry — se il 1° tentativo `_apri_evento()` fallisce, ri-esegue check banner + ricerca prima di arrendersi. Verificato con fake device/matcher i 3 casi (chiuso→tap, ambiguo→tap, aperto→no-op). Nessun test dedicato preesistente per questo file (no regressione su suite generale). Sync dev+prod. |
| WU113 | ArenaMercato: pack 360+15 acquisti=0 — sleep DELAY UI + debug buffer | ALTA | ✅ RISOLTA 04/05 notte. Sintomo utente: arena_mercato 12/12 esecuzioni con `pack360=0 pack15=0` (era 5 pack/run prima). Verifica log: `[MERCATO] btn360 open=-0.030 close=0.070` (entrambi sotto soglia 0.75) → exit immediato. **Root cause**: `tap carrello` (905,68) → `time.sleep(0.3)` → screenshot in transizione → template stale-looking. **Regola DELAY UI vincolante** (vedi CLAUDE.md): tap che apre popup richiede 2.0s minimo prima dello screenshot. **Fix**: sleep 0.3 → **2.0s** dopo `_TAP_CARRELLO`. Inoltre aggiunto `_MercatoDebugBuf` (pattern WU85) con snap a 4 punti chiave (`00_pre_carrello`, `01_post_carrello`, `02_iter0_pre_check_360`, `03_pre_check_15`), flush condizionale solo on `acquisti=0 OR errore`. Toggle `_DEBUG_MERCATO_DUMP=True`. Smoke test: 10/10 pytest verdi. Effetto al prossimo restart bot. |
| WU114 | Arena rebuild truppe: monitor dettaglio + debug screenshot | ALTA | ✅ IMPLEMENTATA 04/05 notte. Sintomo utente: arena non schiera tutte le squadre dopo rebuild WU83. Codice tappa correttamente tutte celle 1/N..N/N (verificato log) ma il risultato visivo a volte è incompleto. **Implementazione debug**: `_DEBUG_REBUILD_DUMP=True` modulo-level + funzione locale `_snap(label)` chiamata a 5+(N×2) punti chiave: `00_pre_rebuild`, `01_post_rimozione`, per ogni cella i: `02_cella{i}_open_selettore` + `03_cella{i}_post_ready`, `04_post_rebuild_complete`. Flush sempre (per N rebuild iniziali analisi) in `data/arena_debug/{ist}_{ts}_{idx}_{label}.png`. Disattivare toggle dopo analisi. Permette validare visivamente: (a) selettore aperto correttamente al tap cella, (b) READY tappato su selettore con truppe disponibili, (c) cella schierata post-READY. Nessuna logica modificata, solo osservabilità. 9 test pre-esistenti falliti (debt pre-WU114, no regressione). **Successivamente migrato a WU115 architettura unificata**. |
| WU115 | Sistema debug screenshot unificato + hot-reload + dashboard toggle | — | ✅ IMPLEMENTATA 04/05 notte (Steps A-E). Sostituisce 3 toggle modulo-level (`_DEBUG_REBUILD_DUMP`, `_DEBUG_MERCATO_DUMP`, `_DEBUG_STORE_FAIL_DUMP`) con architettura indipendente hot-reload. **Componenti**: (A) `shared/debug_buffer.py` (~220 righe) — `DebugBuffer.for_task(task, instance)` + `snap(label, screen)` + `flush(success, force, log_fn)` + cache 30s + `cleanup_old(7gg)`; (B) Schema config: `globali.debug_tasks: dict[str,bool]` in `GlobaliOverride` (Pydantic) + `GlobalConfig` (dataclass); (C) Migrazione 3 task esistenti (arena/arena_mercato/store) al nuovo pattern; (D) Endpoint API `dashboard/routers/api_debug.py`: `GET /api/debug-tasks` (status JSON) + `PATCH /api/debug-tasks/{task}/{enable\|disable}` (toggle JSON); (E) UI dashboard: pannello "🐛 debug screenshot per task" in `index.html` con pill HTMX + endpoint `/ui/partial/debug-tasks` + `/ui/debug-tasks/{task}/{action}` (HTML wrapper). **Logica flush**: `success=True+force=False → no flush`, `success=False → flush`, `force=True → flush sempre` (per anomalie task-specific). **Storage**: `data/{task}_debug/{istanza}_{ts}_{idx:02d}_{label}.png`. **Cleanup automatico** 7gg (chiamare `cleanup_old()` da launcher al boot). **Hot-reload**: cache TTL 30s + invalidate dopo PATCH. **Nuovi task in futuro**: import `DebugBuffer.for_task(self.name(), instance)` + `snap`/`flush` + aggiungere nome a `_KNOWN_TASKS` in api_debug.py. Smoke test: 5/5 endpoint, 5/5 UI partial, 10/10 pytest arena_mercato verdi (no regressione). Test debt pre-esistenti store (5 fail) e arena (9 fail) invariati. |
| 5 | Alleanza — COORD hardcoded | BASSA | ✅ RISOLTA 03/05 (correzione utente — wontfix iniziale era errato). Pre-fix: `tasks/alleanza.py::_esegui_alleanza` usava `device.tap(*coord_alleanza)` coord fissa (760, 505) per il bottone Alleanza. Pattern obsoleto: gli altri task (`donazione.py`, `rifornimento.py`, `arena.py`, `arena_mercato.py`) usano `nav.tap_barra(ctx, "alliance")` con template matching dinamico (`pin_alliance.png` ROI barra inferiore). Su istanze con barra inferiore con meno bottoni il bottone Alliance shifta → tap fisso falliva. **Fix**: `_esegui_alleanza(ctx, device, ...)` ora prima tenta `nav.tap_barra(ctx, "alliance")`, fallback a coord fissa se navigator non disponibile o tap_barra fallisce. Pattern coerente con donazione/rifornimento. Note: 15 test pre-esistenti in test_alleanza.py falliscono per `cfg.coord_rivendica` non più nel modello (debt pre-esistente, no regressione fix WU111) |
| 13 | Boost `gathered` non riconosciuto | MEDIA | ✅ RISOLTA 20/04 (`wait_after_tap_speed: 2.0s`) |
| 14 | Arena START CHALLENGE non visibile (FAU_02/03/04/07/08 ciclo 20→21) | ALTA | ✅ chiuso implicitamente — #83 verificato refuso 03/05, arena cicli odierni 25/25 sfide OK su 5 istanze. Sintomo originale 21/04 era effetto di #88 cascade Vulkan (✅ risolta da DirectX 30/04). Graceful fail (abort+BACK se template non trovato) resta come safety net |
| 14-bis | Arena hang indefinito (FAU_10 ciclo 19/04 — kill manuale) | ALTA | ✅ RISOLTA (F2 timeout 300s `3c959cf`) |
| 17 | Storico `engine_status.storico` filtrato — solo `raccolta`+`arena` | MEDIA | ✅ RISOLTA implicitamente — verificato 03/05: storico ora contiene 8 task distinti (raccolta, main_mission, raccolta_chiusura, rifornimento, truppe, alleanza, messaggi, store). Probabilmente fixed da WU38-44 telemetria |
| 30 | raccolta_only non filtrava task: FauMorfeus tentava boost/vip/arena/... | ALTA | ✅ RISOLTA 23/04 (_thread_istanza filtra su tipologia) |
| 43 | Integrazione DistrictShowdownTask nella dashboard + pipeline | — | ✅ IMPLEMENTATA 24/04 (TaskFlags + valid_tasks + ORDER + template + sync_prod) |
| 44 | DistrictShowdownTask — conformità V6 API (6 bug bloccanti) | ALTA | ✅ RISOLTA 24/04 (name metodo, task_abilitato, TaskResult.message, screen no .frame, zone, e_dovuto(ctx)) |
| 45 | DistrictShowdown — MatchResult sempre truthy, uso `.found` consistente | ALTA | ✅ RISOLTA 24/04 (10 check convertiti al pattern `.found`) |
| 47 | DistrictShowdown — tap hardcoded su Start falliva quando popup shift | ALTA | ✅ RISOLTA 24/04 (tap dinamico su coord match + wait_template_ready adattivo) |
| 48 | DistrictShowdown — loop infinito quando gioco esce + skip animation | ALTA | ✅ RISOLTA 24/04 (early-exit 3 cicli streak + check skip 840,371) |
| 50 | DistrictShowdown — finestre temporali evento (Ven 00:00 → Lun 00:00 UTC, Fund Raid Dom 20:00 → Lun 00:00) | — | ✅ IMPLEMENTATA 24/04 (`_is_in_event_window` + `_is_in_fund_raid_window` in DistrictShowdownTask) |
| 57 | State save per task — fine-grained persistence (BoostState scadenza persa post-cascata FAU_04) | ALTA | ✅ RISOLTA 26/04 (WU25 — `orc.tick()` save dopo ogni task, `_state_dir()` env-based) |
| 59 | Boost debug `_salva_debug_shot` 6 giorni attivo, 105MB accumulati | BASSA | ✅ RISOLTA 26/04 (chiamate commentate, screenshot eliminati prod+dev) |
| 62 | Riordino priorità task + chiusura raccolta (boost→riforn→raccolta→...→raccolta_chiusura) | MEDIA | ✅ RISOLTA 26/04 (RaccoltaChiusuraTask sottoclasse, priority 200; task_setup.json riordinato; filtro raccolta_only esteso) |
| 73 | Pannello tempi medi task con filtro outlier IQR | — | ✅ IMPLEMENTATA 28/04 (WU49 `7984478` — IQR Tukey k=1.5, esclude district_showdown, ordinamento desc per avg) |
| 83 | Arena `_TAP_ULTIMA_SFIDA` cieco — freeze su righe "Watch" | ALTA | ✅ REFUSO — verificato 03/05 sera: arena oggi su 5 istanze (FAU_02/03/04/05/06) tutte 5/5 sfide completate con success=True, zero cascade, zero freeze. La coord fissa (745,482) funziona post-WU79 (last_run preservato) + WU80 (Continue dinamico) + WU88 (DirectX). L'osservazione originale 28/04 era probabilmente sintomo della cascade Vulkan #88 interpretato come "tap Watch". Issue chiusa senza necessità di match dinamico |
| — | Test FAU_03 reinstallato — settings + arena 5/5 OK 159.6s 31.9s/sfida | — | ✅ 29/04 13:19 (WU61 `c:\tmp\test_fau03_settings_arena.py`, pattern identico FAU_02). Issue #85 Glory NON validato (popup assente in entrambi gli hook PRE+post-Arena) |
| 87 | Audit debug PNG su disco + cleanup | MEDIA | ✅ RISOLTA 29/04 (WU63). 6 punti scrittura PNG identificati: 1 disabilitato (`raccolta_ocr_debug` toggle off in prod runtime_overrides), 1 mantenuto (`boot_unknown` discovery), 1 già off (boost commentato), 3 attivi solo su anomalia. Cleanup file accumulati: data/ocr_dataset 2040 file 445MB + debug_task/screenshots 1 file + debug_task/vai_in_home_unknown 3 file → **~448 MB liberati**. boot_unknown/ mantenuto (11 file 9MB). Hot-reload toggle al prossimo tick — niente restart richiesto |
| 86 | Nuovo task TRUPPE — addestramento automatico 4 caserme (Fanteria/Cavalleria/Arcieri/Macchine) | — | ✅ IMPLEMENTATA 29/04 (WU62 `tasks/truppe.py`). Tutte coord FISSE: pannello (30,247) → cerchio Train (564,382) → TRAIN giallo (794,471). Checkbox Fast Training SEMPRE OFF (R-mean box>110=ON, soglia 110). OCR counter X/4 cascade otsu→binary su zona (12,264,30,282) per coprire X=0 (otsu lo perde). 4 PIN estratti (pannello/train_btn/check_on/check_off) ma NON usati dal MVP. Flow: leggi X, se X==4 skip, altrimenti loop (4-X) cicli con delay 5s/step. Test reale FAU_05 4 cicli 0/4→4/4 OK, 4 tipi caserme tutte gestite con stesse coord. **Priority 18 periodic 4h** (subito dopo RaccoltaTask=15, prima di DonazioneTask=20: i primi 3 sono sempre Boost→Rifornimento→Raccolta), integrato in task_setup.json + main.py + TaskFlags + valid_tasks + pill UI dashboard (Row 3 con arena) |
| 82 | Test arena standalone su FAU_02 reinstallato — 5/5 sfide OK 156s | — | ✅ 29/04 12:40 (`c:\tmp\test_fau02_arena_only.py` — coord fisse senza pin check, race cond #83 non riprodotta su istanza pulita). Pattern fragile, da non promuovere a prod senza match dinamico |
| 85 | Template `pin_arena_07_glory.png` ROI troppo piccola — match impossibile | ALTA | ✅ RISOLTA 29/04 sera (template attuale 225×35, ROI era 190×48 → cv2.matchTemplate impossibile (template>image). Fix `(380,410,570,458)→(345,405,615,465)` 270×60 in `tasks/arena.py::_ARENA_PIN["glory"]`. Match ora feasibile, popup tier-up Continue intercettabile) |
| WU74 | Arena skip checkbox — verifica solo 1×/sessione → check ad ogni sfida | ALTA | ✅ RISOLTA 30/04 mattina (in `tasks/arena.py:387` rimosso flag `run.skip_verificato`. **Root cause**: pulizia cache giornaliera WU64 reset checkbox skip al default + template `pin_arena_check.png` falso positivo su skip OFF reale (ROI identica a `pin_arena_no_check`). Su FAU_05 30/04: cache pulita 02:52 → arena 03:57 con `Skip già attivo` (false positive) → 5/5 battaglie timeout 60s. Post-fix: `_assicura_skip()` ad ogni sfida, costo +7.5s/ciclo, beneficio elimination timeout sistematico. Effetto collaterale: WU64 cache cleanup era il trigger ma il problema vero era logica skip in arena.py) |
| WU75 | Arena `_attendi_fine_battaglia` — polling 17 screenshot → sleep+1check | ALTA | ✅ RISOLTA 30/04 mattina (in `tasks/arena.py:528` refactor da `while polling ogni 3.5s` a `time.sleep(60s) + 1 screenshot final`. **Root cause**: 17 screencap consecutivi durante battaglia + 8 nella transizione post-Continue saturavano socket ADB di MuMu già stressato dalle animazioni 3D battle → cascade ADB su 8/11 istanze 30/04 (FAU_00, 01, 03, 04, 06, 07, 08, 10) con 6-17 screenshot falliti dopo battaglia. Pattern: la 1ª cascade scattava su `lista`/`purchase`/`challenge` template — durante transizione tra sfide, NON durante battaglia stessa. Saving: ~94% screencap durante battaglia (17→1). Trade-off: timeout 60s comunque rispettato, ma se skip è OFF reale battaglia >60s e check final fallisce — uguale a comportamento pre-fix per timeout) |
| WU76 | Screenshot pipeline in-memory (exec-out) — porting V5 v5.24 → V6 | ALTA | ✅ RISOLTA 30/04 mattina (refactor `_screenshot_raw` da screencap+pull (3 op disco) a exec-out (0 op). Saving I/O significativo per TUTTI i task. **Però NON risolve cascade arena**: test live FAU_10 30/04 09:36 → tap START CHALLENGE causa ADB offline ISTANTANEO, prima di qualsiasi screencap. WU76 utile come fix architetturale, cascade arena ha causa più profonda) |
| 88 | Cascade ADB durante arena — driver Vulkan MuMu | ALTA | ✅ RISOLTA 30/04 10:00 — test diagnostico approfondito su FAU_10: WU75 (no polling) + WU76 (no I/O) NON eliminavano cascade, ADB offline immediato al tap START CHALLENGE. Settings video LOW vs HIGH stesso esito. **Root cause vera**: driver **Vulkan** di MuMu Player crasha il bridge ADB su animazione 3D battle. **Soluzione**: switch driver da Vulkan a **DirectX** (manuale utente in MuMu Settings → Display). Test post-switch con monitor ADB ogni 0.5s: 271/271 polling ONLINE, 0 OFFLINE su 3 sfide consecutive (10:00-10:04). DA APPLICARE A TUTTE LE 11 ISTANZE MuMu (config manuale o batch script) |
| WU77 | Arena nuovi template Failure + Continue + ROI | ALTA | ✅ IMPLEMENTATA 30/04 10:10 (vedi Issue #89) |
| WU79 | Issue #84 orchestrator last_run aggiornato anche su fail | ALTA | ✅ RISOLTA 30/04 10:14 — in `core/orchestrator.py:316`: `entry.last_run = time.time()` veniva aggiornato SEMPRE (anche su result.success=False o eccezioni), bloccando retry fino al reset daily 24h dopo. Esempio FAU_00 30/04: arena fallita per cascade → last_run aggiornato → arena non ritenta fino al 01/05 02:35 UTC, perde 5 sfide/die. Fix: `if result.success or result.skipped: entry.last_run = time.time()`. Su fail → last_run invariato → retry al prossimo tick |
| WU80 | Arena tap dinamico Continue (loc match invece coord fisse) | ALTA | ✅ RISOLTA 30/04 10:25 — in `tasks/arena.py` post-battaglia: match dinamico template `pin_arena_05_continue.png` → `tap(cont_result.cx, cont_result.cy)`. Pre-fix: coord fisse `_TAP_CONTINUE_VICTORY`/`_FAILURE` (457,515). Live test FAU_00: Victory continue centro (457,469); FAU_01 Failure continue centro (457,516). Delta 47 pixel — coord fisse non sufficienti. Fallback su coord fissa solo se match fallisce |
| WU81 | Arena soglia victory/failure 0.80→0.90 (anti-falso positivo) | ALTA | ✅ RISOLTA 30/04 10:45 — su Failure il template `victory` matchava 0.847 (font/dimensioni simili) → falso positivo, bot avrebbe interpretato Failure come Victory. Live test FAU_00 sfida 2 Failure: victory=0.847, failure=0.995. Soglia 0.90 → Victory non scatta su Failure. Validato FAU_01: victory=0.591, failure=0.999 |
| WU82 | Arena wait battaglia 60s → 15s (DirectX skip ON veloce) | MEDIA | ✅ RISOLTA 30/04 11:30 — `_DELAY_BATTAGLIA_S=8→5` + `_MAX_BATTAGLIA_S=52→10`. Battaglie con skip ON + driver DirectX durano <10s. Saving 45s/sfida × 5 = 225s/ciclo arena (~3.75 min) |
| WU83 | Arena rebuild truppe pre-1ª sfida (1×/die UTC per istanza) | MEDIA | ✅ RISOLTA 30/04 12:25 — prima di tap START CHALLENGE alla 1ª sfida del giorno, rimuove tutte le N truppe schierate (coord 80,80/148/216/283/351) e ricarica via tap cella + READY (auto-deploy migliore composizione). Test live FAU_06 4 celle: power 431k → 685k (+59% truppe nuove). State `data/arena_deploy_state.json` granularità giornaliera UTC. N celle da `max_squadre` config (FAU_00/FauMorfeus=5, altre=4). 5ª cella lucchettata su FAU_06 ignorata silenziosamente. Costo +25-30s solo 1×/die |
| 88 | Cascade ADB durante arena — driver Vulkan MuMu | ALTA | ✅ RISOLTA 30/04 10:00 — bug noto MuMu driver Vulkan crash su animazione 3D battle. Test live FAU_10 con Vulkan: ADB offline immediato al tap START. Switch a **DirectX** → 5 sfide consecutive (FAU_00+FAU_01+FAU_10) totale 593/593 polling ADB ONLINE 0 OFFLINE. Manuale utente per ogni istanza MuMu in Settings → Display → Render mode |
| 89 | Template arena Failure/Victory/Continue stale — UI ridisegnata | ALTA | ✅ RISOLTA 30/04 10:42 — con cascade Issue #88 risolto, finalmente possibile vedere il post-battle. Client gioco ha ridisegnato UI: nuovi template estratti live FAU_00/FAU_10. **Failure**: bianco grande su sfondo magenta (380,42,535,88) score 0.998. **Victory**: bianco grande su sfondo dorato (380,42,535,88) score 1.000 (estratto FAU_00 30/04 10:42 rank 81→53). **Continue**: testo "Tap to Continue" corsivo bianco (380,503,535,530) score 0.996. ROI estese a (370,35,545,95) per tutti |
| WU108 | DistrictShowdown ignora flag dashboard — fix veto esplicito | MEDIA | ✅ RISOLTA 03/05 notte. Sintomo utente: DS disabilitato dalla dashboard ma il task girava comunque (201 fail/201 exec con "icona evento non trovata" oggi). Root cause ([district_showdown.py:158-169](tasks/district_showdown.py)): `should_run` ignorava `task_abilitato("district_showdown")` per design (auto-WU17 27/04 commento "evita rischio flag dimenticato disabilitato durante evento"). Pattern problematico quando: evento esiste in window ma icona non trovata (es. evento saltato settimana, account low-level, banner promo davanti) → 5.6s × 201 = ~19 min/die wasted su task fallimentare. **Fix**: aggiunto check `task_abilitato("district_showdown")` come VETO esplicito prima della window check. Il flag dashboard è ora la fonte di verità (utente sa quando disabilitare). Smoke test: flag=False → should_run=False; flag=True → dipende da window. Effetto hot-reload al prossimo tick di ciascuna istanza. |
| WU117 | Arena tap prima sfida invece di ultima — fix lista incompleta | MEDIA | ✅ IMPLEMENTATA 04/05 mezzogiorno. Pre-fix: `_TAP_ULTIMA_SFIDA=(745,482)` ([tasks/arena.py:67](tasks/arena.py)) tappava ultima riga della lista sfide (5/5). Falliva quando la lista aveva meno di 5 righe (es. account low-level con pochi opponenti idonei) → tap su area vuota o su elemento adiacente non desiderato. **Fix**: rinominata costante `_TAP_PRIMA_SFIDA=(745,250)` calibrata visivamente su FAU_01 04/05 (validata con marker_5_rows.png su `c:/tmp/maschera_inv/`). Aggiornato l'unico call site `tasks/arena.py:403`. Prima riga sempre presente se la lista contiene ≥1 sfida — più robusto. Test rinviato a produzione 05/05 (sfide odierne FAU_01 esaurite dal test arena_mercato + manuale). |
| WU122 | Predictor refinement (sessione 04/05 sera + 05/05 mattina, 8 commit) | MEDIA | ✅ IMPLEMENTATA. Refinement profondo skip + cycle predictor in 8 commit: (1) `7c8911f` skip predictor temporal residuo (T_marcia - elapsed) + cycle predictor NOOP guards (`_boost_will_skip` su scadenza, `_rifornimento_will_skip` su master saturo / provviste / quota); (2) `a264077` `_task_will_be_noop` esteso a `district_showdown` (window UTC + flag); (3) `7a92ec0` guardrail `cooldown_post_retry` init `cicli_dall_ultimo_retry=COOLDOWN` (era 0, bloccava primo skip) + management in main loop; (4) `e632ffe` `saving_estimato_s` nei signals SkipDecision + colonna dashboard predictor-decisions; (5) `672eaa2` cycle predictor offset cumulativo per gate temporali (`now_for_inst = now_utc + Σ T_j`); (6) `2fd44fb` percentile median/p75/p90 in `predict_istanza_duration` + flag CLI `--percentile/--compare` + dashboard dual-stima; (7) `cf8a222` wait inter-task tracking data-driven + fallback bootstrap costante (orchestrator timer); (8) `9f9a75d` tuning post-validazione 05/05: gap_atteso skip predictor median→p75 (riduce falsi positivi, validazione `cycle_accuracy.jsonl`) + fallback wait 8s→1.5s (wait reale 1.1s/transizione, 7× sovrastima). |
| WU125 | arena wait battaglia 15→10s + rebuild celle popolate (3/4 fix) | ALTA | ✅ IMPLEMENTATA 05/05 mattina (3 commit). `240d890` `_MAX_BATTAGLIA_S` 10→5s (totale 15→10s, saving 5s × 5 sfide × 11 istanze ≈ 4.5min/ciclo); `45d2dc9` rebuild WU83 — verifica pre-rimozione celle popolate (Opzione A, edge density Canny ≥18% discrimina piena/vuota). Bug pre-fix: tap rimozione cieco su cella vuota → side-effect UI + shift posizionale post-rebuild. Sintomo utente: "FAU_05 non schiera 4 squadre", validato 6 casi (FAU_03/04 4/4 piene OK; FAU_05/06 3/4 con c4 vuota da ieri → bot tappa 4 rimozioni cieche → step 2 popola 3/4). Fix: helper `_cella_popolata(frame, idx)` controlla edge density Canny prima del tap rimozione. Smoke test 4/4 PASS sui pre_rebuild reali. Recupero +25% potenza arena su istanze con cella ereditata vuota. |
| WU131 | Arena — `segna_esaurite()` anche su 5/5 sfide completate (no popup purchase) | ALTA | ✅ RISOLTA 06/05 (commit `dfd64c3`). Pre-fix: `ArenaState.esaurite=True` settato SOLO al detect di `pin_arena_06_purchase` (popup acquisto sfide aggiuntive). Quel popup compare al tentativo della 6ª sfida, NON dopo OK delle prime 5. Conseguenza: dopo 5/5 ok, `should_run()=True` → arena ripianificata al tick successivo → fallisce con "0 sfide (nessuna sfida eseguita dopo tutti i tentativi)". Telemetria 5-6/05: 22 OK + 12 fail (35% wasted) tutti `esau=False`. Fix in `tasks/arena.py:317-329`: dopo loop, se `run.sfide_eseguite >= MAX_SFIDE` → `ctx.state.arena.segna_esaurite()` esplicita. Reset automatico mezzanotte UTC esistente. |
| WU145 | Gate orario UTC<10 per ArenaTask — sposta picco notturno | BASSA | ✅ IMPLEMENTATA 10/05. Diagnosi pattern cicli notturni: top 5 cicli più lunghi (217/188/181/160/153 min) tutti tra 23:35 e 02:45 UTC. Causa: a 00:00 UTC il gioco resetta simultaneamente DRL master (rifornimento riparte ~50min cumulati) + arena (5 sfide × 11 ist = ~30min cumulati) → primo ciclo post-reset gonfio +75-85min vs regime diurno. **Fix**: gate orario in `tasks/arena.py::ArenaTask.should_run` analogo a MainMissionTask (WU91): `if datetime.now(timezone.utc).hour < 10: return False`. Effetto: arena saltata nei cicli 00:00→10:00 UTC (-25-33 min sul ciclo critico c45-style), scatta al primo ciclo dopo le 10:00 UTC (+25-33 min su un ciclo diurno). Trade-off: bot fermo 14h consecutive nella finestra 10→24 UTC perde la giornata di arena (rischio basso, finestra ampia). NON tocca `last_run` né `segna_esaurite` (WU79: last_run aggiornato solo su success). Effetto al prossimo restart bot. |
| WU148 | Adaptive Scheduler — `_flags_status` legge solo STATIC + reset cross-mezzanotte UTC | ALTA | ✅ RISOLTA 11/05 mattina. **Sintomo utente**: post-restart bot prod alle 10:50, log mostra `[ADAPT-SCHED] resume da planned order: 11 istanze rimanenti` malgrado `runtime_overrides.json::globali.adaptive_scheduler_enabled=false, _shadow_only=true`. **Diagnosi**: `core/adaptive_scheduler.py::_flags_status()` chiamava `load_global()` che legge SOLO `global_config.json` (STATIC = `enabled=true, shadow=false` per default WU138) → ignorava `runtime_overrides.json` (DYNAMIC). Bot girava LIVE invece che OFF. Violava regola `architecture_config_static_dynamic.md` (DYNAMIC prevale a runtime). **Fix in 2 parti**: (1) refactor `_flags_status()` con pattern uguale a `_rifornimento_abilitato` dello stesso modulo: legge prima DYNAMIC (`runtime_overrides::globali.{enabled,shadow_only}`), fallback su STATIC per i campi non override-ati, default safe `(False, True)`; (2) `load_planned_order()` invalida planned_order se `ts_saved.date() != now.date()` UTC — reset cross-mezzanotte UTC quando il gioco azzera DRL master + spedizioni + arena (era già coperto dal TTL 4h nei casi normali ma falliva con restart 00:00-04:00 UTC). **Smoke test post-fix** prod: `_flags_status()` ritorna `(False, True)` coerente con dynamic. **Operazione collaterale**: `scheduler_planned_order.json` esistente spostato in `.bak.20260511_WU148`. **Effetto attivazione**: al prossimo restart bot prod (Python carica modulo aggiornato). Bot corrente in memoria continua con planned_order del ciclo già caricato fino a fine ciclo 2. |
| WU156 | Predictor: arena gate UTC<10 in `_is_task_due` (sync WU145) | MEDIA | ✅ RISOLTA 12/05 mattina (commit `c740683`). **Sintomo utente**: il predictor mostra ancora la vecchia schedulazione arena dopo [[WU-145-Arena-Gate-UTC10\|WU145]] (gate orario UTC<10). Il predictor stima T_ciclo includendo arena nelle finestre 00:00-10:00 UTC dove invece `arena.should_run()` ritorna False → sovrastima T_ciclo notturno di ~3-5 min. **Root cause**: WU145 (10/05) ha aggiunto gate UTC<10 a `tasks/arena.py::ArenaTask.should_run()`, ma `_is_task_due()` in `core/cycle_duration_predictor.py` non è stato aggiornato di conseguenza. Pattern noto: ogni cambio scheduling/gate in `tasks/` richiede sync in cycle_duration_predictor (memoria `feedback_cycle_predictor_sync.md`). **Fix**: aggiunto edge case identico a `main_mission` (WU91) ma con condizione inversa `hour<10` (arena gira 10:00-24:00 UTC, main_mission gira 20:00-24:00). `if task_name == "arena" and now_utc.hour < 10: return False`. Posizionato nel branch `schedule=daily` insieme a main_mission edge. Effetto: predictor T_ciclo notturno (00:00-10:00 UTC) ora esclude arena correttamente → stima coerente con comportamento bot live. Restart dashboard non richiesto (la funzione è importata at-call dal predictor). **Superseded by WU157**: edge case hardcoded sostituiti da gate centralizzato. |
| WU157 | Sistema centralizzato regole scheduling live ↔ predictor | MEDIA | ✅ IMPLEMENTATA 12/05 pomeriggio. **Richiesta utente**: "implementa un sistema centralizzato di regole condiviso tra il live ed il predictor in modo tale che ad un cambio di strategia del live non è necessario modificare l'interfaccia del predictor". **Pattern pre-WU157**: ogni gate orario richiedeva 2 modifiche (live in `tasks/<task>.py::should_run` + predictor in `_is_task_due`). Drift osservato: arena gate UTC<10 (WU145, 10/05) è restato 2 giorni solo nel live, il predictor sovrastimava T_ciclo notturno di 3-5min finché WU156 (12/05) non ha sincronizzato. **Implementazione**: nuovo modulo `shared/task_scheduling.py` con: (a) funzioni gate `time_gate_<task>(now=None) -> bool` (1 per task con vincolo orario); (b) `TIME_GATES: dict[str, Callable]` registry; (c) helper `can_run_by_time_gate(task_name, now=None) -> bool` con failsafe `try/except → True`. Migrati WU91 (`main_mission` UTC>=20) e WU145 (`arena` UTC>=10) come prime due funzioni. **Aggiornamenti**: `tasks/arena.py` import `time_gate_arena` + uso in `should_run`; `tasks/main_mission.py` import `time_gate_main_mission` + uso in `should_run`; `core/cycle_duration_predictor.py` import `can_run_by_time_gate` + chiamata early-return all'inizio di `_is_task_due` (introspection automatica registry), rimossi 2 edge case hardcoded WU91+WU145+WU156. **Smoke test**: 11/11 gate function + 6/6 integrazione `_is_task_due` verdi. **Pattern futuro**: aggiungere nuovo gate orario → definire `time_gate_<task>` + registrare in `TIME_GATES` → predictor automaticamente coerente, zero modifiche all'interfaccia. **Effetto**: al prossimo restart bot prod (Python no hot-reload moduli `shared/tasks/core`). |
| WU-DS-Banner | District Showdown — apri banner eventi prima di cercare icona, chiudi su tutti i path | MEDIA | ✅ IMPLEMENTATA 23/05 (commit `adf9008`). Regola: banner laterale sempre CHIUSO per tutti i task (nasconde icone righe 2/3 quando aperto). DS è unica eccezione: apre banner prima di `_apri_evento()` (score `pin_banner_chiuso ≥ 0.85` → tap `(345,63)` + 1s), poi chiude via `_chiudi_banner()` su tutti e 3 i path di uscita (`_apri_evento` fallisce, `_attiva_auto_roll` fallisce, completamento normale). Da validare alla prossima finestra evento DS (Ven-Lun UTC). |
