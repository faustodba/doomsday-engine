# Issues — Rifornimento & Zaino

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 42 voci totali · 2 aperte · 40 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| — | Delay 2.0s post-tastiera in `_compila_e_invia` — misura prima di tagliare (proposta Gemini) | BASSA | 🔍 IN OSSERVAZIONE 17/07/2026. Gemini ha proposto di ridurre `sleep(2.0)` (`tasks/rifornimento.py:697`, tag WU194/REGOLA DELAY UI vincolante) a 0.5-0.8s perché WU213 ha eliminato la lettura OCR del valore digitato (confermato vero). Verificato però che lo stesso sleep gate ANCHE `_vai_abilitato()` (check pixel gialli sul pulsante VAI, non template matching — meccanismo diverso da WU113/ArenaMercato dove la regola 2.0s è stata calibrata), mai misurato separatamente. Compromesso 1.2s di Gemini ancora un numero "a sentimento". Strumentazione aggiunta (non un fix): checkpoint di sola lettura/log a 0.5/0.8/1.2/1.5/2.0s in `_compila_e_invia`, loggano `[DELAY-MEASURE-rifornimento] t=Xs gialli=N soglia=S vai_ok=bool` — il check reale che conta resta fisso a 2.0s (comportamento identico con o senza debug). Nuova funzione `_conta_pixel_gialli()` (variante read-only di `_vai_abilitato`). Gated da `globali.debug_tasks.rifornimento` (già attivo in prod). Test: 43 pass invariati (9 fail pre-esistenti confermati via `git stash`, non collegati). Sync dev→prod fatto. **Richiede riavvio BOT** per attivare. Prossimo step: raccogliere N campioni `DELAY-MEASURE` e decidere con dati reali se/quanto ridurre il delay. |
| 65 | Wait > 60s rifornimento → anticipare task post-raccolta nel tempo morto | BASSA | 🆕 APERTA 26/04 step 2 — quando wait>60s, eseguire prima i task post-raccolta poi tornare a raccolta dopo verifica tempo trascorso |
| WU163 | Rifornimento — match pin rifugio falso positivo via soglia retry permissiva | MEDIA | 🔍 23/06 IN OSSERVAZIONE. Utente segnala FAU_10 non invia rifornimento "non riesce a tappare sul rifugio". Diagnosi log (`logs/FAU_10.jsonl` 07:02 UTC): ROI primaria fallisce (score=0.406-0.543 < soglia 0.70), ROI retry "trova" con score borderline 0.59-0.60 (soglia permissiva 0.55) — ma è un falso positivo: tap su (435,174) non sblocca RESOURCE SUPPLY (score=0.397-0.406 vs soglia 0.75) → 0 spedizioni. Stesso pattern identico su FAU_05 stesso giorno (tap castello (435,174) IDENTICO, sospetto elemento fisso). Le altre 6/9 istanze attive oggi hanno avuto match diretto forte (score 0.886) e 5/5 spedizioni — non è un problema generale, caso limite già previsto nel commento WU161 (`tasks/rifornimento.py:407-410`). **Ipotesi utente da verificare**: icone evento sulla mappa che coprono/confondono il pin rifugio nella ROI di ricerca. **Azioni**: (1) attivato `globali.debug_tasks.rifornimento=true` (dynamic, hot-reload); (2) nuovo dump screenshot mirato in `_centra_mappa` — tag `suspect` quando il match passa SOLO dalla soglia di retry (score<0.70), tag `fail` quando fallisce su entrambe le ROI (refactor del dump esistente in helper `_dump_debug_screenshot`). Nessun cambio di comportamento/logica di tap, solo osservabilità aggiuntiva in `data/rifornimento_debug/`. Test 43/43 verdi (9 fail pre-esistenti debito tecnico invariato). **Prossimo step**: alla prossima occorrenza, analizzare screenshot `*_suspect_score*.png` per confermare/escludere l'ipotesi icone evento; se confermata, valutare fix (es. dismiss_banners_loop estesa, o esclusione zona icone dalla ROI). |
| — | Notte 28→29/04 maintenance mode 6h49m (motivo "aggiornamento") | — | ⚠️ Utente attiva manualmente maintenance da dashboard alle 22:56 28/04 (per aggiornare software MuMu) e dimentica di disattivarla. Bot in pausa fino kill+restart 05:46 29/04. 0 spedizioni rifornimento notturne |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU194 | Rifornimento — delay insufficiente su lettura OCR valore clampato (dati storico_farm falsati) | MEDIA | ✅ RISOLTA 07/07/2026. Utente segnala valori assurdi in `data/storico_farm.json` per FAU_00/petrolio (999.000.000 due volte, "6" una volta) durante l'analisi del ritardo di saturazione DRL master (vedi `docs/issues/telemetria-predictor.md`). Chiarito dall'utente: il design che digita un placeholder enorme (es. `99999999999`) e lascia che il gioco lo clampi al max realmente inviabile è intenzionale (auto-WU12) — il problema è il **timing** della lettura OCR del valore clampato, non il design. Trovato in `tasks/rifornimento.py::_compila_e_invia`: tra il tap OK tastiera (che fa scattare il clamp e ridisegna il campo) e lo screenshot usato per l'OCR del valore clampato c'era solo `time.sleep(0.5)` — sotto la soglia minima 2.0s della REGOLA DELAY UI vincolante (`.claude/CLAUDE.md`), derivata da un bug rifornimento analogo (sleep 0.3→score 0.387, sleep 2.0→score 0.934). Con 0.5s l'OCR poteva catturare il campo a metà ridisegno — coerente con le letture spurie osservate. Fix: sleep aumentato 0.5s→2.0s in quel punto specifico. Verificato via `git stash` che i 17 test falliti in `test_rifornimento_base.py`/`test_rifornimento.py` sono baseline pre-esistente, non causati da questo fix. Suite pytest 572/713 invariata. Sync dev+prod, commit `064c9a9`. Effetto al prossimo riavvio bot. |
| 1 | Rifornimento — da abilitare e testare | ALTA | ✅ RISOLTA 20/04 (DELAY UI + retry OCR), validata prod 11/11 istanze ciclo 20→21 |
| 3 | Zaino — deposito OCR | MEDIA | ✅ CONFERMATA CHIUSA 04/05 pomeriggio (validazione standalone su FAU_10). Test end-to-end con override soglia pomodoro 10→30M per forzare gap (FAU_10 deposito=20.30M reale): tutte le fasi funzionanti — OCR deposito ✓, calcolo gap ✓, apertura BAG via `tap_barra("bag")` score 0.972 ✓, scan inventario via TM 5 pezzature score 0.93-0.99 ✓, greedy ottimale piano (500K×5+150K×5+50K×13+10K×88+1K×79=4.859M teorici), esecuzione USE×5 step ✓, popup Caution gestito autonomo score 0.988 ✓, OCR POST verifica delta 20.30M→25.10M (+4.800M reale, scarto 1.2% OCR noise vs teorico). 93.3s totali. Le 11/11 telemetria 5gg `output={pomodoro:0,...}` erano interpretate male: output = delta pre→post (carico effettivo) = 0 quando deposito già ≥ soglia, NON = "OCR rotto". Issue #3 era già CHIUSA da RT-20 14/04, validazione 04/05 conferma robustezza algoritmo greedy + handling caution. |
| WU112 | FauMorfeus settings lightweight (Graphics HIGH) — skip raccolta_only | MEDIA | ✅ RISOLTA 03/05 notte. Sintomo utente: FauMorfeus eseguiva sequenza SETTINGS lightweight ([core/launcher.py:1008-1028](core/launcher.py)) tappando "Graphics Quality HIGH (855, 130)" + "Frame Rate MID" + "Optimize Mode HIGH" + cache cleanup ad ogni avvio. FauMorfeus ha tipologia=raccolta_only → registra SOLO RaccoltaTask come task del bot, ma il settings_helper è chiamato a livello launcher (NON come task) quindi non veniva filtrato. Il tap modificava settings utente sull'istanza master senza motivo (es. utente vuole graphics LOW per rifugio destinatario, bot lo rimette HIGH). **Fix**: aggiunto check `tipologia==raccolta_only` in launcher.py prima della chiamata `imposta_settings_lightweight`. Se raccolta_only → skip log "[SETTINGS] SKIP (tipologia=raccolta_only)". Saving secondario: ~22s/avvio ist master + nessuna interferenza utente. |
| 16 | OCR FAU_10 — valore "compila" scambiato per "reali" (legno=999M singola occorrenza) | MEDIA | ✅ RISOLTA implicitamente — verificato 03/05: solo 23/04 ha avuto l'anomalia (legno 1007M), tutti i giorni successivi <11M. Fix in 2 livelli: (1) dashboard `stats_reader._MAX_QTA_SPEDIZIONE=100M` filtra outlier (WU27); (2) bot WU34 27/04 ha sostituito source con `dettaglio_oggi.qta_inviata` (scritto al momento dell'invio dal bot, no OCR). Il bug OCR può ancora teoricamente accadere ma non impatta più aggregati né storico |
| 21 | `gitignore` duplicato errato e `rifornimento_mappa.py` V5 legacy — da valutare eliminazione | BASSA | ✅ RISOLTA 03/05 — verificato: `.gitignore` 0 duplicati (61 righe), `rifornimento_mappa.py` legacy file inesistente, riferimenti residui solo in commenti storici. Bonus fix: `index.html` schema legacy `cfg.rifornimento_mappa.abilitato`/`rifornimento_membri.abilitato` migrato a schema unificato `cfg.rifornimento.mappa_abilitata`/`membri_abilitati` post-WU40/WU94 |
| 26 | Rifornimento distribuzione sbilanciata — soglia_campo_m 50→5 | MEDIA | ✅ RISOLTA 22/04 (pomodoro era sempre sotto soglia 50M) |
| 27 | Dashboard stats OCR anomali gonfiano totali (legno 1.1B vs 117M reali) | MEDIA | ✅ RISOLTA 22/04 (_MAX_QTA_SPEDIZIONE=100M filtro) |
| 29 | Rifornimento ts_invio sottostimava ETA di ~20s | MEDIA | ✅ RISOLTA 23/04 (ts_invio DOPO _compila_e_invia) |
| 37 | setModeRemote JS non definita (pill compound task-flags-v2 silent error) | MEDIA | ✅ RISOLTA 23/04 (commit `c9ced2a` — JS + 2 endpoint PATCH rifornimento-mode/zaino-mode) |
| 40 | Flag rifornimento_mappa duplicato — sub-mode incoerente tra dashboard e bot | MEDIA | ✅ RISOLTA 23/04 (unica fonte rifornimento.mappa_abilitata; eliminato task.rifornimento_mappa) |
| 58 | Rifornimento log netto/lordo/tassa — `inviato_oggi` lordo invece di netto | MEDIA | ✅ RISOLTA 26/04 (qta_clamped_real ritornato da `_compila_e_invia`, `registra_spedizione(qta_inviata=qta_effettiva)` netto) |
| 64 | Raccolta legge slot mentre rifornimento ancora in volo → slot OCR ridotti | MEDIA | ✅ RISOLTA 26/04 step 1 (`RifornimentoState.eta_rientro_ultima` ISO; raccolta wait sempre fino a rientro, cap safety 600s) |
| 34 | Risorse netto/lordo/tassa schema (state + dashboard) | MEDIA | ✅ RISOLTA 27/04 (WU34 `RifornimentoState.inviato_lordo_oggi/tassa_oggi/tassa_pct_avg` + dashboard 6 row card + WU35 RISORSE FARM panel cleanup NETTO + WU36 CSS spacing) |
| 39 | OCR "Daily Receiving Limit" FauMorfeus + dashboard | MEDIA | ✅ RISOLTA 27/04 (WU39 commit `ef81639` — `OCR_DAILY_RECV_LIMIT` 547,146,666,173 + `shared/morfeus_state.py` storage globale + dashboard riga capienza con color coding) |
| 46 | state.rifornimento azzerato post-restart bot — race con `_controlla_reset` | ALTA | ✅ MITIGATO 27/04 (WU45 commit `8b0091f` — dashboard fallback su `data/storico_farm.json` quando state vuoto). Issue root da indagare separatamente |
| 58 | Dashboard mostra dati daily stale dopo pausa lunga | MEDIA | ✅ RISOLTA 29/04 (WU58 — `dashboard/services/stats_reader.py` 3 fix: `get_state_per_istanza`, `get_risorse_farm`, `_load_morfeus_state`. Check `data_riferimento != today_utc` → azzera in-memory totali daily + provviste residue + capienza morfeus. State file NON toccato — bot lo azzera al primo tick rifornimento) |
| WU94 | global_config.json baseline reset — task tutti default false + tick_sleep 60s + max_parallel 1 | MEDIA | ✅ IMPLEMENTATA 02/05 pomeriggio. Riallinea il file statico come "reset configurazione" neutra: 16/16 task=false, sistema={tick_sleep:60, max_parallel:1}, rifornimento schema unificato dashboard (mappa_abilitata/membri_abilitati/provviste_max — rimosse legacy `rifornimento_mappa`/`rifornimento_membri`), `acciaio_abilitato:true` come dashboard, allocazione percentuali 0-100 (era frazioni 0-1), aggiunte sezioni `rifugio`(680,531) + flag root `raccolta_ocr_debug:false` + `auto_learn_banner:true`. Verifica: con runtime_overrides vuoto → 16 task false + sistema=(1,60). Backup `.bak.20260502_pre_reset` dev+prod |
| WU95 | Dashboard config_overrides.html — sezioni complete riallineate a global_config | MEDIA | ✅ IMPLEMENTATA 02/05 pomeriggio. Pre-fix: pagina `/ui/config` esponeva solo `task flags` + sub-form rifornimento parziale (3 campi) + tabella istanze. Mancavano sezioni Sistema, Rifornimento completo (comune+rifugio+modalità), Zaino, Allocazione raccolta, Flag globali (auto_learn_banner+raccolta_ocr_debug). **Modifiche**: riscritto `dashboard/templates/config_overrides.html` con 7 sezioni complete: (1) task flags hot-reload, (2) sistema {max_parallel,tick_sleep_min}, (3) rifornimento full {modalità+rifugio+comune+4 risorse}, (4) zaino {modalità+4 risorse}, (5) allocazione raccolta {4 percentuali}, (6) flag globali {auto-learn-banner, raccolta-ocr-debug toggle}, (7) istanze. **Helper JS in `base.html`**: listener `htmx:configRequest` parsa `name="sezione[campo]"` → nested JSON + coercion `true/false/numeric` strings + serialize JSON con Content-Type per form `hx-ext="json-enc"` (l'estensione non era shipped, ricostruita inline). Smoke test: GET /ui/config 200 OK, tutte le 7 sezioni presenti |
| WU97 | Dashboard config — rimozione duplicate con overview (task flags + istanze) | BASSA | ✅ IMPLEMENTATA 02/05 pomeriggio. `/ui/config` mostrava task flags pill (già presente in overview come `task-flags-v2`) + tabella istanze (già presente in overview come `ist-table`). Rimosse entrambe le sezioni dal template; aggiunta footnote che rimanda alla overview per quei controlli. Page size 38KB → 19KB (-50%). Sezioni rimaste: Sistema · Flag globali · Rifornimento · Zaino · Allocazione raccolta — tutte config-specific |
| WU100 | Dashboard /ui/config/global — fix lettura RAW global_config (allocazione + rifornimento mappa) | ALTA | ✅ RISOLTA 02/05 sera. Bug post-WU99: allocazione 0/0/0/0% nel pannello e modalità mappa NON selezionata di default. **Root cause**: `get_global_config()` passava per `GlobalConfig._from_raw → to_dict()` che legge schema legacy (`rifornimento_mappa.abilitato`, `rifornimento_membri.abilitato`) e divide allocazione per 100 (frazioni 0-1) → template fa `0.35\|round\|int = 0`. Inoltre il dataclass non include `rifugio`, `rifornimento` unificato, `auto_learn_banner`, `raccolta_ocr_debug`, `soglia_allocazione`. **Fix in `app.py::ui_config_global`**: lettura raw via `json.load(_GLOBAL_CONFIG_PATH)` invece di `get_global_config()`. Validazione: alloc pomodoro=35, petrolio=20, mode-mappa active, rifugio (680,531), auto-learn checked |
| WU99 | Dashboard /ui/config/global — layout 4-card identico overview + PATCH endpoint merge | MEDIA | ✅ IMPLEMENTATA 02/05 pomeriggio. Riscritto template con layout `cfg4` (4 col-box affiancate: Sistema · Rifornimento · Zaino · Allocazione) clonato da overview. Ogni card ha: col-head con titolo + indicatore on/off (cerchio dot+label), col-body con p-row/rr/mode-row/alloc-bar, col-foot con bottone arancione "salva [sezione]" full-width + save-ok/save-err. **Card sistema include task baseline (16 checkbox 2-col) + flag globali**. **Tabella istanze** con colonne porta/profilo/truppe/sq/lv/layout/FT (ist-tbl). **Mumu read-only** in fondo. **Nuovo endpoint** `PATCH /api/config/global` che fa merge superficiale top-level scrivendo dict raw (NO round-trip GlobalConfig dataclass — `to_dict()` perdeva campi nuovi tipo rifugio/rifornimento_unificato/auto_learn_banner). Funzioni JS dedicate `gcSalvaSistema/Rifornimento/Zaino/Allocazione/Istanze` con prefix `gc-` per isolamento. Validazione: PATCH preserva rifugio, rifornimento unificato, auto_learn_banner, raccolta_ocr_debug, allocazione percentuali |
| WU98 | Dashboard /ui/config eliminato — unica pagina config /ui/config/global con sezione istanze | MEDIA | ✅ IMPLEMENTATA 02/05 pomeriggio. Utente: la pagina /ui/config era ridondante (duplicato di overview per task/istanze, duplicato di /ui/config/global per sistema/rifornimento/zaino/allocazione/flag). **Modifiche**: (1) `/ui/config` redirect 302 → `/ui/config/global`; (2) menu nav semplificato in `base.html`: solo voce "config" puntando a global; (3) template `config_overrides.html` rinominato `.legacy`; (4) aggiunta sezione **istanze (default statici)** in `config_global.html` con tabella editabile (nome+porta read-only · tipologia · max_squadre · livello · layout · truppe · fuori_territorio · abilitata) → JS `__saveIstanze()` chiama PUT `/api/config/istanze` (scrive su instances.json + runtime_overrides.json). Pagina ora 60KB con 6 sezioni: Sistema·Flag globali · Task baseline · Rifornimento · Zaino·Allocazione · **Istanze (default)** · Mumu read-only |
| WU103 | Predictor 5-invii target + rifornimento metrics | MEDIA | ✅ IMPLEMENTATA 03/05 notte. Estensione skip predictor per valutare cicli combinando raccolta + rifornimento. Hook `aggiungi_invio_rifornimento(istanza, risorsa, qta_netta, eta_residua_s)` in `core/istanza_metrics.py` + chiamato in `tasks/rifornimento.py` (mappa+membri) post-`registra_spedizione`. Schema record esteso con `rifornimento.invii[]`. Nuova regola `_rule_low_total_invii` (score 0.60): TARGET_INVII_CICLO=5, soglia avg = 5 × 0.5 = 2.5; ultimi 3 cicli, se avg(rac+rif) < 2.5 → skip suggerito. Guard: regola disattiva se nessun ciclo nella finestra ha rifornimento. Tool `predictor_shadow` esteso con sezione "Valutazione rifornimento": confronto avg_total_invii con/senza rifornimento per stimare ROI. Pre-restart: 426 record/3gg → tutti rif_off (rifornimento.invii non era tracciato). Post-restart: la sezione si popolerà. |
| WU105 | Rifornimento — bug "1 spedizione invece di 5" quando slot iniziale basso | ALTA | ✅ RISOLTA 03/05 notte. Sintomo: FAU_09 ciclo 18:27 con `slot liberi=1` invia 1 spedizione, attende 86s rientro, esce con `nessun slot libero dopo attesa — stop`. Pattern visto su tutte le istanze con slot iniziale<5. Root cause ([tasks/rifornimento.py:1328-1338](tasks/rifornimento.py)): branch `if slot == 0` faceva `time.sleep(attesa)` + `_aggiorna_coda` + `break` SEMPRE, anche quando dopo l'attesa lo slot si era liberato. Mancava il `continue` per ricominciare il loop e rileggere lo slot. Fix: dopo wait + aggiorna_coda, fa `continue` invece di `break` (rilegge slot OCR all'iterazione successiva, se ≥1 procede con prossima spedizione). Caso `coda vuota + slot=0` resta `break` (squadre fuori per altri task, attesa indefinita). Modo "membri" non impattato (assume `_MAX_SWIPE_TOP` slot, non legge UI). Saving atteso: max_sped 5 invece di 1-2 quando slot iniziale basso → ~3-4 spedizioni in più per ciclo per istanza → ~30-40 spedizioni in più al giorno totali (~25-30 min produttivi). |
| WU106 | Cap istanza rifornimento — cattura giornaliera alla prima spedizione | MEDIA | ✅ IMPLEMENTATA 03/05 notte. Estensione `RifornimentoState`: 2 nuovi campi `cap_invio_iniziale_oggi: int = -1` (NETTO, primo `provviste_residue` letto del giorno) + `qta_max_invio_lordo: int = -1` (LORDO max per singolo invio, dal popup compila clamped). Reset automatico in `_controlla_reset` al cambio data UTC. Nuovo metodo `registra_cap_giornaliero(cap_invio, qta_max_lordo)` idempotente (set solo se attuale `<0`). Hook in `tasks/rifornimento.py` ramo MAPPA + ramo MEMBRI: alla prima sped del giorno (`spedizioni_oggi==0`) salva entrambi e logga `[WU106] cap istanza giornaliero=X M netti \| qta max singolo invio=Y M lordo`. Persistenza in state via `to_dict`/`from_dict` (compat legacy via default `-1`). Permette stima quote dinamiche per istanza nel predictor + dashboard senza dipendere dal Daily Receiving Limit del master. Smoke test: idempotenza OK, reset OK, round-trip OK. |
| WU118 | Refactor dashboard — telemetria/storico pagine separate + pannello copertura squadre + tool report ciclo | MEDIA | ✅ IMPLEMENTATA 04/05 pomeriggio. Pre-refactor: home `/ui` overcrowded con 8 tel-card + storico eventi + cards istanze + cfg4 + tabella istanze + sidebar. Refactor: (1) creato template `dashboard/templates/telemetria.html` con 8 tel-card spostate da home; (2) creato template `dashboard/templates/storico.html` con sola tabella storico eventi spostata; (3) added route `/ui/telemetria` e `/ui/storico` in app.py; (4) nav link in base.html: home → telemetria → storico → config → api; (5) pannello "🛡 copertura squadre — ultimi 5 cicli" full-width in telemetria.html con `partial_copertura_cicli` endpoint, partial template `copertura_cicli.html`, `get_copertura_ultimi_cicli(n=5)` in stats_reader.py; ordine fisso pomodoro/legno/acciaio/petrolio + verdetti ok/warn/bad; (6) trend 7gg spostato in sidebar farm (top) di home.html — semantica produzione storica; (7) `core/istanza_metrics.py::aggiungi_invio_raccolta` esteso con param `load_squadra: int = -1` opzionale; `tasks/raccolta.py` passa load stashed da WU116. **Tool CLI**: `tools/report_copertura_ciclo.py` per istanza×ciclo con verdetti SATURA/NON SATURA. Smoke test 8/8 endpoint OK + dataset finto valido. **Effetto operativo**: dashboard home alleggerita (rimasti: card produzione, cfg4 sistema/rifornimento/zaino/allocazione, tabella istanze, sidebar farm con trend 7gg + risorse + ora-tbl). |
| WU119 | Riordino risorse uniformato pomodoro/legno/acciaio/petrolio in tutta dashboard | BASSA | ✅ IMPLEMENTATA 04/05 pomeriggio. Pre-fix: ordine misto, alcuni pannelli mostravano `pomodoro/legno/petrolio/acciaio` altri `pomodoro/legno/acciaio/petrolio`. **Fix coerente in 3 file**: (1) `dashboard/templates/index.html` — 5 modifiche (rifornimento/zaino card line 91/171, sidebar res-totali line 277/285, ora-tbl header 305, JS updateAllocTotal 418); (2) `dashboard/templates/config_global.html` — 3 modifiche (rifornimento/zaino card 95/156, JS gcUpdateAllocTotal 361); (3) `dashboard/services/stats_reader.py` — `_RISORSE_STANDARD` tuple + tupla locale `_load_storico_farm_today`. Validazione automatica: 7/7 endpoint check posizione emoji nell'HTML rendered (🍅 < 🪵 < ⚙ < 🛢) tutti pass. |
| WU120 | Bug tick_sleep unit mismatch (campo secondi, save minuti) | MEDIA | ✅ RISOLTA 04/05 pomeriggio. Sintomo utente: dashboard mostra `tick sleep 1800` post-save imprevisto. Root cause `index.html`: campo display `value="{{ cfg.tick_sleep }}"` (secondi) + label `tick sleep (s)` ma save spedisce `tick_sleep_min: gi('g-sleep')` (interpretato come minuti dal backend `SistemaOverride`). Mismatch: utente inserisce 30 (intendendo 30s) → salvato come 30 min → ricarico mostra 1800 sec → confusione. **Fix**: cambiato campo a minuti coerentemente — `value="{{ ((cfg.tick_sleep|int) // 60) }}"` + label `tick sleep (min)` + min/max `1-1440` + tooltip. `config_global.html` non toccato (usa schema baseline `tick_sleep` in secondi direttamente, già coerente). **Effetto**: prod runtime_overrides `tick_sleep_min=300` (= 5h) era artefatto bug stesso → al ricarico ora display "300 min" comprensibile, utente può correggere a 5 (validato per rifornimento OFF) o 15 (compromesso post-rifornimento attivato). |
| WU126 | rifornimento — master saturo + break→continue su risorsa fail | ALTA | ✅ IMPLEMENTATA 05/05 mattina (3 commit). `b72f292` `RifornimentoTask.should_run` check `morfeus_state.daily_recv_limit==0` → skip immediato (saving ~80s × N istanze in saturazione master); `ef7d2be` freshness check su DRL=0 vs ts < mezzanotte UTC odierna (evita guard eterno post-reset 00:00 UTC del gioco); `ca8a1fb` **break cieco → continue + skip risorsa**. Sintomo utente: "11/11 istanze inviano solo pomodoro+legno, mai acciaio/petrolio". Root cause: `_compila_e_invia` ritornava `ok=False` per acciaio (campo greyed-out o coord errata o aggiornamento gioco) → loop principale `if not ok: break` → blocco totale del rifornimento ai 2 invii pom+leg, mai più tentativi. Fix: `risorse_attive = dict(risorse_config)` (copia mutabile) + su `not ok` rimuovi risorsa + `continue`. Validazione FAU_02 post-fix: invio 1 acciaio + 1 petrolio nel primo tick post-restart. **Bug colaterale**: dashboard salva "mappa_abilitata=false" durante toggle risorse, va investigato (`dashboard/services/config_manager.py`). |
| WU128 | FauMorfeus produzione/ora gonfiata — sottrazione ricevuto da alleati | MEDIA | ✅ RISOLTA 05/05 mattina (commit `71352ed` v1 skip metrics; v2 sottrazione vera dopo feedback utente "il dato già è in farm.dettaglio"). Sintomo: "produzione/ora di FauMorfeus errata, considera anche risorse ricevute". Root cause `core/state.py::chiudi_sessione_e_calcola`: formula `prod = delta_castle - zaino_delta + rifornimento_inviato` corretta per istanze ordinarie, ma per master `delta_castle` include ~50M/h ricevuti dalle 11 ordinarie → prod_ora gonfiato. **Fix v2** (commit successivo): nuovo helper `_calcola_ricevuto_da_alleati(master_name, t0, t1)` legge `state/*.json::rifornimento.dettaglio_oggi` di TUTTE le altre istanze, filtra per ts in finestra sessione, somma `qta_inviata` (NETTO post-tassa). In `chiudi_sessione_e_calcola` per master: `prod_qty[r] -= ricevuto[r]` PRIMA del calcolo `prod_ora[r]`. Validazione 05/05: ricevuto oggi 46.3M pom + 45.3M leg + 1.0M acc + 1.0M pet (totale 93.6M netto, coerente con storico_farm.json). Master ora ha prod_ora reale (produzione interna castello). Effetto al prossimo restart bot. |
| WU129 | Telemetry pattern detector — esclusione `rifornimento` da `task_timeout_recurring` | BASSA | ✅ RISOLTA 06/05 (commit `ef91985`). Distribuzione trimodale legittima: 0-sped skip ~28s, 2-sped default ~84s, 5-sped catch-up ~278s, max ~500s. Mediana globale ~85s × 3 = 255s flagga sistematicamente i 5-sped legittimi (success=True, outcome=ok, no anomalies) come outlier. Stessa esclusione cosmetica di WU109 per `raccolta_chiusura`. `EXCLUDED_TASKS_TIMEOUT = {"raccolta_chiusura", "rifornimento"}`. Validazione su 132 eventi 5-6/05: pre-fix 5 outlier (max 539s) flaggati, post-fix 0. Real timeout già copertI da `outcome=fail` + `anomalies`. |
| WU135 | Revert hook OCR consumo addestramento (OCR fragile) | BASSA | ✅ RISOLTA 06/05 (commit `b33d6d4`). OCR `leggi_consumo_addestramento` troppo fragile in produzione: sul test FAU_05 12:53:46 (Infantry I) leggeva pomodoro 18.0K=18000 OK ma legno 13.0K interpretato come 3000 (cifra "1" iniziale persa). Errori si accumulano via `aggiungi_consumo` (somma) → state value cumulato permanentemente sbagliato. Esempio 06/05: FAU_05/06 pomodoro=191400 legno=11600 (somma di letture errate); FAU_08 pomodoro=16200 (manca legno, OCR fail totale). Rimossi: hook `tasks/truppe.py:336` chiamata + `state.truppe.aggiungi_consumo`; scorporo `cons_t` in `core/state.py::chiudi_sessione_e_calcola`. Formula prod ora: `delta_castle - zaino_delta + rifornimento_inv` (senza consumo). Mantenuti dormienti: `shared/ocr_truppe.leggi_consumo_addestramento`, `TruppeState.consumo_oggi`, `aggiungi_consumo`. Pulizia state: azzerato `consumo_oggi` residuo in 4 istanze prod. |
| WU138 | Adaptive Scheduler ordine istanze | — | ✅ IMPLEMENTATA 08/05. Sostituisce skip_predictor statico con riordino adattivo del ciclo: greedy che ad ogni step sceglie istanza con `slot_liberi_atteso(t_avvio)` massimo. Master FauMorfeus sempre fissa fondo. **Mai skip totale** — tutte le istanze processate sempre. **4 precondizioni** in OR (master DRL≥30%, rifornimento OFF, ≥50% istanze sature, sped>100, modificabili da dashboard). **2 flag** `adaptive_scheduler_enabled` + `_shadow_only` (default False+True). Persistence `data/scheduler_planned_order.json` (TTL 4h) + resume post-restart. UI card `/ui/config/global` 🎯 con stato live precondizioni. Memoria `project_adaptive_scheduler.md`. |
| WU139 | Bug dashboard PUT endpoint — Pydantic default_factory sovrascriveva campi | ALTA | ✅ RISOLTA 08/05. Sintomo utente: salvi una sezione (es. zaino) e altri campi cambiano (acciaio_abilitato true→false, rifugio coord_y 529→532, allocazione tornano a 25/25/25/25, tick_sleep_min reset). **Root cause**: `PayloadRifornimento.rifornimento_comune = Field(default_factory=RifornimentoComuneOverride)` con default Pydantic `acciaio_abilitato=False` — invio parziale rifabbricava modello con tutti i default. `ov.globali.rifornimento_comune = payload.rifornimento_comune` sostituiva in toto. Stesso pattern per rifugio, zaino, sistema, allocazione. **Fix**: refactor 4 endpoint PUT (`rifornimento`, `allocazione`, `zaino`, `globals`) per parsare JSON raw via `await request.json()` e applicare SOLO le chiavi esplicitamente presenti via `setattr` field-by-field. Test 4/4 verdi: invio parziale preserva tutti gli altri campi. Memoria `feedback_dashboard_save_merge.md` con pattern obbligatorio per nuovi endpoint futuri. |
| WU152 | Dashboard adaptive scheduler PATCH dual-write static+dynamic | MEDIA | ✅ RISOLTA 11/05 sera (commit `a30c786`). Sintomo utente: card adaptive scheduler in `/ui/config/global` non mantiene lo stato di salvataggio. **Root cause**: mismatch scrittura/lettura post-WU148. PATCH endpoint scriveva SOLO STATIC (`global_config.json`, regola WU140), ma `GET /api/adaptive-scheduler → _flags_status` legge DYNAMIC>STATIC (priorità WU148). DYNAMIC con valori vecchi → UI mostra vecchi → "non salva". **Fix**: dual-write su entrambi. STATIC (regola WU140 + bootstrap) + DYNAMIC (`runtime_overrides.json::globali`, attivazione immediata + coerenza con lettura `_flags_status`). Modifiche: `adaptive_scheduler_enabled` / `shadow_only` dual-write; `adaptive_scheduler_thresholds.{drl_residuo_m, pct_istanze_sat, spedizioni_oggi}` dual-write; cleanup legacy `drl_residuo_pct` rimosso da ENTRAMBI; response cambiata `target="static+dynamic", applies_immediately=True`. Effetto: restart dashboard uvicorn richiesto (no `--reload`). |
| WU153 | Dashboard pannelli collapsable con persistenza localStorage | BASSA | ✅ IMPLEMENTATA 11/05 sera (commit `2feaf85`). Feature globale toggle apri/chiudi per pannelli dashboard. Selettori: `.tel-card` (telemetria/predictor/preview), `.col-box` (cfg4 layout sistema/rifornimento/zaino/allocazione), `.section` (sezioni con `.sec-label`). Click su header `.tel-head`/`.col-head`/`.sec-label` → toggle visibilità body. Chevron ▼/▶ indica stato. Persistenza localStorage chiave `panel-collapsed:<slug-titolo>` (slug = testo header lowercased no-emoji max 50 char). Re-init dopo HTMX swap (htmx:afterSwap + afterSettle) garantisce nuovi pannelli con toggle + stato persistito riapplicato. Click safe: ignora target=input/button/select/label/a per non rompere controlli dentro header (checkbox enabled/shadow-only adaptive scheduler, bottone salva). Modifica globale `base.html` (CSS+JS inline), caricato da tutte le pagine. Refresh browser sufficiente. |
| WU154 | Rifornimento: stop loop spedizioni su DRL=0 mid-task | ALTA | ✅ RISOLTA 11/05 sera (commit `10fda02`). **Sintomo prod su FAU_10** (19:27-19:29): la 2ª spedizione satura il master (DRL→0); il bot legge `Daily Receiving Limit FauMorfeus=0` ma **non interrompe il loop**, tenta altre 3 risorse (pomodoro/legno/acciaio) sapendo che VAI sarà disabilitato → fail x3 → log "tutte sotto soglia → stop". ~60s sprecati. **Bug**: `should_run()` controlla DRL=0 PRE-task ma non c'è check DURING-task. **Fix in `_compila_e_invia`**: subito dopo lettura `recv_limit==0` via OCR → `KEYCODE_BACK` + return `master_saturo=True`. Tupla di ritorno estesa da 6 a 7 campi (aggiunto `master_saturo: bool`). Tutti i return interni aggiornati con 7° campo (default False). **Caller** (`_esegui_mappa` + `_esegui_membri`): unpack a 7 campi; nuovo branch `if master_saturo: break` posizionato DOPO `quota_esaurita` ma PRIMA di `not ok` (semantica diversa). Effetto: istanza che satura master mid-task esce subito invece di sprecare ~60s. Effetto al prossimo restart bot prod. |
| WU159 | Rifornimento — AVATAR_MAPPA_SOGLIA 0.75→0.55 per pin_rifugio | ALTA | ✅ RISOLTA 18/05 (commit `4fa9a1b`). Sintomo: 6/11 istanze con 0 spedizioni dopo riattivazione rifornimento (FAU_01/03/05/06/07/08). Root cause: `pin_rifugio.png` (27×28) score 0.558-0.685 sotto soglia 0.75 → fallback tap hardcoded (480,270) che mancava il castello. Debug screenshot (`data/rifornimento_debug/`) confermano castello visibile post-navigazione lente, score basso per rendering variabile (truppe/testo sovrapposti al pin). Fix: `AVATAR_MAPPA_SOGLIA` 0.75→0.55 in `tasks/rifornimento.py:100`. La ROI post-navigazione lente è ristretta (200×200), 0.55 sufficiente senza rischio falsi positivi. Attivo dal restart bot. |
| WU-RifCentratura | Rifornimento — skip centratura mappa dalla 2ª spedizione in poi | MEDIA | ✅ VALIDATA 23/05 (commit `da86da0`). Pre-fix: `_centra_mappa` (navigazione lente + match pin_rifugio + tap, ~8s) chiamata ad OGNI iterazione del loop in `_esegui_mappa`. Post-fix: `_centra_mappa` ora ritorna `tuple[int,int]`; le coordinate sono cachate in `_cached_pin`; dalla 2ª spedizione tap diretto sulle coord cachate + sleep 2.0s; se `_apri_resource_supply` fallisce → ri-centratura completa + retry. Saving confermato: 4 "tap diretto castello cached" per istanza su FAU_00/01/02/03, 5/5 spedizioni invariate, nessun fallback attivato. ~6 min/ciclo guadagnati. |
