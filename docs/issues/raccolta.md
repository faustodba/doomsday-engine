# Issues — Raccolta

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 35 voci totali · 1 aperte · 34 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| — | Modalità raccolta_fast estesa a tutte le istanze (06/05 pomeriggio) | — | 🟡 In corso 06/05 — utente attiva `tipologia=raccolta_fast` su tutte le istanze ordinarie + monitor attivo + debug screenshot toggle ON (`globali.debug_tasks.raccolta_fast=True`). Analisi costi/benefici dopo 1-2 cicli completi: confronto `sec_per_marcia` fast vs standard, ratio successo, `marce_fallite`, `recovery_count`. Telemetria `task=raccolta_fast` distinta da `raccolta` standard, `output.fast=True` campo dedicato. |
| WU173 | Dataset mappatura nodi raccolta — coordinata→tipo/livello, fase 1 raccolta dati | MEDIA | 🟡 FASE 1 IMPLEMENTATA 25/06/2026, fase 2 (uso attivo) sospesa fino a validazione utente. **Richiesta utente**: mappare i nodi della mappa (coordinate ricorrenti? stesso tipo/livello a parità di coordinata?) per poi velocizzare l'invio raccoglitori saltando lo scan CERCA. **Analisi preliminare** (mining log 2gg, 24 file correnti+`.bak`, 357 osservazioni, 214 coordinate distinte): 88% delle coordinate ricorrenti (43/49) hanno tipo+livello SEMPRE coerenti; 25/49 confermate cross-istanza (prova diretta che la mappa è condivisa tra tutte le 12 istanze, coerente con `blacklist_fuori_globale.json` GLOBALE); 1 caso (696_532) mostra cambio tipo a un MESE di distanza (campo 23/05 → petrolio giugno, coerente con l'ipotesi utente "nodo terminato → respawn dopo un periodo, eventualmente diverso"). **Bug scoperto durante l'analisi**: 2 incoerenze isolate sono FauMorfeus (3 tipi diversi sulla stessa chiave 708_531 in 16 minuti, coincidente col proprio rifugio — la lettura coordinate per quell'istanza è verosimilmente rotta, legge il proprio castello invece del nodo). **Implementazione fase 1** (`shared/nodi_mappa.py` NEW + 3 hook in `tasks/raccolta.py::_tenta_marcia`): ogni volta che CERCA legge chiave+tipo+livello (nodo trovato/prenotato O scartato fuori-territorio) viene appesa una osservazione a `data/nodi_mappa_observations.jsonl` (gitignored, runtime-only, pattern `morfeus_state.py`). FauMorfeus escluso alla fonte (`ISTANZE_ESCLUSE`). Nessun cambio di comportamento — solo osservabilità. Nuovo tool `tools/costruisci_catalogo_nodi.py [--prod] [--days N] [--write]`: majority-vote per coordinata, report instabilità + conferme cross-istanza + verdetto di maturità (soglia 70% coordinate ricorrenti concordanti, min 20). Seed iniziale generato dal mining dei log esistenti (357 oss., dev+prod). **Verdetto attuale**: tool dice "MATURO" (91.8% delle ricorrenti concordi) ma **attenzione**: 161/210 coordinate (77%) viste 1 sola volta — zero conferma indipendente. La metrica del tool guarda solo le ricorrenti, non la copertura totale: serve più tempo/cicli prima di considerare il dataset davvero pronto per l'uso attivo. Bug collaterale corretto: fixture autouse isolamento `DOOMSDAY_ROOT` aggiunta a `test_raccolta.py` (sistema anche pollution pre-esistente di `cap_nodi_dataset.jsonl` durante i test). Test: 57/57 verdi `test_raccolta.py`, nessuna regressione sul resto della repo. **Fase 2 (NON implementata)**: uso attivo del catalogo per saltare CERCA — gating esplicito dell'utente, da fare quando il dataset sarà ritenuto maturo per davvero (più giorni di raccolta, copertura totale non solo sulle ricorrenti). |
| WU174 | Dashboard — pagina /ui/nodi-mappa: tabella catalogo nodi | BASSA | ✅ IMPLEMENTATA 25/06/2026, **scatter SVG rimosso su feedback utente (illeggibile)** — resta solo la tabella. Richiesta utente: visualizzare il dataset WU173 in dashboard. Tabella dettaglio filtrabile per tipo e soglia minima osservazioni, badge ⚠ sulle righe ambigue. Campo `ambiguo` calcolato a runtime in `get_nodi_mappa_catalogo()` (non persistito nel catalogo — la variante scartata in caso di parità resta solo nel dataset grezzo). 3 file: `dashboard/services/stats_reader.py` (nuova funzione), `dashboard/app.py` (route, niente più builder SVG), `dashboard/templates/nodi_mappa.html` (nuovo) + link nav in `base.html`. Validato end-to-end con dashboard locale: 210 coordinate, 211 righe tabella (210+header), filtri tipo/min_oss verificati. Sync dev+prod. **Bot prod**: confermato già aggiornato — boot 09:52 UTC successivo alla sincronizzazione di `raccolta.py`, dataset cresciuto 357→367 osservazioni in autonomia, nessun riavvio aggiuntivo necessario per WU173/174. |
| WU175 | Catalogo nodi — separazione netta in-territorio/fuori-territorio + ultima istanza occupante | MEDIA | ✅ IMPLEMENTATA 25/06/2026. Utente ha richiesto: (1) colonna "ultima istanza occupante" sul catalogo; (2) ha notato che 696_532 con 48-49 osservazioni era impossibile parlando di "nodo occupato" — verifica sui dati grezzi: **100% delle osservazioni di quel nodo erano `esito=fuori_territorio`**, MAI una sola `trovato`. Root cause: il catalogo (WU173) mischiava le due popolazioni — `trovato` (squadra realmente inviata, max 4 osservazioni/coordinata su 2gg, 212 coordinate distinte) vs `fuori_territorio` (solo scoperta/scarto durante CERCA, può ripetersi all'infinito senza limite — concentrato su SOLO 3 coordinate con conteggi abnormi 49/32/16). Le due popolazioni non si sovrappongono mai (0 coordinate in comune) — confermato che una volta blacklistato un nodo non viene mai più "trovato". **Fix** (utente: "i nodi fuori territorio devono essere conteggiati a parte, non hanno nessuna utilità, la mappatura è utile [solo per] i nodi in territorio, il nodo occupato è effettivamente l'ultima istanza che ha occupato il nodo"): `tools/costruisci_catalogo_nodi.py` riscritto — il catalogo principale (`nodi_mappa_catalogo.json`) ora contiene SOLO coordinate con ≥1 osservazione `trovato`; le coordinate solo-fuori-territorio sono escluse e contate a parte in un nuovo file `nodi_mappa_catalogo_meta.json` (`n_coordinate_solo_fuori_territorio`). Ogni entry del catalogo ha 2 nuovi campi: `ultima_istanza` + `ultima_occupazione_ts` (dall'osservazione `trovato` più recente, NON dalle `fuori_territorio` che non rappresentano occupazione). Dashboard: nuova colonna "ultima istanza occupante" in tabella + contatore "N fuori territorio (escluse)" nel sommario. Bonus fix scoperto in corso d'opera: `n_cross_istanza` della dashboard contava erroneamente anche le coordinate ambigue/instabili nelle "confermate cross-istanza" (24 vs 20 del tool CLI) — corretto escludendo `ambiguo`. Catalogo rigenerato: 214 coordinate in territorio (50 ricorrenti, 92% concordanti — ancora "maturo" secondo soglia tool ma sempre con la stessa cautela sul 77% visto una sola volta), 3 coordinate fuori territorio escluse. Validato end-to-end con dashboard locale. Test: nessuna regressione. Sync dev+prod. |
| WU176 | Catalogo nodi — ultima_istanza solo da osservazioni live, mai dal seed storico | MEDIA | 🟡 SUPERSEDED da WU177 — il cutoff temporale arbitrario (`SEED_CUTOFF_TS`) è stato sostituito da una distinzione semantica più corretta (eventi distinti "trovato" vs "occupato"). Vedi WU177. Diagnosi originale (94% dei nodi mostravano istanza dal seed storico) resta valida e ha portato al fix definitivo. |
| WU177 | Catalogo nodi — "osservazione" e "occupazione" sono eventi distinti del flusso raccolta | MEDIA | ✅ IMPLEMENTATA 25/06/2026. Utente ha corretto l'approccio WU176 (cutoff temporale arbitrario): "la data ultima osservazione nasce dal cerca e dalla lettura del nodo, mentre l'ultima istanza occupante invece nasce quando è confermato l'invio del raccoglitore" — due eventi REALMENTE distinti nel codice, non lo stesso evento filtrato per tempo. **Fix**: nuovo esito `"occupato"` in `shared/nodi_mappa.py` (terzo valore oltre `trovato`/`fuori_territorio`), nuovo hook in `tasks/raccolta.py` allo **Step 7 (COMMIT)** — dopo `blacklist.commit(chiave, eta_s)`, quando `_esegui_marcia` ha già avuto successo (marcia realmente confermata, non solo nodo trovato). `tools/costruisci_catalogo_nodi.py` riscritto: `prima_osservazione`/`ultima_osservazione`/tipo/livello continuano a derivare da `trovato` (CERCA+lettura, invariato); `ultima_istanza`/`ultima_occupazione_ts` derivano ESCLUSIVAMENTE da `occupato` (marcia confermata) — nessun cutoff arbitrario necessario, perché l'esito "occupato" non esiste nel seed storico (mai minato dai log, che catturavano solo "trovato"/"RESERVED"), quindi è intrinsecamente sempre dato genuinamente live. Test: 57/57 verdi `test_raccolta.py`. Catalogo rigenerato: 218 coordinate, **0 con occupazione confermata** (atteso: il bot deve ancora ricaricare il nuovo hook — restart armato `claude_nodi_occupazione_confermata_WU177`). Verificato che la dashboard riflette il dato senza bisogno di riavvio proprio (legge il catalogo da disco ad ogni richiesta). Sync dev+prod. |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 9 | Raccolta — tipo NON selezionato FAU_01/FAU_02 | MEDIA | ✅ RISOLTA (fix flush frame cached + attendi_template) |
| 11 | Raccolta — Issue tipo NON selezionato FAU_01/FAU_02 | MEDIA | ✅ RISOLTA implicitamente — verificato 03/05: 0 eventi "NON selezionato" / "tipo_bloccato" in tutti i log disponibili (jsonl + .bak). Issue #9 (WU flush frame cached + attendi_template) ha risolto definitivamente |
| 15-bis | Raccolta "No Squads" non rilevato (FAU_08) | MEDIA | ✅ RISOLTA (F3 `pin_no_squads` + uscita loop `701f7bd`) |
| 16-bis | `RACCOLTA_TRUPPE` non letto (FAU_09) | MEDIA | ✅ RISOLTA (`9ba08a0`) |
| 24 | Raccolta No Squads — loop esterno + while interno + check universale (3-level break) | ALTA | ✅ RISOLTA 22/04 (FAU_09/10 da ~40 detection/tick a 1) |
| 31 | TipologiaIstanza pydantic rifiutava raccolta_only → 500 su tutti i PATCH | ALTA | ✅ RISOLTA 23/04 (enum esteso raccolta_only) |
| 26 | Allocazione raccolta non collegata al bot (_RATIO_TARGET_DEFAULT hardcoded) | MEDIA | ✅ RISOLTA 23/04 (commit `424b440` — _from_raw normalize + ratio_cfg end-to-end) |
| 42 | Donazione — ramo "pin_marked non trovato" non chiude Technology → raccolta salta | ALTA | ✅ RISOLTA 23/04 (back x3 nel branch pin_marked assente) |
| 74 | Raccolta fuori territorio per istanza (toggle dashboard) | — | ✅ IMPLEMENTATA 28/04 (WU50 `4012b70` flag `IstanzaOverride.raccolta_fuori_territorio`, WU52 `72f7b0e` sync su `instances.json`, `_nodo_in_territorio` ritorna True ⇒ no add a `BlacklistFuori`) |
| 78 | Data collection OCR slot HOME vs MAPPA — training AI agent | — | ✅ CHIUSA 04/05 (superseded). Implementazione tecnica WU55+WU55-bis OK ma motivazione iniziale (agente AI per OCR slot inaffidabile) **superata** da fix paralleli: WU70 (SX-only ensemble PSM 10/8/7 risolve 5→7 FAU_00), WU68 (sanity check post-marcia + fallback HOME), Issue #24 (3-level break No Squads). OCR classico ora robusto, agente AI non più necessario. Toggle `raccolta_ocr_debug=False` in prod, cartella `data/ocr_dataset/` vuota dopo cleanup WU63. Codice modulo `shared/ocr_dataset.py` + endpoint `/api/raccolta-ocr-debug/*` mantenuti dormienti per riattivazione futura se emergono nuovi problemi OCR. Riapribile cambiando toggle a True. |
| 80 | RaccoltaFastTask — variante fast via tipologia istanza | — | ✅ IMPLEMENTATA 28/04 (WU57 `55d2e61` nuovo `tasks/raccolta_fast.py` 440 righe, delay -33%/-47% su tap_icona/CERCA, recovery 1-shot, switch via `tipologia=raccolta_fast` con runtime swap RaccoltaTask→RaccoltaFastTask in main.py preservando priority 15/interval/schedule e tutti gli altri task attivi) |
| — | UI rename tipologie istanza + colonna FT | BASSA | ✅ RISOLTA 28/04 (`27fd5d2` labels `completo`/`completo · fast`/`solo raccolta`, riordino opzioni, header colonna `⛯`→`FT`) |
| WU67 | Raccolta livello — reset+conta sostituito con delta diretto | MEDIA | ✅ RISOLTA 29/04 sera (era SEMPRE 7 meno + N piu = 7..13 tap. Ora delta = livello - livello_panel, |delta| tap nella direzione. Saving 1.5-2s/raccolta × ~100/die × 11 istanze ≈ ~25-35min/die totali. Mantiene reset classico se OCR pannello fallisce) |
| WU68 | Sanity OCR slot post-marcia — fallback HOME se sospetto | MEDIA | ✅ RISOLTA 29/04 sera (in `_aggiorna_slot_in_mappa`: se `attive_map < attive_pre` (deterministico, bot ha appena confermato +1 squadra) → fallback HOME singolo. Cattura caso patologico `5/5 letti come 4/5` opposto del 4↔7 cross-validation esistente. Costo 13-15s solo nel ~2-3% sospetto) |
| WU69 | Pattern slot pieni — 2× maschera_not_opened → break loop | MEDIA | ✅ RISOLTA 29/04 sera (flag `ctx._raccolta_mask_not_opened` settato in `_esegui_marcia` quando maschera retry fallisce. Counter `mask_not_opened_streak` in `_loop_invio_marce`. >=2 fallimenti consecutivi su tipi diversi → `ctx._raccolta_slot_pieni=True` + break. Saving 60-90s per ciclo patologico) |
| WU70 | OCR slot SX-only ensemble — risolve bug "5→7" | ALTA | ✅ RISOLTA 29/04 sera (proposta utente: tagliare "/" e cifra DX, leggere SOLO SX in ROI 10×24 isolata. Branch primario in `leggi_contatore_slot` quando `totale_noto>0`: 3 PSM 10/8/7 ensemble + sanity pre-vote `0≤v≤totale_noto` + majority vote. Totale=config deterministico. Validazione FAU_00 c6: pre-fix=0 inviate skip, post-fix=1 inviata pulita) |
| WU84 | Cap nodi dataset — censimento + OCR popup gather + storage JSONL + tool analisi | — | ✅ IMPLEMENTATA 30/04 sera. Censimento manuale FAU_07 validato 9/9 OCR test: pomodoro/legno L7=1.32M L6=1.20M; acciaio L7=660K L6=600K; petrolio L7=264K L6=240K. **Pattern L7=L6×1.10**, ratio 100/100/50/20. ROI fissa `(270,280,420,320)` 150×40 px (popup gather sempre al centro mappa). 4 file: `shared/ocr_helpers.py` `leggi_capacita_nodo()` cascade PSM6 raw→binv th150, `shared/cap_nodi_dataset.py` JSONL append `data/cap_nodi_dataset.jsonl` (best-effort thread-safe), `tasks/raccolta.py` hook post-`_leggi_livello_nodo` (registra anche nodi sotto livello_min, try/except), `tools/analisi_cap_nodi.py` CLI 3 sezioni (cap nominale per tipo+liv, campioni per istanza, residuo% per (istanza,tipo)). `sync_prod.bat` esteso `xcopy tools/*.py`. Memoria `reference_capacita_nodi.md`. **Validazione 1/5**: 177 campioni in 15h, OCR 100% match nominali, residuo medio 88-100% per istanza (FAU_10 80.9% più stressata) |
| WU87 | Storico eventi dashboard — durata 0 + filtro non persistente | MEDIA | ✅ RISOLTA 01/05 (commit `6f48eaf`). 3 bug: (1) `main.py:840` usava `max(orc._entries)` → solo ultimo task (raccolta_chiusura, sempre durata 0); (2) `_TaskEntry` non aveva campo durata; (3) HTMX refresh 30s azzerava i select filtro. Fix: aggiunto `last_duration_s` in `core/orchestrator.py` (start/end timing attorno a `task.run()`), `main.py` itera tutti gli `entries` con `last_run >= _tick_start_ts`, dashboard select hanno `name="..."` + `hx-include="#f-istanza, #f-task"` per persistenza. Dropdown task esteso con `main_mission`. Storico truppe: ora itera da `load_instances()` per includere FauMorfeus + dedup |
| WU89 | Persistenza metriche per-istanza per-ciclo (foundation skip predictor) | — | ✅ IMPLEMENTATA 01/05 sera (commit `6f48eaf`). Modulo `core/istanza_metrics.py` con buffer in-memory thread-safe per istanza + flush atomic JSONL su `chiudi_tick`. Schema record: `ts, instance, cycle_id, boot_home_s, tick_total_s, raccolta{attive_pre/post,totali,invii[{tipo,livello,cap_nodo,eta_marcia_s}]}, task_durations_s{}, outcome`. 5 hook minimal: `inizia_tick`/`chiudi_tick` in `main.py::_thread_istanza` (start+fine, propaga `last_duration_s` da orchestrator), `imposta_boot_home` in `core/launcher.py` post-HOME, `aggiungi_invio_raccolta` in `tasks/raccolta.py` post-`_esegui_marcia` OK, `imposta_raccolta_slot` a fine `RaccoltaTask.run()`. Storage `data/istanza_metrics.jsonl` append-only, best-effort silenced. CLI `tools/analisi_istanza_metrics.py [--prod] [--days N]` con 5 sezioni (Boot HOME, Tick totale, Task durata, Raccolta, ETA marcia). Validazione first records: FAU_03 c0 340s 2 invii / FAU_04 c0 491s 3 invii / FAU_05 c0 290s 0 invii (slot pieni). **Step 3-5 proposti (NON implementati)**: skip_predictor.py + hook main loop + dashboard pannello predizioni vs realtà |
| WU91 | MainMissionTask — daily 24h con guard ora UTC ≥ 20 | MEDIA | ✅ IMPLEMENTATA 02/05 mattina. Pre-fix: `periodic 12h` → 2 run/die in qualsiasi ora. Post-fix: `daily 24h` + `should_run` con `if datetime.now(timezone.utc).hour < 20: return False`. **Razionale**: ricompense mission accumulano nel giorno, raccolta a fine-giornata massimizza chest milestone (AP elevato). Finestra utile 20:00→01:00 UTC (5h, 2-3 tick disponibili). Se bot fermo nella finestra → salta giorno (accettato). File: `config/task_setup.json:7`, `tasks/main_mission.py:38-40` (import datetime/timezone) + `222-231` (guard) |
| WU89-Step3 | Skip Predictor — predictor flag-driven, no side-effect, growth-aware | MEDIA | ✅ IMPLEMENTATA 03/05 sera. Modulo `core/skip_predictor.py`: pure function `predict(istanza, history, state) → SkipDecision(should_skip, reason, score, signals, growth_phase, guardrail_triggered)`. **5 regole** (in ordine): squadre_fuori (slot saturi+ETA), trend_magro (last3 avg_inv<0.5), recovery (outcome degraded+gap<5min), low_prod (prod/h<100K, MIN 5 cicli, prod>0 protezione WU47). **Growth phase block**: truppe<100K (es. FAU_09) → low_prod inibita per non spezzare loop raccolta→truppe→raccoglitori. **Guardrail anti-stallo**: max 3 skip consecutivi → forza retry, re-eval ogni 6 cicli, cooldown 2 cicli post-retry. **Tool CLI** `tools/predictor_shadow.py [--prod] [--days N] [--istanza X] [--verbose]`: replay storico con stato simulato, output skip count + ragioni + saving stimato. Flag config `skip_predictor_enabled` (default False) + `skip_predictor_shadow_only` (default True) in `globali.*` + `GlobaliOverride` + `GlobalConfig`. **Validazione 416 record/3gg**: 33 skip suggeriti (8%), 44 guardrail-blocked, saving stim 116 min. Step 4 (hook orchestrator) e Step 5 (dashboard) NON implementati per non toccare struttura bot |
| Bug A | runtime_overrides perde `raccolta` ad ogni save dashboard → tutte le istanze skippano raccolta dal 18:23 UTC 03/05 | ALTA | ✅ FIX A 03/05 notte (immediato hot-reload). Aggiunta `"raccolta": true` direttamente in runtime_overrides prod. Combinato con WU102 (root cause). Sintomo: `Orchestrator: [raccolta] should_run=False → saltato` su FAU_01 ciclo 18:45-18:48. |
| WU102 | TaskFlags.raccolta — fix root cause Bug A | ALTA | ✅ RISOLTA 03/05 notte. Aggiunto `raccolta: bool = True` al modello Pydantic `TaskFlags` ([dashboard/models.py:40-78](dashboard/models.py)). Era stato escluso con il rationale "gira sempre, non controllabile" → ogni `_save_ov(ov)` faceva `model_dump()` serializzando solo i 15 task **senza** `raccolta`, cancellando la chiave da `runtime_overrides.json::globali.task` ad ogni save dashboard. Fallback su `global_config.json::task.raccolta=false` (baseline reset WU94) → tutte le istanze skippavano raccolta. UI non lo espone come toggle (resta sempre ON), ma chiave preservata in JSON ad ogni save. Validazione: `TaskFlags().raccolta=True`, `model_fields` include `raccolta`. |
| WU109 | Telemetry pattern detector — esclusione raccolta_chiusura da task_timeout_recurring | BASSA | ✅ RISOLTA 03/05 notte. Sintomo: pattern detector flaggava 15 outlier `raccolta_chiusura` con max 225s (severity high). Analisi: il task ha distribuzione bimodale legittima (mediana ~3s in skip-mode "0 squadre libere", 60-225s in work-mode "invio 1-4 marce raccolta"). Tutti gli outlier osservati avevano `invii≥1` = lavoro reale, no timeout patologico. La soglia mediana×3 dell'algoritmo fallisce su distribuzioni bimodali. **Fix** ([core/telemetry.py:594-597](core/telemetry.py)): set `EXCLUDED_TASKS_TIMEOUT = {"raccolta_chiusura"}` skippato nel detector. Cosmetico (non incide sul bot, solo dashboard). Smoke test: pre-fix 2 entries (raccolta+raccolta_chiusura), post-fix 1 entry (solo raccolta). |
| WU116 | OCR carico squadra (Load) + dataset copertura squadre + tool analisi | MEDIA | ✅ IMPLEMENTATA 04/05 mattina. Misura quanti nodi non vengono "chiusi" dalla squadra che li attacca (squadra underprovisioned → nodo non rigenera al max, residuo deve essere chiuso da altre istanze/giocatori). 4 file: (1) `shared/ocr_helpers.py`: `_ZONA_LOAD_SQUADRA=(610,420,780,455)` 170×35 px sopra MARCH btn, `leggi_load_squadra(img)` cascade `raw → binv150` con regex `_LOAD_RE` per estrarre primo gruppo digit-comma (resiste a rumore timer ETA sottostante). Validato 8/8 test FAU_01: cifre 5-9, popup gather no falsi positivi, binv200 scartato perché distorce cifre piccole "5"→"9". (2) `shared/cap_nodi_dataset.py::registra_cap_sample()`: aggiunto param opzionale `load_squadra: int = -1` (backward compat). Schema record esteso `{ts, instance, tipo, livello, capacita, load_squadra}`. (3) `tasks/raccolta.py`: `_esegui_marcia` legge load post-`_leggi_eta_marcia` e stash su `ctx._raccolta_load_squadra`. Registrazione differita al post-marcia con cap+load combinati (1 record per invio invece di 2). Su marcia FAIL: load=-1, cap comunque registrato. (4) `tools/analisi_cap_nodi.py`: nuova **Sezione 4 "Copertura squadra"** per (istanza, tipo) = `load_squadra / cap_nodo`, verdetti `OK satura ≥95% / marginale 75-94% / ⚠ underprovisioned <75%`. Aggregato per istanza media coperture. Smoke test funzionale: identifica correttamente FAU_09 con copertura 50% come underprovisioned. **Razionale gameplay**: con `RACCOLTA_TRUPPE=0` (auto), il game usa il minimo truppe per riempire il nodo. Se squadra non basta → load < cap_nodo → nodo non chiuso → resta "aperto" finché qualcun altro non lo finisce, NON rigenera al max nominale. Il KPI guida la decisione di training truppe per istanze deboli. Effetto al prossimo restart bot. |
| WU127 | boot_home_s include post-HOME overhead (settings + troops) | MEDIA | ✅ RISOLTA 05/05 mattina (commit `1d0ab34`). Sintomo utente: predictor sotto-stima T_ciclo. Per FAU_00 con solo 'raccolta' dovuta, predicted=251.9s vs reale tick_total=926.4s (gap include boot+settings+troops+8 task non previsti). Pre-fix: `imposta_boot_home(nome, home_total_s)` chiamato in `attendi_home` SUBITO dopo HOME stab (riga 1004), PRIMA di settings_lightweight (~32s) + troops_reader (~0-30s) + banner dismissal (~5-15s). Quindi `boot_home_s` rifletteva solo "avvio→HOME" (~120s), non "avvio→pronto-per-task" (~157s). Effetto: ~37s × 12 istanze = ~7min sotto-stima sistematica T_ciclo. Fix Opzione A: spostato la chiamata a FINE `attendi_home` (post settings+troops) calcolando `ready_total_s = now - _t_attesa_home_start`. Pro: 1 file, 2 righe modificate, schema invariato, predictor auto-converge in ~10 cicli via rolling stats. Contro: discontinuità record vecchi (~120s) vs nuovi (~157s) per ~10 cicli, cambia semantica del campo `boot_home_s` (ora "avvio istanza → pronto per orchestrator.tick()"). |
| WU133 | Raccolta — registra slot pre/post anche su skip slot pieni | MEDIA | ✅ RISOLTA 06/05 (commit `b27d347`). Pre-fix: 3 path early-return per "slot pieni" (`libere==0` da param, OCR anomalo `attive>totale`, post-OCR HOME) saltavano `imposta_raccolta_slot()` → record `data/istanza_metrics.jsonl` con `raccolta.invii=[]` ma SENZA `attive_pre/attive_post/totali`. Effetto: pannello dashboard `P(squadre_fuori al ritorno)` saltava silenziosamente proprio i tick più rilevanti per la metrica (slot saturi al ritorno). Esempio FAU_00 ciclo 170 (10:59:52): `raccolta:{invii:[]}` senza `attive_pre`. Visivamente confermato slot saturi (squadre fuori ciclo precedente non rientrate, coerente con `reference_capacita_nodi.md` tempo raccolta L7 ~2h). Fix: 3 chiamate `imposta_raccolta_slot` aggiunte prima di ogni return early con valori coerenti (attive_pre=attive_post=attive_inizio, totali=obiettivo); per OCR anomalo: attive_pre=attive_post=totale_noto. |
| WU134 | RaccoltaFastTask — DebugBuffer (WU115) integrato + esposizione UI debug | BASSA | ✅ IMPLEMENTATA 06/05 (commit `fe117f5`). Pre-fix: `RaccoltaFastTask` non aveva DebugBuffer; `"raccolta_fast"` non in `_KNOWN_TASKS` dashboard. Post-fix: `DebugBuffer.for_task("raccolta_fast", instance)` factory; 7 snap point (`00_post_home`, `01_post_vai_mappa`, `02_marcia{N}_pre_{tipo}`, `03_marcia{N}_fail_{tipo}`, `04_post_loop_home`, `99_vai_in_mappa_fail`, `99_recovery_fail_marcia{N}`); flush condizionale (anomalia `marce_fallite>0 OR recovery_count>0` OR `debug.enabled` forza flush per analisi); 2 hook `imposta_raccolta_slot` aggiunti coerenti con WU133. `"raccolta_fast"` aggiunto a `_KNOWN_TASKS` in `api_debug.py` per esposizione UI WU115. Restart bot necessario. |
| WU142 | Dashboard refactor UI 08/05 — card istanza inline + pagina advanced + cleanup | — | ✅ IMPLEMENTATA 08/05 pomeriggio/sera. **Refactor UI esteso**: (1) Card istanza HOME con sezione controlli inline (`_build_ctrl_block_html` in `app.py`, 6 campi: ON/FT/tipologia/truppe/livello→**nodo**/max_squadre→**squadre** + bottone salva). PATCH real-time → dynamic. Auto-refresh sospeso quando input in focus (listener `htmx:beforeRequest`). Scroll position preservato post-save (listener one-shot `htmx:afterSwap`). (2) Pagina `/ui/advanced` NEW (link nav top-bar) con 2 sezioni: tabella istanze bulk (configurazione veloce) + addestramento truppe (default globale + override per istanza). Save bulk SEQUENZIALE (`for await`) invece di `Promise.all` — fix race condition su file `runtime_overrides.json`. (3) Pagina `/ui/storico` ELIMINATA (route + nav rimossi, partial endpoint mantenuto come API). (4) `/ui/predictor` con sezione "configurazione predictor (WU89)" SOPRA a tutto, sezione predictor RIMOSSA dalla card sistema HOME. (5) `/ui/telemetria` 3 pannelli rimossi (ciclo corrente, tempi medi 7gg IQR, copertura squadre 5 cicli — ridondante con predictor). (6) `/ui/config/global` ridotto: sezione truppe spostata in advanced. **7 bug fix UI**: enum tipologia render `TipologiaIstanza.full`→`full` (`.value`), tipologia="raccolta" alias morto rimosso, `partial_ist_table` `livello`/`max_squadre` override>static, scroll lost on refresh, focus lost during edit, master istanza FauMorfeus aggiunta ai controlli card, race condition save bulk. Memoria `project_dashboard_refactor_08_05.md`. |
| WU143 | Switch tipologia raccolta_fast → full per FAU_01..FAU_10 | ALTA | ✅ APPLICATO 09/05 sera. Diagnosi: efficacia `raccolta_fast` crollata 86.5% (07/05) → 50.9% (08/05) → 43.2% (09/05) per **deadlock blacklist fuori globale**. Sintomo log FAU_01/05/07/10: `RaccoltaFast [campo]: nodo 726_538 in blacklist fuori — skip` ripetuto fino a 0/N inviate. Root cause: `_tenta_marcia` 1-shot non avanza al prossimo nodo della lista lente quando il primo è in blacklist (CERCA deterministica → stesso primo nodo → recovery+restart-from-zero). Concausa: WU141 cleanup `livello` 7→6 ha esposto le istanze ai nodi blacklistati L6. **Fix multi-livello**: (1) **DYNAMIC switch** `tipologia: raccolta_fast → full` per FAU_01..FAU_10 in `runtime_overrides.json`; (2) STATIC `instances.json` allineato; (3) blacklist fuori globale svuotata (18→0 entries, ricostituita a 3 nodi reali post-cicli notturni). FAU_00 + FauMorfeus invariate. **Trovato bug correlato**: prima del fix la modifica statica isolata era inefficace, perché `_carica_istanze_ciclo` ([main.py:128](main.py#L128)) priorizza `runtime_overrides::tipologia` su `instances::profilo`. Memorizzato in `feedback_modifiche_sempre_dinamiche.md` (regola: ogni modifica config va su DYNAMIC). **Risultato post-switch (notte 09→10/05)**: marce/run da 0.63 → **1.75** (+178%); FAU_01 da 0/12 → 13 marce in 7 run; slot pieni post-task 60-100% per istanza. Cicli più lunghi (~85-90 min vs 70 min fast) ma molto più produttivi. |
| WU149 | Dashboard — pagina dedicata "AB test adaptive scheduler" | BASSA | ✅ IMPLEMENTATA 11/05 mattina. Nuova pagina `/ui/ab-test` per analisi storico A/B `adaptive vs naive` ordering scheduler. Sorgente dati: `data/predictions/scheduler_ab.jsonl` (31 record validati prod 08-11/05, media delta_slot **+2.94 slot/ciclo**, win rate **80.6%**). **3 file**: (a) `dashboard/templates/ab_test.html` (66 righe, 3 sezioni HTMX); (b) `dashboard/templates/base.html` voce nav "ab test" inserita tra "predictor istanze" e "config"; (c) `dashboard/app.py` 4 endpoint dopo `ui_predictor_redirect` (riga 304+): route principale `/ui/ab-test` + 3 partial `/ui/partial/ab-test-{summary,trend,detail}` + helper `_load_ab_records()`. **3 sezioni pagina**: (1) Riepilogo sintetico — N cicli, delta medio/best/worst, vince/tie/perde, barra win rate, trigger frequenti; (2) Trend SVG inline 860×160 — punti colorati verde/rosso per ciclo, linea baseline naive tratteggiata; (3) Tabella ultimi 15 cicli — ts/trigger/n_istanze/Σadaptive/Σnaive/delta/ordine_adaptive. **Correzioni codice ricevuto vs file esistente**: `_data_path` non esiste → sostituito con `Path("C:/doomsday-engine-prod") / "data" / "predictions" / "scheduler_ab.jsonl"` (pattern uguale a righe 1938/1953/2387/2730); `_env_css()` non esiste → `_env_label()` ritorna già dict con `env_css`, uso `**_env_label()` per unpack; `async def` → `def` sincrono per coerenza route UI; `include_in_schema=False` aggiunto; `import json as _json` locale (top-level non lo importa). Smoke test: `ast.parse` OK syntax dev. Sync prod via `sync_prod.bat`. Restart dashboard prod necessario per attivare (uvicorn no `--reload`). Commit dev `2bb12b3`. |
| WU160 | Raccolta — fallback livello 6→7 ripristinato (simmetrico a 7→6) | MEDIA | ✅ RISOLTA 18/05 (commit `75473af`). Sintomo: istanze `livello=6` (FAU_01..FAU_08) non tentavano nodi L7 quando territorio L6 saturo → 0 marce inviate. Root cause: modifica 08/05 aveva cambiato `livello=6 → [6]` (strict). Logica corretta confermata dall'utente: simmetrica. Fix: `elif livello_base == 6: sequenza_livelli = [6, 7]` in `tasks/raccolta.py:1589`. Razionale gameplay: 11 istanze sulla stessa mappa saturano territorio L6, il fallback L7 evita blocchi. Attivo dal restart bot. |
| WU-Raccolta-Lv | Raccolta — selezione livello nodo: delay reset + verifica pre-CERCA | MEDIA | ✅ RISOLTA 22/05 (commit `e7421c5`). Sintomo: FAU_01/FAU_02 (livello=6) tappavano CERCA prima che il selettore livello si aggiornasse → "NON selezionato" + tipo_bloccato. Fix: delay reset 0.3→0.8s + rilettura livello post-reset + tap delay 0.25→0.4s + verifica finale pre-CERCA. Validato 22/05: 0 NON selezionato + 0 tipo_bloccato su FAU_01/02/03/05 nei log odierni. |
| WU-MaxSquadre | FAU_00/FauMorfeus usavano 4 slot invece di 5 (default silenzioso config) | MEDIA | ✅ RISOLTA 07/06. Sintomo utente: FauMorfeus non completava tutte le raccolte. Root cause (bug-class C6 dell'analisi WU163): `config_loader._ovr("max_squadre", 4)` legge SOLO l'override dynamic, mai lo static `instances.json`; FAU_00/FauMorfeus avevano `max_squadre=5` in static ma il campo MANCAVA in `runtime_overrides.json::istanze` → fallback hardcoded 4 → 1 slot raccolta perso/tick. Fix: aggiunto `max_squadre: 5` dynamic per entrambe. Validato post-restart: FAU_00 `inviate=5`. Fix strutturale C6 (fallback `_ovr` su static prima del default) rimandato a Fase 2 dell'analisi. |
