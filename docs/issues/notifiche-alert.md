# Issues — Notifiche & alert (email/Telegram)

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 7 voci totali · 1 aperte · 6 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 81 | Update Version popup gioco — detect + gestione | BASSA | 🆕 APERTA 28/04 (downgrade ALTA→BASSA 04/05) — pulsante "Update Version" + icona triangolo arancione "Up" appare in HOME riga eventi superiore quando client gioco ha nuova versione. Zona ~520-590, 40-95. Proposta: template `pin_update_version.png` + hook `attendi_home`, decisione skip istanza/alert dashboard/auto-pause se >=80% istanze. Pattern affine a #77 MAINTENANCE ma livello client. APK update richiede interazione utente (sideload/store) — bot non può autonomamente. Non bloccante finché non ricorre |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU137 | Email Notifier — pipeline daily report + alert (Step A-F) | — | ✅ IMPLEMENTATA fase 1 + enrichment 08/05. 6 file nuovi: `shared/mailer.py` (SMTP), `shared/secrets.py` (app password storage write-only), `core/notifier.py` (queue persistente JSONL + dispatcher async + retry exponential backoff), `core/daily_report.py` (builder 10 sezioni: cicli/produzione/trend7gg/rifornimento/truppe/performance/boot/copertura/eventi/anomalie), `dashboard/routers/api_notifications.py`, `dashboard/templates/partials/notifications_card.html`. Hook `main.py` post-ciclo per `maybe_send_daily_report()` + dispatcher boot. App password gestita da dashboard (`data/secrets.json`, gitignored, mai esposta da API GET). Schema config `globali.notifications.{enabled, daily_report_enabled, daily_report_hour_utc, from_addr, recipients, smtp.{host,port}}`. Tutto parametrico, `_NOTIFICATIONS_DEFAULT` vuoti — `load_effective_notifications()` merge baseline+runtime_overrides. Validato end-to-end 08/05 con `bot.dooms.report@gmail.com` → `fausto.pace@gmail.com`. Fase 2 alert real-time PENDING. |
| WU140 | Architettura config STATICA vs DINAMICA + bootstrap + reset | — | ✅ IMPLEMENTATA 08/05 sera. Regola architetturale vincolante: STATIC (`global_config.json`+`instances.json`) modificabile SOLO da `/ui/config/global`, DYNAMIC (`runtime_overrides.json`) SOLO da HOME. Mai mirror automatico, i 2 piani INDIPENDENTI durante funzionamento. Match SOLO in **bootstrap** (primo avvio senza runtime) e **reset** esplicito. Implementato: (a) `bootstrap_runtime_from_static_if_missing()` in `config_loader.py` con helper `_build_runtime_from_static()` (deep copy, esclude mumu/_note); (b) hook in `main.py` boot pre-load; (c) endpoint `POST /api/config/reset` (force=True); (d) banner UI giallo + bottone "↺ reset runtime"; (e) refactor 5 endpoint config (PUT /istanze, PUT /istanze/truppe, PATCH /notifications, PATCH /adaptive-scheduler) per scrivere SOLO static; (f) PATCH `/api/config/overrides/istanze/{nome}` rimosso write su instances.json (HOME → solo dynamic). Memoria `architecture_config_static_dynamic.md`. |
| WU141 | Cleanup file statici instances.json + global_config.json | BASSA | ✅ IMPLEMENTATA 08/05. Cleanup `instances.json`: (a) rimosso campo `layout` (deprecato WU22 — TM dinamico); (b) rimosso campo `lingua` (non più supportato, sempre EN); (c) allineato `livello` con valori dynamic correnti utente (FAU_01..FAU_08 7→6, FAU_00/FauMorfeus restano 7). Cleanup `global_config.json`: aggiunte sezioni mancanti per garantire bootstrap funzionante — `truppe.caserme = {infantry,rider,ranged,engine: True}`, `notifications.{enabled=true, from_addr, recipients}`, `debug_tasks = {}`. Pre-fix il reset/bootstrap cancellava truppe e mail config utente perché static incompleto. Bug fix correlati: `_InstanceCfg` rimossi `cfg.layout` e `cfg.lingua` (codice morto), `IstanzaOverride.layout` rimosso da Pydantic, `allowed_fields` `save_instances_fields` esteso a 8 campi (pre 4: max_squadre/livello/raccolta_fuori/master; post: + abilitata/truppe/profilo/fascia_oraria/truppe_override). Mapping `tipologia` enum → `profilo` instances.json gestito esplicito con `.value`. |
| WU144 | Fix alert `master_saturo_long` STALE post-mezzanotte UTC + auto-reset DRL | MEDIA | ✅ RISOLTA 10/05. Sintomo utente: alert "master saturo" continua a scattare anche il giorno DOPO la saturazione (mail spedite a 00:14 UTC anche se gioco aveva già resettato DRL). **Root cause**: `core/alerts.py::check_master_saturo` confrontava `daily_recv_limit==0` + `elapsed_s = now - ts > soglia`, ma `ts` è timestamp ULTIMA LETTURA OCR — se FauMorfeus disabilitata o ultima nel ciclo, post-mezzanotte UTC il file `morfeus_state.json` resta con `daily_recv_limit=0, ts=ieri`. Il gioco resetta DRL a 00:00 UTC ma il bot non ha ancora ri-letto → falso "saturo da 15h". **Fix in 2 livelli**: (1) `core/alerts.py::check_master_saturo` skip se `ts.date() != now.date()` UTC, log diagnostico; (2) `shared/morfeus_state.py::load` auto-reset valore stale: se `ts.date() != today` ritorna in memoria `daily_recv_limit = daily_recv_limit_max` (cap stimato 200M dal max monotone), file su disco invariato (la prossima `save()` da OCR riallinea). Marker `_stale_reset_applied=True` per diagnostica. **Beneficio collaterale**: tutti i consumer (`adaptive_scheduler._master_drl_residuo_m`, dashboard pannello DRL, daily_report) ora vedono valore non-stale → niente più precondizione adaptive falsa post-mezzanotte. Test 3/3 verdi. Effetto al prossimo restart bot. |
| WU146 | Daily report — revisione completa sezioni 1-11 + nuova sez 2 produzione interna rifugio | — | ✅ IMPLEMENTATA 10/05. Stress test ridondanze su tutte le 11 sezioni (eliminato dato duplicato tra sez 1/3/5/10), aggiunta nuova **sezione 2 PRODUZIONE INTERNA RIFUGIO** (effettiva, da `state/<ist>.json::produzione_storico[]`) distinta dalle "risorse inviate al master" della sezione 3. Numerazione shifted (era 1-10, ora 1-11). Regola formattazione `_fmt_dur_s` aggiornata (mai secondi se ≥60s). 11 sezioni rivisitate: (1) CICLI con uptime%/produttività; (2) PRODUZIONE INTERNA RIFUGIO NEW; (3) RISORSE INVIATE AL MASTER (totale netto + throughput + tassa scartata); (4) TREND vs media 7gg con ▲▼= e totale aggregato; (5) RIFORNIMENTO con range tassa + saturazione + tabella tutte 11 istanze; (6) TRUPPE master separato + Δ ieri/7gg per tutte 12; (7) PERFORMANCE TASK tutti i 15 + p95 + outliers%; (8) BOOT HOME→READY label chiarito (WU127); (9) COPERTURA SQUADRE solo <100% + summary 100% + n_attacchi; (10) EVENTI RILEVANTI alert email + restart + HOME timeout (consolidato da sez 1); (11) ANOMALIE TASK aggregato per task con fail_rate% + causa principale. Memoria `project_daily_report_revisione.md` + `feedback_format_durata.md` + `reference_tassa_rifornimento.md` per drivers tassa rifornimento (livello rifugio, NON distanza). |
| WU163 | Analisi multi-agente read-only + Fase 0 osservabilità | ALTA | ✅ IMPLEMENTATA 07/06 (commit `5090ef5`). Analisi approfondita 44 agenti / 16 subsystem (report `docs/analisi_2026-06-07.md`, 27 findings critical/high tutti verificati su codice, 0 falsi positivi). 5 temi: monitoring cieco, default silenzioso da config, success spurio su screenshot None, fragilità OCR/ROI, igiene repo. Piano in 5 fasi. **Fase 0 — ripristino osservabilità**: (C1/O1) `core/alerts.py::check_heartbeat_cicli` non scattava MAI — leggeva `cicli.json` (dict `{"cicli":[...]}`) come fosse una lista → iterava le chiavi-stringa → `return False` sempre; inoltre cercava chiave `ts_end` invece di `end_ts` reale. Fix: riusa `telemetry.load_cicli()` (import lazy) + chiave `end_ts`. (C3/O4) `main.py::record_istanza_tick_end` hardcoded `esito="ok"` anche su cascade ADB → sezioni cascade/abort/fail del daily report erano codice morto. Fix: il thread `_thread_istanza` propaga l'esito reale (`cascade`/`ok`) via dict module-level `_ultimo_esito_tick`, letto dal main loop dopo `t.join()`. Validato: py_compile + simulazione heartbeat su `cicli.json` prod (PRE=None sempre, POST=ultimo ciclo completato). Item 3: `debug_tasks={}` prod verificato. Effetto al restart bot. |
