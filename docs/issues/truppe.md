# Issues — Truppe (addestramento)

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 6 voci totali · 0 aperte · 6 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU65 | Lettura giornaliera Total Squads + storico crescita | — | ✅ IMPLEMENTATA 29/04 sera (`core/troops_reader.py`, hook in `attendi_home` post-settings. OCR `_ZONA_TOTAL_SQUADS=(830,60,945,90)` cascade otsu→binary. Storage `data/storico_truppe.json` retention 365gg, atomic write, idempotenza intra-day. Runtime: FAU_10=112,848, FAU_00=2,665,764 registrati c4) |
| WU66 | Dashboard truppe — card istanza + storico 8gg | — | ✅ IMPLEMENTATA 29/04 sera (Layout A riga 🪖 + Δ7gg + sparkline ASCII 7-char in card produzione istanze; Layout B endpoint `/ui/partial/truppe-storico` + section index.html con tabella ordinata Δ% desc + riga TOTALE; HTMX refresh 60s. Funzioni `get_truppe_istanza`, `get_truppe_storico_aggregato` in stats_reader) |
| WU73 | Dashboard storico truppe — ordinamento per indice istanza | BASSA | ✅ RISOLTA 30/04 notte (in `get_truppe_storico_aggregato`: sort key cambiato da `delta_pct desc` a `r["nome"]` alfabetico → FAU_00, FAU_01, ..., FAU_10, FauMorfeus. Più intuitivo per debug per-istanza) |
| WU101 | Master istanza — flag config-driven generalizzato | ALTA | ✅ IMPLEMENTATA 03/05 notte. Helper `shared/instance_meta.py` (cache 30s + `is_master_instance` + `get_master_instances` + `invalidate_cache`) legge flag `master:bool` per istanza da `instances.json` (default statico) + `runtime_overrides.json::istanze.<nome>.master` (override hot, prevale). Schema config esteso: `IstanzaOverride.master:bool`, `InstanceStats.master:bool`, persistito in instances.json via `save_instances_fields` (allowed_fields esteso). **9 punti di esclusione** lato server: `predict()` (early-return), `predictor_shadow`, `get_truppe_storico_aggregato` (espone `master_row` separato), `get_all_stats(include_master=False)`, `get_produzione_istanze`, `get_risorse_farm`, `get_produzione_storico_24h`, `_compute_trend_7gg`, `_build_rollup_from_events`. **UI**: colonna "M" toggle in tabella istanze /ui/config/global + ★ accanto al nome, ★ in card_istanza partial, riga master con sfondo dorato in `partial_truppe_storico`. Cache invalidata in PUT /api/config/istanze. Migrazione: FauMorfeus.master=true (dev+prod, instances+overrides). Smoke test 7/7 verdi. |
| WU121 | Master FauMorfeus ripristino + ★ marker uniforme in dashboard | BASSA | ✅ RISOLTA 04/05 pomeriggio. Diagnosi utente: master indicator non visibile in dashboard. Verifica: `is_master_instance("FauMorfeus") = False`, `get_master_instances() = frozenset()` — flag perso (probabile artefatto save dashboard recente con Pydantic default `master=False`). 4 file aggiornati: `instances.json` + `runtime_overrides.json` (dev: già OK, prod: aggiornato `FauMorfeus.master=true`). Cache invalidata. **Bonus fix**: `dashboard/app.py::partial_ist_table` (tabella istanze home overview) non aveva ★ marker accanto a istanze master — aggiunto coerente con altri 4 pannelli (produzione-istanze cards, truppe-storico riga separata sfondo dorato, /ui/config/global colonna M toggle). Validazione 4/4 pannelli ★+FauMorfeus presenti. **Effetto**: 9 punti di esclusione server-side (predictor, telemetry, rollup farm, copertura squadre) tornano a operare correttamente — FauMorfeus non più aggregata con istanze produttive. **REVISITATA 04/05 sera**: utente segnala check ancora cliccabili → eliminata colonna M dalla UI (index.html + config_global.html), `shared/instance_meta.py` riscritto con `_HARDCODED_MASTERS=frozenset({"FauMorfeus"})` (no più config-driven, no cache TTL). Master cambiabile solo da PR/commit. ★ marker resta presente in 5 pannelli. |
| WU132 | Truppe — schema config 4-caserme indipendenti + override per istanza | MEDIA | ✅ IMPLEMENTATA 06/05 (commit `d36c593`). Pre-fix: `TruppeOverride` aveva `tipo_solo` (1 sola caserma) + `livello` (forza tap target) + `count_min` (skip se < soglia). Limitato. Post-fix: `globali.truppe.caserme.{infantry,rider,ranged,engine}` con 4 flag indipendenti. Niente più scelta livello (gioco usa max disponibile) né count_min (TRAIN col default gioco). Override per istanza Opzione A (completo): `istanze.<nome>.truppe_override.caserme.{tipo}: Optional[bool]` (null=eredita default). 5 file modificati: `dashboard/models.py` (TruppeCaserme + TruppeOverride + TruppeIstanzaOverride); `config/config_loader.py` (GlobalConfig fields + merge override istanza); `tasks/truppe.py` (refactor _esegui_ciclo: OCR titolo → check flag → skip "skip_flag" o procedi); `dashboard/templates/config_global.html` (rimossa sub-sezione legacy in card sistema, nuova sezione full-width con card globale 4 checkbox + tabella per istanza con select default/ON/OFF + reset); `dashboard/routers/api_config_overrides.py` (nuovo endpoint `PUT /api/config/istanze/truppe`, `PUT /istanze` esistente fa merge per non cancellare truppe_override). Validazione 6/6 endpoint live. |
