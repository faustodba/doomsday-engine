# Issues — OCR · Template matching · Banner

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 20 voci totali · 3 aperte · 17 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 54 | Banner catalog & dismissal pipeline boot stabilization — 573 UNKNOWN polls | — | 🟡 parziale — framework + 4 banner attivi (exit_game_dialog, auto_collect_afk_banner, equipment_report, privileges_subscription_trial WU237 22/07); banner_eventi_laterale resta disabilitato (26/04, conflitto icona DS) |
| 54 | Banner catalog & dismissal pipeline boot stabilization | — | 🟡 parziale (estesa con `pin_btn_x_close` + `pin_btn_back_arrow` in WU26/66) |
| — | BannerLearner — dedup non collassa varianti sulla stessa x_coords, MAX_ENTRIES saturo | BASSA | 🔍 17/07/2026 — trovato durante uno scambio con Gemini (`shared_ai_exchange/`), inizialmente frainteso come "pipeline morta" (vedi nota sotto), poi corretto e approfondito. **Il sistema FUNZIONA**: l'AUTOLEARN inline in `shared/ui_helpers.py::dismiss_banners_loop` (righe 480-529, dentro lo Step A1 X-dorata) supera la race documentata da WU110 (03/05, deprecazione del vecchio blocco "Step LEARNER" separato a riga 575, che quello sì resta strutturalmente morto/ridondante). Prova: `data/learned_banners.json` in prod ha 25 entry (= `MAX_ENTRIES`, cap pieno), `last_used` più recente il giorno stesso, `hit_count` fino a 281. **Problema reale verificato**: le 25 entry si concentrano su sole 7 posizioni X fisiche (8 duplicati su `(870,97)`, 7 su `(825,138)`) — il dedup (`find_duplicate`, soglia `template_similarity>=0.85` sul crop titolo 780×50px) non collassa mai le ri-registrazioni sulla stessa posizione: misurato empiricamente 0.27–0.82 fra coppie duplicate, sempre sotto soglia. Causa radice (analisi indipendente convergente di Claude e Gemini): il crop titolo è troppo ampio e cattura contenuto grafico dinamico (artwork/countdown/testo variabile) che varia fra popup diversi condividendo la stessa X di chiusura, impedendo il match anche quando si tratta "concettualmente" dello stesso tipo di popup. Impatto pratico basso — `success_count` alti anche sulle entry duplicate, il dismiss funziona comunque (una delle varianti matcha). **Decisione condivisa: nessuna modifica a caldo** — rischio di regressione su una pipeline che sta già producendo tassi di successo alti, non giustificato dall'impatto (spreco di capacità, non blocco funzionale). Backlog per revisione futura: raffinamento `title_roi` (crop più stretto/stabile) o dedup a 2 livelli (bucket per prossimità `x_coords` prima del confronto template). Dettagli completi in `shared_ai_exchange/claude_to_gemini.md` (seq 50-52). |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU192 | FauMorfeus (+altre) — splash "Doomsday x Fairy Tail" (v1.58.0) non riconosciuto, boot abortito dopo 300s | ALTA | ✅ RISOLTA 05/07/2026. Richiesta utente ("sembra che il bot non stia mandando raccoglitori" su FauMorfeus). Log `bot.log`+`FauMorfeus.jsonl`: 5 episodi in 24h per FauMorfeus (17:51, 20:40, 23:28, 01:56, 05:14 — quasi ogni suo turno, sempre ultima in coda) con `[BANNER-LOOP] nessun banner riconosciuto` + `[LEARNER] detect_x_candidates: 0 candidate` ripetuti per 300s → `TIMEOUT: schermata ancora UNKNOWN` → `attendi_home() fallito` → istanza chiusa, zero raccolta per quel giro. Screenshot `debug_task/boot_unknown/*_streak5_*.png` confermano lo stesso schermo su **FauMorfeus + FAU_01 + FAU_06 + FAU_07** (1 episodio ciascuna nelle stesse 24h): splash crossover "DOOMSDAY x FAIRY TAIL" (client v1.58.0), barra caricamento ferma 6-23%. **Non un bug del banner-learner (WU190)**: quel sistema impara popup con una X da chiudere — una schermata di loading non ne ha, `detect_x_candidates: 0` è il comportamento corretto per un oggetto fuori scopo. **Causa reale**: `shared/ui_helpers.py::is_loading_splash()` — funzione dedicata già esistente, pensata apposta per riconoscere "sto ancora caricando" tramite 2 anchor invarianti a prova di reskin evento (icona "Live Chat" basso-sx + testo "Version" alto-dx, soglia 0.75) — su QUESTO specifico splash la barra di progresso dell'evento si sovrappone al bordo destro della ROI Live Chat, degradando il match. Misurato su screenshot reale: score Live Chat=0.599, Version=0.209, entrambi sotto soglia 0.75 → `is_loading_splash()` tornava `False` → il bot smetteva di aspettare passivamente e trattava il caricamento come popup sconosciuto. **Fix**: soglia Live Chat abbassata 0.75→0.55 (soglia Version invariata, non serve — l'OR la copre già). Validato: score reale 0.599 ora sopra soglia; su 3 screenshot MAP genuini (non-splash) lo stesso anchor scora -0.06/0.06/0.0 — ampio margine, nessun rischio falsi positivi. Test funzionale end-to-end con screenshot reali (splash→True, MAP→False) + suite pytest 572/713 invariata (nessun test dedicato preesistente su `ui_helpers.py`/`launcher.py`). Sync dev+prod, commit `0939d58`. |
| 13-bis | ADB screenshot None cascata (FAU_03/04/06/07 ciclo 19/04) | ALTA | ✅ RISOLTA (F1a `624ba7a` + F1b `1d1b4eb`) |
| 25 | Tracciamento diamanti nello state (OCR già letto ma non persistito) | BASSA | ✅ RISOLTA 03/05 — pre-fix: `ProduzioneSession.diamanti_iniziali` salvato in `apri_sessione`, ma `chiudi_sessione_e_calcola(risorse_finali, ts_fine)` non riceveva `diamanti_finali` → delta non calcolato. Fix: aggiunti campi `diamanti_finali: int = -1` + `diamanti_delta: int = 0` a ProduzioneSession (with from_dict/to_dict), parametro `diamanti_finali` opzionale in `chiudi_sessione_e_calcola`, chiamata in `main.py` ora passa `rd.diamanti`. Smoke test: round-trip OK, delta calcolato correttamente |
| 55 | Store re-match fallback multi-candidate — swipe non-idempotente bordi mappa | ALTA | ✅ RISOLTA 26/04 (WU23 — multi-candidate sorted desc, cascade retry) |
| 61 | Discovery snapshot mancante post-dismiss banner / mid-tick UNKNOWN | MEDIA | ✅ RISOLTA 26/04 → SUPERSEDED da #63 (snapshot rimossi, sostituiti da tap X auto in memoria) |
| 63 | Tap X auto-fallback per banner unmatched + no più screenshot su disco | MEDIA | ✅ RISOLTA 26/04 (`dismiss_banners_loop` tap (910,80) post-HOME-check; counter `_unmatched_tap_x` in dict ritorno; cartelle banner_unmatched/+vai_in_home_unknown/ rimosse) |
| 66 | Banner unmatched: X cerchio dorato + freccia BACK ↩ via match dinamico (2 template) | MEDIA | ✅ RISOLTA 26/04 (`pin_btn_x_close.png` 45×50 cerchio dorato per popup eventi; `pin_btn_back_arrow.png` 45×55 freccia BACK per schermate nidificate Alliance/Hero/Bag; flow A1 X→A2 BACK→B HOME/MAP→C BREAK). Test: Pompeii X=1.000, Alliance BACK=1.000 |
| 68 | Stabilizzazione HOME: dismiss banners loop NON chiamato attivamente (149s avvio FAU_05 invece di ~30s) | ALTA | ✅ RISOLTA 26/04 (launcher.attendi_home invoca `_try_dismiss()` pre-stab + on-instability + pre-vai_in_home_finale; rimosso `_snap_post_home` write su disco) |
| 71 | Store scan grid: continua tutti i 25 step anche con match >= 0.80 (~30-40s sprecati) | MEDIA | ✅ RISOLTA 26/04 (`soglia_store_early_exit=0.80`: scan interrotto al primo match alto, procede diretto al tap; multi-candidate fallback preservato per casi sotto early_exit) |
| 60 | Settings lightweight client gioco (Graphics/Frame/Optimize LOW) | ALTA | ✅ IMPLEMENTATA 29/04 + INTEGRATA in launcher (WU60+WU61 — `core/settings_helper.py` `imposta_settings_lightweight(ctx)` 8 step Avatar→Settings→System→Graphics LOW→Frame LOW→check Optimize visuale→[tap se non attivo]→3 BACK. Hook in `core/launcher.py::attendi_home` post-`vai_in_home()` finale, try/except con lazy import. Coord calibrate via getevent FAU_01. Toggle stateful Optimize via template `pin_settings_optimize_low_active.png` ROI 108-198×317-357 soglia 0.70. Delay maggiorati PC lento NAV=3.0/TOGGLE=2.0/PRE_CHECK=1.5/BACK=2.0 ~22s totali) |
| WU64 | Pulizia cache giornaliera 1×/die in fase settings | — | ✅ IMPLEMENTATA 29/04 sera (state `data/cache_state.json`, hook in `core/settings_helper.imposta_settings_lightweight` post-BACK1, polling CLOSE template `pin_clear_cache_close.png` ogni 5s max 120s. Coord calibrate FAU_10: Help (570,235), Clear cache (666,375), Clear icon (480,200), CLOSE (480,445). Runtime FAU_10/FAU_00 c4: CLOSE detected dopo 6s, ~22s totale fix. Skip-on-already-done idempotente intra-day) |
| WU71 | Stabilizzazione HOME — polling 3s → 1s | BASSA | ✅ RISOLTA 29/04 sera (in `core/launcher.attendi_home` polling stable_count, sleep 3.0→1.0. Saving 5×2 = 10s/istanza × 11 = ~110s/ciclo. Trade-off 3× CPU screenshot+match al secondo durante stab. Attivo al prossimo restart spontaneo) |
| WU88 | Nuovo task `main_mission` — Chapter + Main + Daily + chest milestone | — | ✅ IMPLEMENTATA 01/05 (estesa supporto 3-tab in pomeriggio). Recupera ricompense da Chapter Mission (alcune istanze, tap CLAIM cieco grigio=no-op), Main Mission (find_one + tap dinamico, 2 sezioni Main+Side), Daily Mission (find_one + tap dinamico) e chest milestone bonus (5 chest 20/40/60/80/100 con AP >= soglia). **Detection runtime 2-tab vs 3-tab** via OCR testo "Chapter" su ROI (10,75,110,135). Layout adattivo: 2-tab Main(50,100)+Daily(50,185); 3-tab Chapter(50,105)+Main(50,185)+Daily(50,265). Template CLAIM `pin_btn_claim_mission.png` 80×35 verde, ROI Main (790,100,890,470) ampia per coprire sezioni multiple. CLAIM Chapter coord fissa (850,455). OCR Current AP ROI (180,130,240,175) upscale 3x + th>200 + PSM7, **letto DOPO claim daily** (i claim aggiungono AP). Validato FAU_02 (2-tab): 1 daily→AP 0→50→2 chest. Validato FAU_08 (3-tab): 1 chapter + 8 main claim (auto-scroll lista) + 0 daily + 3 chest (AP=70). Delay PC lento: 3s/2.5s/2s/3s/2s |
| WU85 | Store debug screenshot buffer — dump on fail/skip per analisi UI cambiata | MEDIA | ✅ IMPLEMENTATA 01/05 mattina. Issue: store 22 runs notte 30/04→1/05 con **68% fail/skip** (7 ok, 6 fail "Store non trovato", 9 skip "Merchant/Label/Carrello"). Pattern bimodale temporale (peggio sera/notte, ok alba). **Buffer in-memory** `_StoreDebugBuf` accumula screenshot a punti chiave durante esecuzione: `00_pre_banner`, `01_post_banner`, `02_no_candidates`, `03_rematch_fail`, `10_pre_tap_mercante`, `11_post_tap_(diretto/edificio)`, `12_label_fail_<score>`, `13_carrello_fail_<score>`, `14_merch_open_close`. **Flush solo on outcome ≠ COMPLETATO** → no spreco disco su 7/22 ok. Path: `data/store_debug/{istanza}_{ts}_{idx:02d}_{label}.png`. Toggle: `_DEBUG_STORE_FAIL_DUMP=True` modulo-level (disattivare dopo analisi). 5 punti snap nel codice (3 in `_esegui_store`, 5 in `_gestisci_negozio`). Smoke test pass; 5 pytest fail erano pre-esistenti unrelated |
| WU172 | Store — memorizzazione posizione edificio per istanza, evita scan completo quando invariata | MEDIA | ✅ IMPLEMENTATA 25/06/2026 — richiesta utente: l'edificio Store è fisso nel mondo per ogni istanza (non si sposta mai), lo scan a griglia spirale 25 passi (anche con early-exit WU71) costa ~20-40s ad ogni esecuzione anche quando la posizione è identica al run precedente. **Mining storico**: analizzati i log `[STORE] passo N → score=... *** match ***` su tutte le istanze (log correnti + `.jsonl.bak` del giorno precedente) — **passo 7 vincente per 10/11 istanze** (FAU_07 passo 8), corrispondente a un offset di swipe netto `(0,+300)` dalla vista di partenza (calcolato dalla griglia a spirale `cfg.griglia`, passo 8 → `(+300,+300)`). Segnale fortissimo e consistente, non casuale. **Implementazione**: nuovo modulo `shared/store_position.py` (`load`/`save`, storage `data/store_position.json` per istanza, atomic write tmp+fsync+os.replace, pattern identico a `morfeus_state.py`). In `tasks/store.py::_esegui_store`: prima di qualunque scan, se esiste una posizione memorizzata per l'istanza → applica un singolo swipe diretto (`_applica_delta_swipe`, helper estratto e riusato anche dalla cascata di recovery multi-candidato preesistente) + 1 verifica; se confermata (score >= soglia_store) → procede direttamente al negozio, scan saltato. Se non confermata → torna allo start (swipe inverso) e procede con lo scan classico invariato, che a fine ricerca aggiorna la memoria **solo se la posizione è nuova o diversa** dalla precedente. Seed iniziale `data/store_position.json` popolato con i dati minati (dev+prod). **Bug collaterale trovato e corretto**: il nuovo codice chiamava sempre `store_position.load/save`, scrivendo nella vera cartella `data/` del repo dev durante i test (nessun isolamento `DOOMSDAY_ROOT`) — aggiunta fixture `autouse` in `tests/tasks/test_store.py` che monta `DOOMSDAY_ROOT` su `tmp_path`, pattern già usato in `test_telemetry_rollup.py`. Test: 34/39 verdi (5 fail pre-esistenti invariate, stesso identico set pre/post-fix verificato via `git stash`), nessuna regressione sul resto della repo (149 fail pre-esistenti totali, tutte unrelated — navigator/rifornimento_base/task ABC async). |
| WU181 | Store "non trovato" — origine scan non ancorata al rifugio (pan ereditato dal task precedente) | ALTA | ✅ RISOLTA 26/06/2026 — diagnosi: i fail "Store non trovato" NON sono assenza dell'edificio (sempre presente) né revert grafica HIGH (UI HOME rende a 0.988 identica in fail/ok), ma **origine non ancorata**. L'offset memorizzato (`store_position`, WU172) e la griglia ±600px sono relativi al pan di camera lasciato dal task precedente; `vai_in_home()` ri-centra sul rifugio SOLO via toggle MAP→HOME, ma entrando da HOME già attiva è un no-op → pan ereditato. **Correlazione telemetria 26/06** (28 run): TUTTI i 9 fail preceduti da `messaggi` (7) o `arena_mercato` (2) — pan non centrato; **0 fail su 15 run** dopo `raccolta`/`donazione` — pan centrato. Evidenza score: verify pos.memo 0.33-0.36 + grid_max 0.40-0.43 (rumore) nei fail vs verify 0.66-0.74 negli ok. **Fix** (`tasks/store.py::run` step 0): forzato giro `vai_in_mappa()` + `vai_in_home()` prima dello scan → camera agganciata al rifugio in modo deterministico, offset valido a prescindere dal predecessore. Banner ri-chiuso dal `_comprimi_banner` successivo (regola default=chiuso). Chiude anche la vecchia ipotesi "Store edificio da spostare" (non era la posizione dell'edificio). Test 34/39 (5 fail pre-esistenti invariati, verificato via `git stash`). **VALIDATO SUL CAMPO 27/06** (bot riavviato 26/06 19:26 UTC, log `Re-center rifugio via MAP→HOME ✓` presente): 11 run **0 fail (0%)** vs 9/28 (32%) pre-fix. I predecessori che causavano TUTTI i fail ora ne causano 0: `arena_mercato` 7 run (6 ok, 1 skip), `messaggi` 1 run (ok). I 4 skip residui sono `Merchant non confermato`/`Carrello non trovato` legittimi (store trovato e aperto, mercante non offerente per rotazione VIP) — non più "Store non trovato". |
| WU182 | Produzione risorse — valori anomali da misread OCR (delta telescopico) | ALTA | ✅ RISOLTA 27/06/2026 — diagnosi: la produzione è la somma di delta `fin−ini` di letture OCR a tick consecutivi → **telescopa** a (ultima lettura del giorno − prima). Un singolo misread OCR agli estremi inquina l'intero totale giornaliero della risorsa. Esempio FAU_05 acciaio: valore vero ~74.10M stabile (5 letture identiche), misread 11.x/41.3 agli estremi → fantasma `41.30−11.00=30.30M`; FAU_02 legno: jitter ±0.2M su ~35M piatto → −0.5M. Causa: `ocr_risorse_robust` teneva "prima lettura ≠ −1 vince", non filtrava i misread plausibili (11.10M e 74.10M entrambi validi). **Fix (punto b)**: lettura a **consenso 3-su-5** su screenshot freschi (~0.8s — su quella scala la produzione reale ≪ granularità 0.1M, ogni divergenza è errore OCR); valore accettato solo se compare 3 volte (moda), minoranza scartata; senza consenso → −1 → fallback al valore precedente. Early-exit a 3 letture se stabili, diamanti inclusi. Chiamanti `main.py` aggiornati. Smoke test 4 scenari OK, `py_compile` OK. Limite: intercetta misread frame-dipendenti (caso FAU_05); punti (a) mediana mobile / (c) filtro per-salto da valutare dopo runtime. |
| WU183 | Lettura risorse — ordine boot (read prima dei settings ciechi) + stab HOME 5→7 + statistica | ALTA | ✅ RISOLTA 27/06/2026 — l'utente ha individuato che la lettura risorse girava DOPO i settings a click cieco (Graphics HIGH): su sistema lento lo schermo poteva essere sporco (banner / tap ciechi su HOME non stabile) → OCR fallita. Ordine reale: stabilizzazione → vai_in_home → **settings ciechi** → troops → comprimi_banner → read. **Fix**: (1) read iniettata in `attendi_home` come callback `on_home_ready`, eseguita subito dopo `vai_in_home()` finale e **prima** dei settings (HOME più pulita; closure in main perché usa ctx.state); (2) stabilizzazione HOME **5→7 poll** (+~2.5s/istanza, trascurabile); (3) statistica persistente `data/ocr_read_stats.jsonl` (`{ts,instance,fallback,tutte_ko,diamanti_ok}`, append-only) + tool `tools/ocr_stats.py`. `py_compile` OK. Monitor attivo fino 28/06 mezzanotte. Complementare a WU182 (consenso): WU182 neutralizza i misread numerici, WU183 garantisce che il read giri su HOME pulita. **(cont. 28/06)**: la statistica ha catturato FAU_02 `tutte_ko=True` (banner copre la top-bar dopo la stabilizzazione; `pin_region` resta 0.988 anche con barra coperta). Fix: dismiss banner **in-loop** in `ocr_risorse_robust` (param `on_banner` + budget `max_dismiss=2`, guard anti-loop) — chiude il banner e ritenta senza consumare i tentativi di consenso. `main.py` passa `_dismiss_banner` a entrambe le letture. Smoke test E/F verdi. Limite: recupera banner transienti; `exit_game_dialog` ricorrente di FAU_02 = salute istanza MuMu (separato). |
| WU237 | Banner catalog — "Privileges Subscription Trial" popup IAP | MEDIA | ✅ IMPLEMENTATA 22/07/2026. Scoperta live (non da `debug_task/boot_unknown/`): utente collegato via ADB diretto a FAU_00 (bot fermo, sessione di calibrazione manuale) ha trovato il popup promo abbonamento (30gg trial) navigando nel Mall — segnalato come **ricorrente su tutte le istanze SENZA acquisti reali** (tutte le ordinarie). Titolo "Privileges Subscription Trial" su banner scuro bordato oro + pulsante "Go Claim" (**mai tappato** — un trial subscription può avere conseguenze diverse da un semplice claim risorse, es. rinnovo automatico) + X cerchio dorato in alto a destra, posizione fissa `(813,94)` diversa dal canonico `DEFAULT_X_TOPRIGHT (910,80)`. Template `pin_privileges_subscription_title.png` (510×60, titolo completo) croppato dallo screenshot live. Entry `BANNER_CATALOG` priority 3 (tra equipment_report=2 e banner_eventi_laterale=5 disabilitato), `dismiss_action="tap_x_topright"` con `dismiss_coords` override. File: `shared/banner_catalog.py`. Effetto al prossimo restart bot. |
| WU92 | Banner catalog — Equipment Report popup IAP | MEDIA | ✅ IMPLEMENTATA 02/05 mattina. Discovery: bot prod osservato pattern UNKNOWN persistente ~57s su FAU_02 09:04 + FAU_03 09:10. Cattura via `_save_discovery_snapshot` esistente (streak 4 → `debug_task/boot_unknown/FAU_02_*streak4_20260502_094902.png`). Visivamente: popup IAP gioco "Equipment Report" — titolo arancione su carta beige, 6 icone equipment, CTA €19,99. Chiusura: cartellino bordeaux a forma di diamante con X bianca/oro in alto-destra del popup (~800-855 × 25-80, NON la stessa X cerchio dorato di `pin_btn_x_close`). 2 template aggiunti: `pin_equipment_report_title.png` (34×340 titolo) + `pin_btn_x_tag_diamond.png` (51×55 X chiusura). Entry `BANNER_CATALOG` priority 2 (dopo afk priority 1) con threshold 0.80, dismiss_template ROI (780,15,880,90). File: `shared/banner_catalog.py` + 2 nuovi PIN. Effetto atteso: chiusura al primo iter invece di 57s freeze + 2 tap X cieco fallback |
| WU136 | Dismiss banner pre-OCR risorse castello + retry post-fail | MEDIA | ✅ RISOLTA 06/05 (commit `ffff14f`). Sintomo FAU_00 ciclo restart 15:40: `[BANNER] non rilevato (aperto=0.231 chiuso=0.391) — no-op` poi 3 tentativi OCR risorse castello tutti KO → fallback prec → prod/h=0 spurio. Root cause: `comprimi_banner_home` chiamato pre-OCR fa solo check banner LATERALE (no-op se assente), non gestisce overlay top-bar (Equipment Report, eventi laterali). Fix in 2 livelli (`main.py::_thread_istanza`): (1) PRE-OCR `dismiss_banners_loop(max_iter=4)` smaltisce catalog + X cerchio dorato + BACK arrow, log `[OCR-PREDISMISS] banner chiusi: ...`; (2) POST-FAIL: se TUTTE 4 risorse castello = -1 → 1 round dismiss attivo + 1 retry finale (`max_attempts=2, sleep_s=1.5` per delay UI vincolante). Effetto al prossimo restart bot. |
