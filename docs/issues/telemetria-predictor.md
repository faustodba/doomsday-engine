# Issues — Telemetria · Predictor · Scheduler

> Archivio tematico voci WU/issue (estratto verbatim da `.claude/CLAUDE.md` il 07/06/2026).
> 22 voci totali · 1 aperte · 21 risolte. Legenda stato: ✅ risolta · 🟡 parziale · 🆕 aperta · 🔍 da osservare · ⏸ pausa.

## 🔓 Aperti / parziali

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| 53 | Telemetria task & dashboard analytics — events JSONL + rollup + KPI | — | 🆕 APERTA 26/04 — MVP ~12h (memoria `project_telemetria_arch.md`) |

## ✅ Risolti

| # | Issue | Priorità | Stato |
|---|-------|----------|-------|
| WU210 | Rimozione A/B test scheduler (adaptive vs naive) — UI + funzionalità | BASSA | ✅ RIMOSSA 14/07/2026 su richiesta utente. La feature WU149 (proposta E 08/05: confronto **virtuale** ordine adaptive greedy vs naive alfabetico, `Σ slot_liberi_atteso`) non serve più. **Rimosso — UI**: nav link `ab test` in `base.html`, pagina `dashboard/templates/ab_test.html` (git rm), 4 endpoint in `app.py` (`/ui/ab-test` + `/ui/partial/ab-test-{summary,trend,detail}`) + helper `_load_ab_records`. **Rimosso — funzionalità**: `core/adaptive_scheduler.py::compute_ab_test_metrics` + `record_ab_test`, il call-site nel loop ciclo di `main.py`, e il target `data/predictions/scheduler_ab.jsonl` da `tools/rotate_predictor_logs.py`. Il greedy adaptive resta invariato (tolto solo il confronto virtuale col naive). py_compile + jinja + import moduli OK, nessun test referenzia le funzioni rimosse. Le route rimosse danno 404 pulito dopo restart dashboard; niente più scrittura di `scheduler_ab.jsonl` dopo restart bot. OVERVIEW/reference.html/ROADMAP-storico conservano la storia. |
| WU202 | Cutover predictor Fase B — T_marcia da misura empirica (flag-gated, tiered) | MEDIA | ✅ IMPLEMENTATA 12/07/2026. Piano completo estratto in `docs/issues/predictor-cutover-plan.md` (analisi predictor attuale + verifica dati prod + design a fasi). Richiesta utente: "estrai, verifica e progetta" → poi "procedi con la fase B completa". **Cosa fa**: `core/skip_predictor.py::_calc_t_marcia_min` diventa TIERED — stima empirica primaria (`shared/tempo_raccolta_estimator.stima_tempo_raccolta`, `durata_s + eta_ritorno`) quando la cella (istanza,tipo,livello) ha ≥3 campioni, altrimenti fallback alla formula statica invariata (saturazione×T_L_max×coef). Gate su nuovo flag `globali.tempo_raccolta_empirico_enabled` (**default OFF**, pattern `adaptive_scheduler_enabled`, DYNAMIC>STATIC, hot-reload cache 15s): con flag OFF il risultato è **byte-identico** al pre-Fase B (zero regressione, verificato). Firma invariata → i 3 consumer (`compute_slot_liberi_atteso`, `predict_slot_liberi_l1`, `_rule_squadre_fuori`) beneficiano da un solo punto. Bonus: l'empirico non richiede `load_squadra`, copre anche invii pre-WU116 dove lo statico ritorna None. **Correzioni collaterali**: (1) confronto WU200ter usava `2×eta+durata_s` (un eta di troppo, l'andata è già in `durata_s`) → corretto a `eta+durata_s` (~1min, cosmetico); (2) cache mtime sul dataset di output (`_carica_dataset_output`, pattern WU197) — il confronto WU200ter già oggi rileggeva l'intero file per-invio ad ogni run scheduler. **Analisi eliminazioni rivista sui dati** (vs memoria WU200ter): confermato eliminare `core/t_marcia_calibration.py` (closed-loop quasi inerte: 5/21 coef attivi, marginali 0.95-0.975); **NON** eliminare `config/predictor_t_l_max.json` (declassare a fallback ultimo — acciaio L6 n=1, campo L6 n=4 restano scarni); `empirical_slot_predictor` blend rimandato (ortogonale). **Verifica prod (155 match, 24/40 celle ≥3 campioni)**: FAU_00 petrolio7 statico 127min vs empirico 131min (+3.2%, allineata); FAU_02 petrolio7 164.5 vs 190.2min (**+15.6%**, statico sottostimava ~25min); acciaio L6 fallback identico. UI: toggle nella card adaptive scheduler (`/api/adaptive-scheduler` PATCH dual-write). Test `tests/unit/test_calc_t_marcia_tiered.py` (6), suite correlata 42/42, baseline pytest invariata. **Pending (utente)**: attivare shadow→LIVE pilota. Fase C (cleanup calibrazione) non iniziata. |
| WU200 | Stimatore empirico tempo di raccolta reale per (istanza, tipo, livello) | MEDIA | ✅ COSTRUITO 11/07/2026, in prod. Sostituisce la stima statica ("~2h per L7", `reference_capacita_nodi`) incrociando due dataset indipendenti: evento INVIO (`esito="occupato"` in `nodi_mappa_observations.jsonl`, hook `tasks/raccolta.py` riattivato) ed evento COMPLETAMENTO (`report_raccolta_dataset.jsonl`, vedi WU199 in `docs/issues/raccolta.md`). Nuovo modulo `shared/tempo_raccolta_estimator.py`: `esegui_riconciliazione()` (match con stato persistito `data/tempo_raccolta_match_state.json`, cursori byte incrementali sui due dataset sorgente) + `stima_tempo_raccolta(instance, tipo, livello)` (mediana con fallback a granularità (tipo,livello) globale, poi None) + `pota_dataset_vecchio()`. Loop periodico in `dashboard/app.py::_tempo_raccolta_loop()` (15 min, pattern `_nodi_mappa_rebuild_loop`) — **mai** nel task raccolta, per esplicita richiesta utente. `TTL_ORFANE_ORE=4.0` + `RETENTION_GIORNI=15` (richiesta utente, commit `43abd32`, etichettato WU200bis — **collisione di numerazione** con la voce dashboard sotto, disambiguare per hash commit). Output `data/tempo_raccolta_dataset.jsonl`: `{ts_match, instance, coordinata, tipo, livello, ts_invio, ts_raccolta, durata_s}`. 32 test unitari (`tests/unit/test_tempo_raccolta_estimator.py`). **4 bug trovati e corretti durante lo sviluppo, tutti da verifica dati live richiesta dall'utente**: (WU200quater, commit `69c068e`) ordine `occupazioni→potatura→match` perdeva per sempre coppie valide al primo run "di recupero" su storico già accumulato — fix riordina a `occupazioni→match→potatura`, il match si tenta sempre prima di scartare qualunque cosa; (WU200quinquies, `94ec85f`) chiave di match solo `istanza|coordinata` rischiava mismatch su respawn nodo con tipo/livello diverso — filtro tipo+livello aggiunto; (WU200sexies, `779b64e`) su richiesta esplicita utente la chiave è stata resa esplicita a 4 componenti `istanza|coordinata|tipo|livello` invece di un filtro procedurale, con migrazione automatica trasparente di tutti i formati storici; (WU200septies, `1203ce1`) caso reale (nodo 707_543 su FAU_06, occupazione Lv6 vs report Lv7 con capacità nominale L7 esatta) ha rivelato che `livello_nodo` registrato all'invio è il TARGET di ricerca, non una lettura del nodo reale (12-30% mismatch storico già documentato in `tasks/raccolta.py::_cerca_nodo`) — il livello è stato rimosso dalla chiave (torna a 3 componenti `istanza|coordinata|tipo`), il Tab Report diventa fonte di verità sul campo `livello` finale, l'originale registrato all'invio resta in `livello_invio` per analisi futura del tasso di mismatch. Smoke test su dati reali prod: scarto 4-10% fra stima statica ed empirica, FAU_00 ~30-35% più veloce delle altre istanze (coerente con essere la più sviluppata). Dettagli completi in memoria `project_tempo_raccolta_estimator.md`. |
| WU200ter | Collegamento osservativo tempo_raccolta_estimator ↔ adaptive scheduler | BASSA | ✅ IMPLEMENTATO 11/07/2026 (commit `7c97436`), su richiesta utente ("incominciamo a collegare senza regressione del sistema attuale"). `core/adaptive_scheduler.py::compute_slot_liberi_atteso()` calcola in aggiunta un campo `confronto_tempo_raccolta` (lista per invio: tipo, livello, `t_det_min` deterministico vs `t_emp_min` empirico vs `diff_min`) — **puramente additivo**, non influenza `t_residue_min`/`rientro_atteso`/`slot_liberi_atteso`/`score` (verificato con test dedicato `tests/unit/test_adaptive_scheduler_confronto.py`, stessi valori con o senza il confronto disponibile). Candidati a eliminazione futura una volta accumulati abbastanza campioni per cella: `core/t_marcia_calibration.py`, `config/predictor_t_l_max.json`, la formula (non il modulo) in `core/skip_predictor.py::_calc_t_marcia_min` — nessun cutover ancora deciso. |
| WU200bis | Pannello dashboard `/ui/report-raccolta` — validazione live report_raccolta + tempo_raccolta_estimator | MEDIA | ✅ IMPLEMENTATO 11-12/07/2026 (commit `898e33e`, `e52d896`, `f57e619` — **stessa etichetta WU200bis riusata per errore per il fix TTL/retention sopra**, disambiguare per hash commit). Richiesta utente: "puoi costruire una pannello di appoggio per visualizzare in questa fase?". Nuovo `dashboard/services/report_raccolta_reader.py` (read-only, pattern `stats_reader.py` no-cache) + 6 endpoint partial in `dashboard/app.py` + template `dashboard/templates/report_raccolta.html` (pattern page-shell + HTMX partial, come `predictor_istanze.html`) + link nav. **6 sezioni**: (1) riepilogo dataset (occupazioni/report/match/pending/mismatch%), (2) raccoglitori in volo con stima arrivo (`stima_tempo_raccolta`), (3) timeline eventi invio+completamento, (4) tempo di raccolta stimato aggregato per (istanza,tipo,livello) — **sostituita su richiesta utente** ("l'ultimo pannello la durata di ogni raccolta non mi sembra utile") al posto della lista raw per-match iniziale, ora mediana/media/n/range con soglia di affidabilità coerente a `MIN_CAMPIONI_CELLA`; (5)-(6) produzione oraria unificata per istanza + totale farm (riusa `get_produzione_istanze()`/`shared/prod_unificata.py`, nessun nuovo calcolo). Smoke-test contro dati reali prod ad ogni iterazione (read-only). |
| WU197 | Dashboard — pannello "simulazione ordine adattivo" 45s per risposta, 3 scanner ridondanti di `istanza_metrics.jsonl` | ALTA | ✅ RISOLTA 07/07/2026. Richiesta utente: "simulazione ordine adattivo compare con molta lentezza, verifica la lentezza della dashboard". Misurato `preview_adaptive_scheduler()` (`GET /ui/partial/adaptive-scheduler-preview`, poll ogni 30s da `predictor_istanze.html`) su dati reali prod: **45.3s** per una singola risposta (panel quindi quasi sempre "mid-fetch", mai davvero aggiornato entro il poll interval). **Root cause**: `core/adaptive_scheduler.py::ordina_istanze_adaptive` è un greedy O(n²) (~66-78 chiamate per 11-12 istanze) che ad ogni candidato-per-step chiama `compute_slot_liberi_atteso` → `core/skip_predictor.py::load_metrics_history()`, la quale **rileggeva l'intero file `data/istanza_metrics.jsonl` (6903 righe, 5.8MB) da zero ad OGNI chiamata**, senza cache (misurato 428ms/call → 28.2s per 66 chiamate). Scavando oltre, trovati **altri 2 scanner indipendenti** dello stesso file con lo stesso bug (nessuna cache, uno scan completo per istanza): `core/skip_predictor.py::_l2_collect_samples` (usato da `predict_slot_liberi_l2`, chiamato da `predict_cycle_duration` per il task raccolta/raccolta_fast, ~350ms×12) e `core/cycle_duration_predictor.py::_read_units_history` (per rifornimento, stesso pattern) — entrambi raggiunti dal calcolo `_stima_durata_istanza_min` → `predict_cycle_from_config(strict_schedule=True)`, che da solo misurava **11.6-13.1s per chiamata, SEMPRE cold** (nessuna cache condivisa nonostante `_get_stats()` ne avesse una propria da 30min, irrilevante qui). **Fix**: nuovo `core/skip_predictor.py::_load_metrics_index()` — indice `{istanza: [record,...]}` dell'intero file, cache invalidata su cambio `mtime` (non TTL: zero staleness percepibile, il file cresce di poche righe per tick). `load_metrics_history()` e `_l2_collect_samples()` riscritte per attingere da questo indice condiviso; `core/cycle_duration_predictor.py::_read_units_history()` importa lo stesso indice cross-modulo. Risultato misurato: `load_metrics_history`×66 28.2s→0.42s, `predict_cycle_from_config` cold 13.1s→1.2s (warm 11.6s→0.2s), `preview_adaptive_scheduler()` end-to-end **45.3s→1.2s a processo freddo, ~0.1-0.2s a cache calda** (36-450×). Validato: output di `_l2_collect_samples`/`_read_units_history` confrontato bit-a-bit vecchio vs nuovo su tutte le 12 istanze/3 task su dati reali prod — zero mismatch. Suite pytest 573/140/4err invariata (nessun test dedicato ai moduli `core/` toccati). Sync dev+prod verificato byte-identico. **Effetto**: richiede riavvio del processo DASHBOARD (`run_dashboard_prod.bat`, servizio separato dal bot — nessun riavvio bot necessario per questo fix specifico, sebbene `core/skip_predictor.py` sia condiviso anche da `tasks/raccolta.py`/`raccolta_fast.py` e quindi benefici anche lì al prossimo riavvio bot già pianificato per WU195/196 nella stessa sessione). |
| WU191 | Adaptive scheduler — 3 fix su predizione slot liberi (finestra 59gg, bucket, anchoring) | ALTA | ✅ RISOLTA 05/07/2026. Richiesta utente: verifica funzionalità adaptive scheduler sui cicli LIVE (03/07-04/07, log `bot.log`+`.bak`, uniche 2 sessioni conservate) incrociando `slot_liberi_atteso` predetto con `attive_pre` reale (OCR HOME) — solo **28% match esatto** su 104 confronti validi (40% sottostima, 32% sovrastima, delta medio -0.09). **3 cause individuate**: (1) `core/empirical_slot_predictor.py::_build_lookup()` non aveva MAI un limite temporale — scansionava tutto lo storico (fino a 59 giorni) mescolando regimi radicalmente diversi (switch `raccolta_fast→full` del 09/05 WU143, crescita truppe/livelli); il lookup pesa 70% del blend non appena `n_samples≥30` (soglia raggiunta da settimane, oggi 540-580 campioni/istanza) quindi lo scheduler girava quasi sempre al minimo di segnale "fresco". Fix: `WINDOW_DAYS=14` + soglia `MIN_SAMPLES=5` (sotto la quale ritorna `None` invece di una mediana rumorosa). (2) Bucket gap troppo grossolani (`>120min` sconfinato, assorbiva la maggioranza dei cicli reali da 150-220min con IQR quasi pari all'intero range slot — verificato `p25=3 p75=5` su 5 slot totali). Fix: 7 fasce fino a 240min + nuove funzioni pubbliche `get_full_lookup()`/`bucket_labels()` per **eliminare la copia duplicata** in `dashboard/app.py` (bucket propri definiti l'8/05, mai più aggiornati — commento "raramente >180min" falso con l'adaptive scheduler LIVE, bug collaterale corretto: path prod hardcoded ora rispetta `DOOMSDAY_ROOT`). (3) `core/adaptive_scheduler.py::compute_slot_liberi_atteso` ancorava il residuo T_marcia al `ts` di fine-tick del record precedente invece che al `ts_invio` reale di ciascuna marcia (disponibile ma non usato) — sottostimava l'elapsed per le marce partite a inizio di un tick lungo, spiegando lo sbilanciamento 40%/32% osservato. Fix: anchoring per-marcia su `ts_invio`, fallback al comportamento precedente se assente (dati storici). **Verificato**: `ts_invio` è già catturato dopo la conferma reale della marcia (`tasks/raccolta.py::_esegui_marcia`, post-verifica "maschera chiusa"), non al tap iniziale — nessuna ulteriore correzione necessaria lì. Smoke test isolato (sandbox `DOOMSDAY_ROOT` dedicato) 3/3 scenari verdi, suite pytest 573/713 invariata (nessuna regressione). Sync dev+prod, commit `6889a88`. Effetto al prossimo restart bot. |
| WU186 | Retention automatica file JSONL predittivo (60gg) | MEDIA | ✅ RISOLTA 02/07/2026 — `tools/rotate_predictor_logs.py` (WU168) esisteva ma era SOLO manuale, mai eseguito in prod (`istanza_metrics.jsonl` 5.4MB/6.619 righe, nessun `data/archive/`). Estratta `run_retention()` riutilizzabile dal tool (usata sia da CLI sia da chiamante programmatico), aggiunto `data/predictions/scheduler_ab.jsonl` ai target (stesso problema, nessuna retention nemmeno manuale). Nuovo background task `dashboard/app.py::_predictor_retention_loop` (stesso pattern di `_predictor_recorder_loop`/`_nodi_mappa_rebuild_loop`): check ogni 30min, esegue rotazione 1×/die se non già fatto oggi (persistenza `data/predictor_retention_state.json`), cutoff `PREDICTOR_RETENTION_DAYS=60`. Smoke test sandbox isolata: righe >60gg archiviate in `data/archive/<file>_<YYYY-MM>.jsonl`, righe recenti mantenute nel file live — verificato bit-a-bit. Richiede riavvio dashboard per attivare il loop (nessun riavvio bot necessario, file toccati solo da dashboard+bot in scrittura append, mai dal bot in lettura esclusiva) |
| WU168 | Adaptive scheduler — 3 fix dataset/calibrazione predictor | ALTA | ✅ RISOLTA 19/06/2026, scoperta durante analisi proposta dall'utente ("recap predictor, qual è il dataset?" → "proposta migliorativa" → "implementa tutti i punti"). **Bug critico scoperto durante l'implementazione** (non nella proposta iniziale): la calibrazione closed-loop T_marcia (`core/t_marcia_calibration.py`, proposta B 08/05) è sempre stata **completamente inerte** dal giorno della sua introduzione — 0 campioni, `coef` sempre 1.0 per ogni (istanza, livello). Causa, **doppio bug cumulativo** in `core/istanza_metrics.py::imposta_adaptive_scheduler_meta`: (1) scriveva nel buffer la chiave `"adaptive_scheduler"` ma il reader (`t_marcia_calibration.py`) legge `"adaptive_scheduler_meta"` (mismatch mai notato); (2) la chiave non era nella whitelist di `chiudi_tick()` che filtra cosa va su disco — il dato restava solo in memoria. (3) anche risolvendo 1+2, l'hook viene chiamato dal main loop PRIMA che `_thread_istanza` chiami `inizia_tick()` per quell'istanza (lo scheduler calcola l'ordine di TUTTE le istanze prima di spawnare i thread) → buffer sempre `None` al momento della chiamata. Fix: chiave corretta + whitelist aggiornata + area di staging `_PENDING_SCHEDULER_META` consumata da `inizia_tick()` alla creazione del buffer, qualunque sia l'ordine di chiamata. Verificato end-to-end con la sequenza reale (meta prima, inizia_tick dopo) su sandbox isolata. **Fix #2 — scoping calibrazione**: `core/skip_predictor.py::_calc_t_marcia_min` applicava `coef` all'intera `T_marcia = 2×eta + sat×T_L_max`, correggendo anche `eta_marcia` che è una MISURA OCR diretta per singolo invio, non una stima — il coefficiente aggregato per (istanza,livello) ci introduceva rumore inutile. Ora `coef` moltiplica solo il termine `sat×T_L_max`. Aggiunto anche `effective_t_l_max` (informativo, `base×coef`) nell'output di `compute_calibration()` per vedere se il baseline manuale `config/predictor_t_l_max.json` è driftato — NON sovrascritto automaticamente (rischio doppia correzione). **Fix #3 — smoothing cliff**: `core/adaptive_scheduler.py::_blend_alpha` era a gradini netti (n=4→α=1.0, n=5→α=0.7: salto di 0.3 per un solo campione in più, poteva far cambiare bruscamente lo score/ordine delle istanze senza nulla di reale cambiato). Sostituito con interpolazione lineare continua, stessi estremi (α=1.0 a n=0, α=0.3 a n≥30). **Fix #4 — igiene storage**: `data/predictions/cycle_snapshots.jsonl` pesava 6.3MB/3.649 righe (più del doppio di `istanza_metrics.jsonl` con meno della metà delle righe) perché ogni snapshot (ogni 15min) duplicava per intero `input_context` (liste istanze/task quasi sempre identiche). Dedup write-side in `record_snapshot()` (`"input_context_ref":"same_as_prev"` se identico al precedente) + resolver read-side trasparente (`_read_snapshots_resolved()`, unico punto di risoluzione) — zero modifiche ai 3 consumer esistenti in `dashboard/app.py`. Aggiunto anche `tools/rotate_predictor_logs.py` (CLI manuale, dry-run default, archivia per mese le righe più vecchie di `--days`, sicuro con bot live perché ogni write è un `open("a")` discreto) — testato su sandbox isolata, ricostruzione bit-a-bit identica all'originale. Nessun test dedicato (repo non testa unitariamente `core/`, solo `tasks/*.py`); validazione via sandbox/dry-run manuali. Sync dev+prod. Effetto del fix #1 (closed-loop ora vivo) osservabile solo dopo accumulo di nuovi campioni post-fix (richiede ≥5 osservazioni per coppia istanza/livello). |
| 39 | Flag abilitata istanza applicato solo a fine ciclo (fino ~2h ritardo mid-cycle) | MEDIA | ✅ RISOLTA 23/04 (hot-check runtime_overrides prima di reset_istanza in main loop) |
| 53 | Telemetria task & dashboard analytics — events JSONL + rollup + KPI | — | ✅ CHIUSA 27/04 (WU38-44 — pipeline 8/8 step, 9 commit `5153733`→`399eba0`, 19/19 test verdi). Vedi sezione "Sessione 27/04/2026" in ROADMAP.md |
| 47 | Pannello dashboard 'produzione/ora' sempre vuoto — `metrics.*_per_ora` mai popolati | MEDIA | ✅ RISOLTA 27/04 (WU47 commit `aeaa6fb` — `chiudi_sessione_e_calcola()` propaga `prod_ora` a `metrics.aggiorna_risorse()` se durata≥300s) |
| 49 | Cicli persistenti su file dedicato + numerazione globale crescente | MEDIA | ✅ RISOLTA 27/04 (WU46 `41711bd` storage `data/telemetry/cicli.json` + hooks main.py + pannello dashboard `📚 storico cicli`. WU48 `6498d11` numerazione globale + run_id + auto-close stale `aborted=True`) |
| 59 | Pannello "📚 storico cicli" colonna DATA | BASSA | ✅ RISOLTA 29/04 (WU59 — `CicloStorico.start_date` DD/MM UTC + colonna in tabella) |
| WU72 | Dashboard storico cicli — UTC raw vs locale (disallineamento card istanza + date sbagliate a cavallo mezzanotte UTC) | MEDIA | ✅ RISOLTA 30/04 notte (in `dashboard/services/telemetry_reader.get_storico_cicli`: helper `_ts_to_local_hhmm` + `_ts_to_local_date` convertono ISO UTC in ora locale via `datetime.fromisoformat().astimezone()`. Pre-fix: ciclo iniziato 22:58 UTC mostrato come `29/04 22:58` mentre card FAU_00 mostrava 01:02 locale. Post-fix: `30/04 00:58` coerente con card. Dashboard restartata) |
| WU86 | Pannello dashboard "ultimi 5 cicli" per istanza | BASSA | ✅ IMPLEMENTATA 01/05 (commit `6f48eaf`). In card produzione istanze nuova sezione che mostra gli ultimi 5 tick della singola istanza (avvio→chiusura, durata, outcome ok/cascade/abort). Dato letto da `data/telemetry/cicli.json` filtrato per istanza+ordinamento desc. HTMX refresh 60s |
| WU107 | Tick_sleep dashboard ignorato dal bot — fix conversione + lettura config | ALTA | ✅ RISOLTA 03/05 notte. Sintomo utente: dashboard mostra `tick_sleep_min=5` (5 min) ma bot girava ogni 60s (1 min). Verifica `data/telemetry/cicli.json` gap fra cicli 119-133 = 60s confermato. **Doppio bug**: (1) `config_loader._merge_globali` aliasa `tick_sleep_min` → `tick_sleep` SENZA conversione (5 → 5 secondi invece di 5×60=300); (2) `main.py::SLEEP_CICLO = args.tick_sleep` ignorava completamente il config — usava solo CLI (`run_prod.bat --tick-sleep 60`). **Fix**: (a) `config_loader.py:282-284` conversione esplicita `tick_sleep_min × 60 → tick_sleep` secondi; (b) `main.py` argparse default `--tick-sleep=-1` (sentinel "leggi da config") + nuova logica risoluzione: CLI esplicito > config > default 300s; (c) `run_prod.bat` rimosso `--tick-sleep 60` esplicito (lascia che il config decida). Log nuovi: `tick_sleep da config: 300s (=5.0min)` o `tick_sleep CLI esplicito: Ns`. Validazione: merge conferma 5 min → 300s. Effetto al prossimo restart bot. |
| WU-CycleDur | Cycle Duration Predictor — stima durata ciclo bot schedule-aware | MEDIA | ✅ IMPLEMENTATA 04/05 sera. Stimatore self-improving che calcola `T_ciclo = Σ(boot_home_median + Σ task_due_median) + tick_sleep` per ogni istanza basandosi su rolling stats ultimi 20 record da `data/istanza_metrics.jsonl`. **`predict_cycle_from_config(strict_schedule=True)`** filtra task per istanza in base a `state[istanza].schedule[task]` + `interval_hours` da `task_setup.json` + edge cases (es. `main_mission` gate UTC≥20). Cache TTL 30min. **2 file**: `core/cycle_duration_predictor.py` (~250 righe) + `tools/predict_cycle.py` CLI standalone. **Output prod 04/05**: T_ciclo strict 81.5min vs old 125.7min (delta -44min, eliminata sovrastima task non dovuti). Per-istanza breakdown ordinato desc per T_s. **Integrazione skip predictor**: `_predict_gap_minutes()` usa `predict_cycle_from_config()` come gap_atteso → decisioni più accurate. **Vincolo (memoria)**: aggiungere/modificare task in `task_setup.json` richiede aggiornamento `CLASS_TO_TASK_NAME` + edge cases `_is_task_due` in cycle_duration_predictor. |
| WU-CycleAccuracy | Cycle Predictor recorder + accuracy fine-ciclo + drilldown what-if | MEDIA | ✅ IMPLEMENTATA 04/05 sera. Recorder background-task in dashboard `lifespan` che ogni 15min: (1) `record_snapshot(predicted_min, n_istanze, confidence, input_context)` su `data/predictions/cycle_snapshots.jsonl` con auto-correlazione cycle_numero in corso; (2) `evaluate_cycles()` per cicli completati: `error_pct = |predicted - actual| / actual × 100` per ogni snapshot, output `cycle_accuracy.jsonl`. **Schema input_context**: istanze_abilitate, task_globali_abilitati, tasks_per_istanza_due, per_istanza_predicted_s, tick_sleep_s — permette analisi reproducibile + planning what-if. **Pagina dedicata `/ui/predictor`** (NEW): 3 sezioni: cycle predictor (corrente + drilldown what-if "T_ciclo se skippo X" + accuracy storica) + skip predictor (decisioni live + futuro accuracy) + configurazione helper. Nav link in topbar. Pannelli predictor rimossi da `/ui/telemetria` (consolidati in pagina dedicata). 5 file modificati: `core/cycle_predictor_recorder.py` NEW + dashboard app/templates. Restart dashboard richiesto per attivare background loop. |
| WU89-4 | Skip Predictor live hook (uscita da shadow-only Step 3) | MEDIA | ✅ IMPLEMENTATA 04/05 sera. Hook flag-driven nel `main.py::_thread_istanza` PRIMA dell'avvio MuMu (massimo saving). 2 flag schema esistenti: `globali.skip_predictor_enabled` (default False) + `globali.skip_predictor_shadow_only` (default True). 3 stati: (1) enabled=False → no-op completo, bot identico; (2) enabled=True+shadow=True → predict + log + telemetria, no skip; (3) enabled=True+shadow=False → predict + log + telemetria + se should_skip → skip ciclo (early return da `_thread_istanza`, no avvio MuMu, ~600s saving stimato). **Componenti**: module-level `_predictor_states: dict[str, IstanzaSkipState]` (in-memory, reset su restart) per guardrail tracking + helper `_append_predictor_decision(nome, decision, mode, applied)` scrive `data/predictor_decisions.jsonl` (alimenterà Step 5 dashboard precision/recall) + hook ~30 righe nel `_thread_istanza` con try/except failsafe. **0 modifiche** a orchestrator/launcher/task. **Workflow attivazione**: deploy → bot invariato (default off) → utente abilita shadow da dashboard → analizza decisions JSONL su 6-12 cicli → se OK abilita LIVE. Smoke test 3/3 scenari OK (proceed shadow, skip live applied, skip shadow not applied). Validazione: hot-reload flag attivo al prossimo tick, predictor 5 regole già validate Step 3 + master istanze sempre proceed. Effetto al prossimo restart bot. |
| WU123 | dashboard predictor — p75 prominente + drilldown breakdown expand | BASSA | ✅ IMPLEMENTATA 05/05 mattina. Commit `544ae05` (header p75 grande/median piccolo, validazione cycle_accuracy.jsonl errore 5-15% p75 vs 25-43% median sui cicli pieni); `3b28459` colonna NEW "task eseguiti (ultimo)" in `/ui/partial/cycle-snapshot-detail` con confronto pianificati vs reali (rosso=eseguito non pianificato, arancione barrato=pianificato non eseguito, ⚠N marker discordanze); `0d77717` riga expand breakdown stima cliccabile (boot_home + ogni task + wait inter-task + TOT). |
| WU124 | messaggi `_Esito.OK→COMPLETATO` (100% fail post-WU115) | ALTA | ✅ RISOLTA 05/05 mattina (commit `ca7b499`). Refuso da refactor WU115 (debug buffer): `tasks/messaggi.py:95-97` referenziava `_Esito.OK` ma classe definisce `COMPLETATO/SCHERMATA_NON_APERTA/ERRORE`. Telemetria 05/05: 36/36 fail (100%) con eccezione `AttributeError: type object '_Esito' has no attribute 'OK'`. Effetto: task faceva il claim alleanza+system poi crashava → `last_run` non aggiornato post-WU79 → rigirava ad ogni tick invece che ogni 4h. Saving ~13min/giorno × 12 istanze. Fix: 2 occorrenze sostituite, smoke test reload OK. |
| WU130 | Dashboard banner home — info restart automatico (3 trigger) | BASSA | ✅ IMPLEMENTATA 06/05 (commit `e55eb5b`). Pre-fix: banner home `/ui/partial/restart-banner` mostrava solo flag manuale (1° trigger). Post-fix: 3 stati ordinati: (1) flag manuale → banner blu "RESTART PENDENTE" + bottone annulla (esistente); (2) schedule e/o cicli max configurati → banner grigio info "🔄 restart automatico · ⏰ schedule HH:MM UTC (prossimo fra Xh) · ♻ cicli-max X/N (restano K)" + link a `/ui/config/global`; (3) nessun trigger → vuoto. Validazione 4 scenari A solo schedule, B solo cicli, C entrambi, D nessuno via TestClient. |
